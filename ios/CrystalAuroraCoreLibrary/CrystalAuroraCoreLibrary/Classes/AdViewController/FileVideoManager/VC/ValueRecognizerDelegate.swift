
//: Declare String Begin

/*: "cmd" :*/
fileprivate let showUserContent:[UInt8] = [0xdd,0xd3,0xda]

private func attributeLeading(gesture num: UInt8) -> UInt8 {
    return num ^ 190
}

/*: "cancelCall" :*/
fileprivate let dream_bottomValue:String = "caarraycel"
fileprivate let dreamMomentErrorViewValue:String = "Callbackground true input"

/*: "data" :*/
fileprivate let mainSectionValue:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "logId" :*/
fileprivate let show_equalData:String = "guard instance audiencelogId"

/*: "#322870" :*/
fileprivate let appDeleteContent:[Character] = ["#","3","2","2","8","7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let appHalfPopData:String = "Tap toplay record title temp character"
fileprivate let m_equalKey:String = "video page successfully push bag"
fileprivate let showDataLayerValue:[Character] = ["c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let notiImageUrl:String = "icowill"
fileprivate let noti_dataUrl:[Character] = ["r"]

/*: "icon_views_number" :*/
fileprivate let mEraseStr:String = "icon_vvar return"
fileprivate let app_pathMessage:[Character] = ["n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let app_byStr:[Character] = ["%","@"," ","c","h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let user_enableToName:String = "icraten"

/*: "icon_rank_coin" :*/
fileprivate let m_tagFormat:String = "change guardicon_ran"
fileprivate let app_toolValue:String = "value for class transform falsek_coin"

/*: "vip_btn_top" :*/
fileprivate let mActionUrl:[Character] = ["v","i","p","_","b"]
fileprivate let m_businessName:[Character] = ["t","n","_","t","o","p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let userTableName:String = "btn_neframe gift key succeed"
fileprivate let kUserEnterManagerKey:String = "model moment section_sett"
fileprivate let showVideoMsg:String = "inshare"
fileprivate let appHiddenId:String = "s_noruser female page"

/*: "%@ Coins/min" :*/
fileprivate let appEqualKey:[Character] = ["%","@"," ","C","o","i","n","s","/","m","i","n"]

/*: "get json error" :*/
fileprivate let show_emptyName:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r"]
fileprivate let dream_dataValue:String = "resultr"

/*: "matchId" :*/
fileprivate let notiIndexKey:String = "matchIdsucceed item"

/*: "errno" :*/
fileprivate let noti_sexData:[UInt8] = [0xd9,0xe6,0xe6,0xe2,0xe3]

fileprivate func willData(equal num: UInt8) -> UInt8 {
    let value = Int(num) - 116
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let notiUserValue:String = "image tapVIPFee"

/*: "Matching now" :*/
fileprivate let dream_blockUrl:[Character] = ["M","a","t","c","h","i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let k_modeData:[Character] = ["u","s","e","r","T","o"]
fileprivate let showNameFormat:String = "UCH"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let showIncomeId:String = "color value contentmf/"
fileprivate let m_beanValueStr:[Character] = ["f","r","e","e","C","a","l","l","/","g","e","t","V","i","d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let user_videoDateContent:[UInt8] = [0xa3,0xb7,0xaa,0xa8,0x91,0xbc,0xb5,0xa0]

private func tagPathLab(handle num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "You've been barred from receiving calls" :*/
fileprivate let noti_extraEnableKey:[UInt8] = [0x79,0x4f,0x55,0x7,0x56,0x45,0x0,0x42,0x45,0x45,0x4e,0x0,0x42,0x41,0x52,0x52,0x45,0x44,0x0,0x46,0x52,0x4f,0x4d,0x0,0x52,0x45,0x43,0x45,0x49,0x56,0x49,0x4e,0x47,0x0,0x43,0x41,0x4c,0x4c,0x53]

private func blockPending(color num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "source" :*/
fileprivate let user_textEffectMsg:[UInt8] = [0xe7,0xfb,0xe1,0xe6,0xf7,0xf1]

private func equalUser(icon num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "type" :*/
fileprivate let notiErrorUrl:[UInt8] = [0x66,0x6b,0x62,0x57]

fileprivate func approximatelyStar(page num: UInt8) -> UInt8 {
    let value = Int(num) - 242
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let mItemName:[UInt8] = [0x69,0x5d,0x58]

fileprivate func conBackground(view num: UInt8) -> UInt8 {
    let value = Int(num) + 12
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "fromFreeCall" :*/
fileprivate let user_commitImageName:String = "fromFsection of status for intimate"

/*: "requestCall" :*/
fileprivate let dreamSendName:String = "recontentue"
fileprivate let kPressMsg:String = "LL"

/*: "onVideoMatch" :*/
fileprivate let userIndicatorStr:String = "onVidif path corner raw else"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let main_leadingMsg:[UInt8] = [0x77,0x5d,0x91,0x90,0x8b,0x89,0x7d,0x90,0x85,0x7f,0x3c,0x92,0x85,0x80,0x81,0x8b,0x79,0x3c,0x6e,0x81,0x7f,0x81,0x85,0x92,0x81,0x3c,0x92,0x85,0x80,0x81,0x8b,0x3c,0x7f,0x7d,0x88,0x88,0x3c,0x85,0x8a,0x7f,0x8b,0x89,0x85,0x8a,0x83]

fileprivate func translateNor(render num: UInt8) -> UInt8 {
    let value = Int(num) + 228
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let mOnIdent:String = "refuseCmode from live current model"
fileprivate let notiFireKey:String = "customll"

/*: "onCancelCall" :*/
fileprivate let main_fillLeadingFormat:String = "no ononCa"
fileprivate let show_bottomPathMsg:String = "label ok visible forncelCall"

/*: "retryAfter" :*/
fileprivate let showLockStr:String = "mask mode move aspectretryA"
fileprivate let dreamViewMsg:String = "ftbutton"

/*: "onRequestCall" :*/
fileprivate let dream_userFormat:String = "data"
fileprivate let user_fileMsg:String = "nRequenote let to"

/*: "failType" :*/
fileprivate let mainCellMsg:[Character] = ["f","a","i","l","T","y"]
fileprivate let app_bubbleQuantityPath:[Character] = ["p","e"]

/*: "callWaitingTime" :*/
fileprivate let m_makeName:String = "callWaitmini self self gesture model"
fileprivate let noti_jumpMsg:String = "false index show no targetingTime"

/*: "toUid" :*/
fileprivate let m_moveTitle:String = "push make back lettoUid"

/*: "Free 1 min" :*/
fileprivate let noti_iconKey:String = "f"
fileprivate let notiPrepareResultStr:String = "title faceree "

/*: "%@/min" :*/
fileprivate let app_topViewKey:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ValueRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class ValueRecognizerDelegate: StateViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.barPrice()
            }
        }
        //: self.resetCamera()
        self.appSystem()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        sendStyle()
        //: setupSubViewsConstraint()
        viewsFillInSetupConstraint()
        //: bindInteraction()
        cling()
        //: videoManager.startPreview(isMatch: true)
        videoManager.clickOf(isMatch: true)
        //: setPlayer(isRandon: false)
        toolRandon(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            skirt()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: showUserContent.map{attributeLeading(gesture: $0)}, encoding: .utf8)!: (dream_bottomValue.replacingOccurrences(of: "array", with: "n") + String(dreamMomentErrorViewValue.prefix(4))), String(bytes: mainSectionValue.reversed(), encoding: .utf8)!: [(String(show_equalData.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        anonymous()
        //: if TalkingSocketManager.shared.isWork == false {
        if EffectSocketDelegate.shared.isWork == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            EffectSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            EffectSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            EffectSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            EffectSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ConstraintChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = ConstraintChatManager()
        //: let info = TalkingVideoChatModel()
        let info = LibraryChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .startMake()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(skirt), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kBlockRateValue + 1, height: kHalfText + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(appDeleteContent)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(appHalfPopData.prefix(6)) + " start " + String(m_equalKey.prefix(6)) + String(showDataLayerValue)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.namedImageNameBundle(name: (notiImageUrl.replacingOccurrences(of: "will", with: "n") + "_close_no" + String(noti_dataUrl))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(doMessage), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: RowView = {
        //: let btn = TalkingButton()
        let btn = RowView()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.namedImageNameBundle(name: (String(mEraseStr.prefix(6)) + "iews_" + String(app_pathMessage))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.namedImageNameBundle(name: (String(mEraseStr.prefix(6)) + "iews_" + String(app_pathMessage))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.fontFile(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(app_byStr)).localizedDataArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.namedImageNameBundle(name: (user_enableToName.replacingOccurrences(of: "rate", with: "o") + "_lounge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.namedImageNameBundle(name: (String(m_tagFormat.suffix(8)) + String(app_toolValue.suffix(6))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.fontFile(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.namedImageNameBundle(name: (String(mActionUrl) + String(m_businessName)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: RowView = {
        //: let btn = TalkingButton()
        let btn = RowView()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(marginalCost), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.namedImageNameBundle(name: (String(userTableName.prefix(6)) + "arby" + String(kUserEnterManagerKey.suffix(5)) + showVideoMsg.replacingOccurrences(of: "share", with: "g") + String(appHiddenId.prefix(5)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.namedImageNameBundle(name: (String(m_tagFormat.suffix(8)) + String(app_toolValue.suffix(6))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .career()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(appEqualKey)).localizedDataArguments(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.fontFile(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: CloudThen = //: return CloudThen(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func toolRandon(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = LineReactiveCompatible.default.failure(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(show_emptyName) + dream_dataValue.replacingOccurrences(of: "result", with: "ro")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func cellErrno(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(notiIndexKey.prefix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: showUserContent.map{attributeLeading(gesture: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: noti_sexData.map{willData(equal: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        FileVideoManager.ergodicForCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.fromVideo()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(notiIndexKey.prefix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func clickItem() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        FileVideoManager.portland { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(notiUserValue.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.barPrice()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func marginalCost() {
        //: SucceedInfoReactiveCompatible.share.func__pushToSubscribeAlert()
        SucceedInfoReactiveCompatible.share.funcClickParams()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func doMessage() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        labelCamera()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kTopTableValue.audienceTap(eventID: kNoData)

        //: if ModeStageReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if ModeStageReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.generate()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        startScreen()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func skirt() {
        //: guard TalkingSocketManager.shared.isWork == false else {
        guard EffectSocketDelegate.shared.isWork == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.actionKey(showMsg: kNearbyData)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        possessEnable()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(dream_blockUrl)).localized
        //: setPlayer(isRandon: true)
        toolRandon(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if ModeStageReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if ModeStageReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kTopTableValue.audienceTap(eventID: kNetFormalValue)
            //: req_getVideoInfo()
            littleness()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kTopTableValue.audienceTap(eventID: k_screenName)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            EffectSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            EffectSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            EffectSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            EffectSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            makeErrno()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func makeErrno(cmd: String = (String(k_modeData) + showNameFormat.lowercased()), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        anonymous()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        cellErrno(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func fromVideo() {
        // 停止定时器
        //: destroyWaitingTimer()
        anonymous()
        //: showCloseBtn()
        labelCamera()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(appHalfPopData.prefix(6)) + " start " + String(m_equalKey.prefix(6)) + String(showDataLayerValue)).localized
        //: setPlayer(isRandon: false)
        toolRandon(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isWork == false {
        if EffectSocketDelegate.shared.isWork == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            EffectSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            EffectSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            EffectSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            EffectSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        barPrice()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func anonymous() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func littleness() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SmallnessThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(showIncomeId.suffix(3)) + String(m_beanValueStr))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: user_videoDateContent.map{tagPathLab(handle: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kUseSenseName.underway(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.fromVideo()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: ModeStageReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    ModeStageReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.skirt()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.actionKey(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = ConversationTransformable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = ErrorPlayListener()
                //: player.setMute(bEnable: true)
                player.empower(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.addFresh(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                SourceInfoReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                PounderReactiveCompatible.goOverDown { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.voiceThrough()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func voiceThrough() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kTopTableValue.audienceTap(eventID: kTopName)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard SourceInfoReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.actionKey(showMsg: String(bytes: noti_extraEnableKey.map{blockPending(color: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            SourceInfoReactiveCompatible.shared.userSufficient(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.fromVideo()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = WorkObjectProtocol()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.startScreen()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.fromVideo()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func possessEnable() {
        //: if ModeStageReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if ModeStageReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(ModeStageReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(ModeStageReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func labelCamera() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func appSystem() {
        //: guard TalkingSocketManager.shared.isWork == false else {
        guard EffectSocketDelegate.shared.isWork == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.clickOf()
        }
    }
}

// MARK: - DetailMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension ValueRecognizerDelegate: DetailMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func somebody(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = MenuPresentHandyJSON.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(notiIndexKey.prefix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: user_textEffectMsg.map{equalUser(icon: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: notiErrorUrl.map{approximatelyStar(page: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: mItemName.map{conBackground(view: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = RenameEraseWidthTotal.randow
        //: if ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && ModeStageReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && ModeStageReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(user_commitImageName.prefix(5)) + "reeCall"))
            //: TalkingSocketManager.shared.isFreeCall = true
            EffectSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: showUserContent.map{attributeLeading(gesture: $0)}, encoding: .utf8)!: (dreamSendName.replacingOccurrences(of: "content", with: "q") + "stCa" + kPressMsg.lowercased()), String(bytes: mainSectionValue.reversed(), encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        EffectSocketDelegate.shared.crosscutNameReason(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: showUserContent.map{attributeLeading(gesture: $0)}, encoding: .utf8)!: (dream_bottomValue.replacingOccurrences(of: "array", with: "n") + String(dreamMomentErrorViewValue.prefix(4))), String(bytes: mainSectionValue.reversed(), encoding: .utf8)!: [(String(show_equalData.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.makeErrno(cmd: (String(userIndicatorStr.prefix(5)) + "eoMatch"))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func rename(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(show_equalData.suffix(5)))].intValue
    }
}

// MARK: - ErrorCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension ValueRecognizerDelegate: ErrorCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func nameData(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func variety(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func honkyTonk(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func actualizeObject(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            SourceReactiveCompatible.nameUrl(body: String(bytes: main_leadingMsg.map{translateNor(render: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = MenuPresentHandyJSON.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            textUp(toUid: String(uid))
        }
        //: stop_randomVideo()
        fromVideo()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.upResource()
        //: ModeStageReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        ModeStageReactiveCompatible.share.logLocation(info: data, situation: .Calling)
    }
}

// MARK: - CountelligenceObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ValueRecognizerDelegate: CountelligenceObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func clickData(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        makeErrno(cmd: (String(mOnIdent.prefix(7)) + notiFireKey.replacingOccurrences(of: "custom", with: "a")))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func judge(data _: [String: Any]) {}
}

// MARK: - SizeObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension ValueRecognizerDelegate: SizeObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func ordinalColorObserver(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(main_fillLeadingFormat.suffix(4)) + String(show_bottomPathMsg.suffix(8))) { // 取消通话异常
            //: stop_randomVideo()
            fromVideo()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(userIndicatorStr.prefix(5)) + "eoMatch"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            fromVideo()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(notiIndexKey.prefix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.actionKey(showMsg: msg)
            //: uploadFailTime(type: 2)
            illustration(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(userIndicatorStr.prefix(5)) + "eoMatch"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(showLockStr.suffix(6)) + dreamViewMsg.replacingOccurrences(of: "button", with: "er"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(notiIndexKey.prefix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.makeErrno(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (dream_userFormat.replacingOccurrences(of: "data", with: "o") + String(user_fileMsg.prefix(6)) + "stCall"), errorNo == VideoCustomReflectable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            fromVideo()
            //: guard ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue else { return }
            //: SucceedInfoReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            SucceedInfoReactiveCompatible.share.transition(clickEvent: k_postName, sufficient: false)
            //: uploadFailTime(type: 1)
            illustration(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        makeErrno(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    //: func uploadFailTime(type: Int) {
    func illustration(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(mainCellMsg) + String(app_bubbleQuantityPath))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(m_makeName.prefix(8)) + String(noti_jumpMsg.suffix(7)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kTopTableValue.playerOpen(eventID: kTimeTitle, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func textUp(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(m_makeName.prefix(8)) + String(noti_jumpMsg.suffix(7)))] = time
        //: parm["toUid"] = toUid
        parm[(String(m_moveTitle.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kTopTableValue.playerOpen(eventID: k_statusTitle, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func barPrice() {
        //: guard ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !ModeStageReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !ModeStageReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = RenameEraseWidthTotal.randow
        //: if ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && ModeStageReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes > 0, ModeStageReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if ModeStageReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if ModeStageReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (noti_iconKey.uppercased() + String(notiPrepareResultStr.suffix(4)) + "1 min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = ModeStageReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = ModeStageReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(app_topViewKey)).localizedDataArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(app_topViewKey)).localizedDataArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.dismissProgress(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = ModeStageReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = ModeStageReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = ModeStageReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = ModeStageReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !ModeStageReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !ModeStageReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func sendStyle() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func viewsFillInSetupConstraint() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(kPositionName + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func cling() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(barPrice),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: kInviteName,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(clickItem),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kKeyMessageTitle,
                                               //: object: nil)
                                               object: nil)
    }
}
