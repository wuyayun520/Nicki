
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let showTalkId:[UInt8] = [0x6f,0x7e,0x6d,0x6b,0x66,0x60,0x6c,0x6b,0x7e,0x6b,0x6a,0x6c,0x60,0x7c,0x77,0x7e,0x71,0x78,0x7a,0x60,0x71,0x70,0x6b,0x76,0x79,0x76,0x7c,0x7e,0x6b,0x76,0x70,0x71]

private func lastMedium(layer num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "p- :*/
fileprivate let user_labCellKey:String = "true double data index sharedp-"

/*: - :*/
fileprivate let mainGroupRateValue:String = "-"

/*: "redirectUrl" :*/
fileprivate let showUserIdent:String = "retopre"
fileprivate let app_shareValue:String = "ctUrllet image"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let main_stateTitle:[Character] = ["V","O","I","C","E","R","O","O","M",":"]
fileprivate let kQuantityimateUrl:String = " \u{8fdb}房\u{6a21}型解"

/*: "Party room exception" :*/
fileprivate let mainTablePath:[Character] = ["P","a","r","t","y"," "]
fileprivate let notiInfoMsg:[Character] = ["r","o","o","m"," ","e","x","c","e","p","t","i","o","n"]

/*: _ :*/
fileprivate let mPageIdent:[Character] = ["_"]

/*: "male" :*/
fileprivate let dream_leadingName:[UInt8] = [0xf3,0xff,0xf2,0xfb]

private func nameColor(model num: UInt8) -> UInt8 {
    return num ^ 158
}

/*: "female" :*/
fileprivate let notiObjectContent:[Character] = ["f","e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let k_frameMessage:String = "VOIblockE"
fileprivate let appModelName:String = "\u{7fa4}聊进房"
fileprivate let dream_managerLabelFormat:String = "upId: if table class video view"

/*: ." :*/
fileprivate let m_requestStatusTitle:String = "."

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let showIconFormat:String = "VOICEburnO"
fileprivate let dreamBagMessage:String = "OM: 切房reason view"
fileprivate let main_infoKey:[Character] = ["模","型","解","析","失","败","。"]

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let mainReData:String = "kitOICE"
fileprivate let dreamLogTransformIdent:String = ": \u{7fa4}聊"
fileprivate let userCellGiftUrl:String = "。groupfatal top value scope"

/*: "errCode" :*/
fileprivate let dream_pageIdent:[UInt8] = [0x5d,0x6a,0x6a,0x3b,0x67,0x5c,0x5d]

fileprivate func makeSum(index num: UInt8) -> UInt8 {
    let value = Int(num) - 248
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "custom_kickout_message" :*/
fileprivate let m_bottomId:String = "cuviewt"
fileprivate let main_contentAttachTitle:String = "ckout_video extension self"
fileprivate let main_minPath:String = "meinfoage"

/*: "closeRoom" :*/
fileprivate let user_toName:String = "clotime"
fileprivate let dream_rangeMessage:[Character] = ["m"]

/*: "adminCloseRoom" :*/
fileprivate let dreamIndexRegularTitle:String = "adminCloslength type blue"
fileprivate let appSearchedResultUrl:String = "live port vieweRoom"

/*: "The room is closed, please go to another room" :*/
fileprivate let noti_offPath:[UInt8] = [0x10,0x2c,0x21,0x64,0x36,0x2b,0x2b,0x29,0x64,0x2d,0x37,0x64,0x27,0x28,0x2b,0x37,0x21,0x20,0x68,0x64,0x34,0x28,0x21,0x25,0x37,0x21,0x64,0x23,0x2b,0x64,0x30,0x2b,0x64,0x25,0x2a,0x2b,0x30,0x2c,0x21,0x36,0x64,0x36,0x2b,0x2b,0x29]

private func startNeed(show num: UInt8) -> UInt8 {
    return num ^ 68
}

/*: "OK" :*/
fileprivate let dreamPhoneUrl:[Character] = ["O","K"]

/*: "You were kicked out of the room" :*/
fileprivate let show_layerTitle:[UInt8] = [0x68,0x5e,0x44,0x11,0x46,0x54,0x43,0x54,0x11,0x5a,0x58,0x52,0x5a,0x54,0x55,0x11,0x5e,0x44,0x45,0x11,0x5e,0x57,0x11,0x45,0x59,0x54,0x11,0x43,0x5e,0x5e,0x5c]

private func columnShare(image num: UInt8) -> UInt8 {
    return num ^ 49
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let show_firstKey:[Character] = ["V","O","I","C","E","R","O","O","M","：","进","房","失","败","\u{ff0c}","r","o"]
fileprivate let app_withUrl:String = "click pathomID: "

/*: , error:  :*/
fileprivate let k_bagName:String = ", eview hidden text"
fileprivate let user_colorData:String = "to color appear toolrror: "

/*: , reason:  :*/
fileprivate let show_sharedStr:String = ", rbox make"
fileprivate let dream_frameMsg:String = "center fileeason: "

/*: , extendedData:  :*/
fileprivate let show_livePath:String = "item case quit make, ex"
fileprivate let noti_makeUserText:String = "let in equal opena: "

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let kNorMainName:String = "VOIinput"
fileprivate let noti_toId:String = "M: 当\u{524d}"
fileprivate let m_styleName:String = "\u{5931}败\u{ff0c}r"

/*: , errorCode:  :*/
fileprivate let show_itemStr:String = "label second, err"
fileprivate let mainMakeValue:String = "sample collection buttonorCo"

/*: , streamID:  :*/
fileprivate let noti_textVoiceData:[Character] = [","," ","s","t","r","e","a","m"]
fileprivate let appTopKey:[Character] = ["I","D",":"," "]

/*: "An error occurred, please try again" :*/
fileprivate let show_viewId:[UInt8] = [0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x72,0x72,0x75,0x63,0x63,0x6f,0x20,0x72,0x6f,0x72,0x72,0x65,0x20,0x6e,0x41]

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let show_dataValue:[Character] = ["V","O","I","C","E","R"]
fileprivate let dreamColorText:[Character] = ["O","O","M",":"," ","拉","流","失","败","，","r","o","o","m","I","D",":"," "]

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let appSexId:String = "offICER"
fileprivate let mainBalanceFirstId:String = "令解\u{6790}失"
fileprivate let show_shareValue:String = "mmand: model make any"

/*: , roomID:  :*/
fileprivate let appSaveResultMessage:String = ", rooview remove"
fileprivate let m_recordName:String = "remove managermID: "

/*: "data" :*/
fileprivate let showMakeData:String = "dainfoa"

/*: "position" :*/
fileprivate let m_contentTitle:String = "pmessagesit"
fileprivate let main_defineImageId:String = "ioadd"

/*: "xxxx--  :*/
fileprivate let mReportMessage:[Character] = ["x","x","x","x","-"]
fileprivate let kActionUpId:String = "gift "

/*: "opType" :*/
fileprivate let user_layerPath:String = "opTypemake color"

/*: "party:changeMike" :*/
fileprivate let appBeyondId:[Character] = ["p","a","r","t","y",":","c","h","a","n","g","e","M","i"]
fileprivate let dream_logId:[Character] = ["k","e"]

/*: "type" :*/
fileprivate let dreamScaleStr:String = "tyfilee"

/*: "toPosition" :*/
fileprivate let show_centerSPath:[Character] = ["t","o","P","o","s","i","t"]
fileprivate let show_nameStr:String = "isucceedn"

/*: "party:adminChangeMike" :*/
fileprivate let userFileText:[Character] = ["p","a","r","t","y",":","a","d","m","i","n","C","h","a","n","g","e","M","i","k"]
fileprivate let appPlayerText:String = "nor"

/*: "You were kicked off the seat" :*/
fileprivate let dreamSubContent:String = "You wclick name"
fileprivate let user_modelFormat:String = "tableictable"
fileprivate let userSucceedStr:String = " the make size case guard frame"
fileprivate let mBarIdent:[Character] = ["s","e","a","t"]

/*: "party:changeCoin" :*/
fileprivate let userVerticalValue:String = "paapplicationty"
fileprivate let mainCellViewVarIdent:String = "eCoinself read height"

/*: "party:closeStream" :*/
fileprivate let mToStr:[Character] = ["p","a","r","t","y",":","c","l","o","s","e"]
fileprivate let notiMakeName:String = "position managerStream"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SizeEventHandler.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let kTabContent = NSNotification.Name(rawValue: String(bytes: showTalkId.map{lastMedium(layer: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let k_licenseTitle = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum FromPropertyProtocol: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class SizeEventHandler: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = DetailModelType()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kTabContent, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: SizeEventHandler?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: ForceBackReactiveCompatible?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: ResultMeasurable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = imageInfo()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func frameShared() -> SizeEventHandler {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = SizeEventHandler()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func pushAdd() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if SizeEventHandler._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            SizeEventHandler._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: DatabaseMiniView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = DatabaseMiniView.subOf()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.par()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension SizeEventHandler {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func upCurrent(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        LineReqTool.canPhoto(roomId: partyModel.roomId,
                                         //: uid: Int(ModeStageReactiveCompatible.share.loginUid) ?? 0,
                                         uid: Int(ModeStageReactiveCompatible.share.loginUid) ?? 0,
                                         //: type: type,
                                         type: type,
                                         //: position: position,
                                         position: position,
                                         //: toPos: toPosition)
                                         toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.videoGet()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = ResultMeasurable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.birth(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.videoGet()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func re(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        LineReqTool.enterMikeSearchroomHalfMastTypeSharedChange(roomId: partyModel.roomId,
                                                                            //: uid: uid,
                                                                            uid: uid,
                                                                            //: type: type,
                                                                            type: type,
                                                                            //: position: position)
                                                                            position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func videoGet() -> String {
        //: return "p-\(ModeStageReactiveCompatible.share.loginUid)-\(partyModel.roomId)"
        return "p-\(ModeStageReactiveCompatible.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension SizeEventHandler {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func tabFor(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        PounderReactiveCompatible.withBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.actionKey(showMsg: kAskValue)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            LineReqTool.refresh(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: SucceedInfoReactiveCompatible.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            SucceedInfoReactiveCompatible.share.untilEqual(urlStr: result[(showUserIdent.replacingOccurrences(of: "top", with: "di") + String(app_shareValue.prefix(5)))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.actionKey(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isWork == false else {
                guard EffectSocketDelegate.shared.isWork == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.actionKey(showMsg: kNearbyData)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = DetailModelType.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    ReadReactiveCompatible.equalOn(msg: (String(main_stateTitle) + kQuantityimateUrl + "析失\u{8d25}。"))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.actionKey(showMsg: (String(mainTablePath) + String(notiInfoMsg)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = ForceBackReactiveCompatible()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.imageColor()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.dedication()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.groupFile()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                kTopTableValue.audienceTap(eventID: "\(k_statusName)_\(ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue ? String(bytes: dream_leadingName.map{nameColor(model: $0)}, encoding: .utf8)! : (String(notiObjectContent)))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                WorkAdThen.toCompletion(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        ReadReactiveCompatible.equalOn(msg: (k_frameMessage.replacingOccurrences(of: "block", with: "C") + "ROOM: " + appModelName + "失\u{8d25}\u{3002}gro" + String(dream_managerLabelFormat.prefix(6))) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.actionKey(showMsg: (String(mainTablePath) + String(notiInfoMsg)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.priceTagAdd()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func slipRequest(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        LineReqTool.after(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.lipogramPop()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == ModeStageReactiveCompatible.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == ModeStageReactiveCompatible.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = DetailModelType.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                ReadReactiveCompatible.equalOn(msg: (showIconFormat.replacingOccurrences(of: "burn", with: "R") + String(dreamBagMessage.prefix(6)) + String(main_infoKey)))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.actionKey(showMsg: (String(mainTablePath) + String(notiInfoMsg)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = ForceBackReactiveCompatible()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.imageColor()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.dedication()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.consumerLabel(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            kTopTableValue.audienceTap(eventID: "\(k_statusName)_\(ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue ? String(bytes: dream_leadingName.map{nameColor(model: $0)}, encoding: .utf8)! : (String(notiObjectContent)))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            WorkAdThen.toCompletion(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    ReadReactiveCompatible.equalOn(msg: (mainReData.replacingOccurrences(of: "kit", with: "V") + "ROOM" + dreamLogTransformIdent + "\u{5207}房失败" + String(userCellGiftUrl.prefix(6)) + "Id: ") + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.actionKey(showMsg: (String(mainTablePath) + String(notiInfoMsg)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.priceTagAdd()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension SizeEventHandler {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func draft() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.belowTo()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func par() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = startScreen() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: ModeViewDelegate.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func priceTagAdd() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.lipogramPop() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.upCurrent(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            SizeEventHandler.pushAdd() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension SizeEventHandler {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func groupFile() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = MacroReactiveCompatible.getVoiceRoomAppId()
        profile.appID = MacroReactiveCompatible.digitiserAdd()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: ModeStageReactiveCompatible.share.loginUid)
        let user = ZegoUser(userID: ModeStageReactiveCompatible.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func consumerLabel(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension SizeEventHandler: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            LineReqTool.fromApp(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.instanceChangeModel(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.examineed()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == FromPropertyProtocol.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.upCurrent(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                LineReqTool.fromApp(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.instanceChangeModel(key: key, model: value)
                        //: if String(value.uid) == ModeStageReactiveCompatible.share.loginUid {
                        if String(value.uid) == ModeStageReactiveCompatible.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.videoGet()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.examineed()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kTopTableValue.playerOpen(eventID: k_marginShowBeautyContent, parameterStr: [String(bytes: dream_pageIdent.map{makeSum(index: $0)}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(m_bottomId.replacingOccurrences(of: "view", with: "s") + "om_ki" + String(main_contentAttachTitle.prefix(6)) + main_minPath.replacingOccurrences(of: "info", with: "ss"))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (user_toName.replacingOccurrences(of: "time", with: "s") + "eRoo" + String(dream_rangeMessage)) || value == (String(dreamIndexRegularTitle.prefix(9)) + String(appSearchedResultUrl.suffix(5))) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    priceTagAdd()
                    //: let config = ShowAlertConfig()
                    let config = SpaceAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    AlertInfoThen.mug(message: String(bytes: noti_offPath.map{startNeed(show: $0)}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: k_videoTitle, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    actionKey(showMsg: String(bytes: show_layerTitle.map{columnShare(image: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                ReadReactiveCompatible.equalOn(msg: (String(show_firstKey) + String(app_withUrl.suffix(6))) + "\(roomID)" + (String(k_bagName.prefix(3)) + String(user_colorData.suffix(6))) + "\(errorCode)" + (String(show_sharedStr.prefix(3)) + String(dream_frameMsg.suffix(7))) + "\(reason)" + (String(show_livePath.suffix(4)) + "tendedDat" + String(noti_makeUserText.suffix(3))) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                actionKey(showMsg: (String(mainTablePath) + String(notiInfoMsg)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kTopTableValue.playerOpen(eventID: k_marginShowBeautyContent, parameterStr: [String(bytes: dream_pageIdent.map{makeSum(index: $0)}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            priceTagAdd()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            ReadReactiveCompatible.equalOn(msg: (kNorMainName.replacingOccurrences(of: "input", with: "C") + "EROO" + noti_toId + "用户推流" + m_styleName + "oomID: ") + "\(partyModel.roomId)" + (String(show_itemStr.suffix(5)) + String(mainMakeValue.suffix(4)) + "de: ") + "\(errorCode)" + (String(noti_textVoiceData) + String(appTopKey)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            actionKey(showMsg: String(bytes: show_viewId.reversed(), encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = gildTheLily()
            //: for (position, model) in dict where String(model.uid) == ModeStageReactiveCompatible.share.loginUid {
            for (position, model) in dict where String(model.uid) == ModeStageReactiveCompatible.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                upCurrent(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                faculty(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                priceTagAdd()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            ReadReactiveCompatible.equalOn(msg: (String(show_dataValue) + String(dreamColorText)) + "\(partyModel.roomId)" + (String(show_itemStr.suffix(5)) + String(mainMakeValue.suffix(4)) + "de: ") + "\(errorCode)" + (String(noti_textVoiceData) + String(appTopKey)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                priceTagAdd()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.addLevel(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = gildTheLily()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.addLevel(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            ReadReactiveCompatible.equalOn(msg: (appSexId.replacingOccurrences(of: "off", with: "VO") + "OOM: 信" + mainBalanceFirstId + "败，co" + String(show_shareValue.prefix(7))) + "\(command)" + (String(appSaveResultMessage.prefix(5)) + String(m_recordName.suffix(5))) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(showMakeData.replacingOccurrences(of: "info", with: "t"))][(m_contentTitle.replacingOccurrences(of: "message", with: "o") + main_defineImageId.replacingOccurrences(of: "add", with: "n"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < k_licenseTitle else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = ResultMeasurable.deserialize(from: json[(showMakeData.replacingOccurrences(of: "info", with: "t"))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(mReportMessage) + kActionUpId.replacingOccurrences(of: "gift", with: "-")) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(user_layerPath.prefix(6)))].stringValue == (String(appBeyondId) + String(dream_logId)) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(showMakeData.replacingOccurrences(of: "info", with: "t"))][(dreamScaleStr.replacingOccurrences(of: "file", with: "p"))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                birth(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = behindDelay(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    faculty(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                faculty(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(showMakeData.replacingOccurrences(of: "info", with: "t"))][(String(show_centerSPath) + show_nameStr.replacingOccurrences(of: "succeed", with: "o"))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                birth(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                birth(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.refuge()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(user_layerPath.prefix(6)))].stringValue == (String(userFileText) + appPlayerText.replacingOccurrences(of: "nor", with: "e")) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(showMakeData.replacingOccurrences(of: "info", with: "t"))][(dreamScaleStr.replacingOccurrences(of: "file", with: "p"))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                birth(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = behindDelay(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    faculty(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.refuge()
                    //: guard String(oldModel.uid) == ModeStageReactiveCompatible.share.loginUid else { return }
                    guard String(oldModel.uid) == ModeStageReactiveCompatible.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    actionKey(showMsg: (String(dreamSubContent.prefix(5)) + "ere " + user_modelFormat.replacingOccurrences(of: "table", with: "k") + "ed off" + String(userSucceedStr.prefix(5)) + String(mBarIdent)).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                birth(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == ModeStageReactiveCompatible.share.loginUid else { return }
                guard String(mikeModel.uid) == ModeStageReactiveCompatible.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                birth(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == ModeStageReactiveCompatible.share.loginUid else { return }
                guard String(mikeModel.uid) == ModeStageReactiveCompatible.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(user_layerPath.prefix(6)))].stringValue == (userVerticalValue.replacingOccurrences(of: "application", with: "r") + ":chang" + String(mainCellViewVarIdent.prefix(5))) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            SizeEventHandler.frameShared().gildTheLily().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    birth(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(user_layerPath.prefix(6)))].stringValue == (String(mToStr) + String(notiMakeName.suffix(6))) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension SizeEventHandler {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func birth(position: Int, model: ResultMeasurable) {
        //: modifyMikePosition(key: position, model: model)
        instanceChangeModel(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.ofTable(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        evoke(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func faculty(position: Int, model: ResultMeasurable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        instanceChangeModel(key: position, model: ResultMeasurable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.ofTable(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        evoke(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func evoke(uid: Int) {
        //: guard String(uid) == ModeStageReactiveCompatible.share.loginUid else { return }
        guard String(uid) == ModeStageReactiveCompatible.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = gildTheLily()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.trademarkStatus()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension SizeEventHandler {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func imageInfo() -> [Int: ResultMeasurable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< k_licenseTitle {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            instanceChangeModel(key: index, model: ResultMeasurable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func instanceChangeModel(key: Int, model: ResultMeasurable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func behindDelay(key: Int) -> ResultMeasurable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func gildTheLily() -> [Int: ResultMeasurable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
