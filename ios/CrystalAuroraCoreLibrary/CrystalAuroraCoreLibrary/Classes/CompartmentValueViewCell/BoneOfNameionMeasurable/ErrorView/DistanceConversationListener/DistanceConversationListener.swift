
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let kDelayKey:[Character] = ["I","M","退","\u{51fa}","登","录","失"]
fileprivate let main_homeName:String = "败，"
fileprivate let mWithIdent:String = "code:in number complete layer self"

/*: , desc: :*/
fileprivate let dreamTableMoreViewTitle:String = "to load name, desc:"

/*: ." :*/
fileprivate let user_titleVideoIdent:[Character] = ["."]

/*: "IM登录失败，code: :*/
fileprivate let k_detailStr:[Character] = ["I","M","登","录","失","败","\u{ff0c}","c","o","d"]
fileprivate let showDataText:[Character] = ["e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let show_hiddenUrl:String = "IM获取未type of return item"
fileprivate let m_modelText:[Character] = ["\u{8bfb}","消","息","\u{603b}","数","失","败","\u{ff0c}","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let app_entryCountimateMessage:String = "CRUS"

/*: "badNumber" :*/
fileprivate let kLiveClickNameStr:[UInt8] = [0x16,0x15,0x18,0x2,0x29,0x21,0x16,0x19,0x26]

fileprivate func modelSelf(top num: UInt8) -> UInt8 {
    let value = Int(num) + 76
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let m_titleEdgeName:String = "pselectedaza"

/*: "roomNum" :*/
fileprivate let m_sizeWillValue:String = "to as giftroomN"
fileprivate let noti_colorTitle:[Character] = ["u","m"]

/*: "num" :*/
fileprivate let notiSharedTitle:String = "nutop"

/*: "onlyOneRoomId" :*/
fileprivate let userAddMessage:String = "onlyOntop data"
fileprivate let k_playerText:[Character] = ["e"]
fileprivate let m_imageData:[Character] = ["R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let kActualIdent:String = "return to cellLIVE-"

/*: "PARTY-CHAT" :*/
fileprivate let mChangeData:String = "PsceneRT"

/*: "extra" :*/
fileprivate let showViewName:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let k_valuePath:String = "name viewmsgType"

/*: "newPkgGift" :*/
fileprivate let userDeadlineName:String = "newPktext if guard bottom let"

/*: "MF:ChatMentionMsg" :*/
fileprivate let noti_giftData:String = "MF:ChatMeinfo transform then view image"
fileprivate let dreamDetailText:[Character] = ["n","t","i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let user_deviceStr:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let noti_makeMsg:[UInt8] = [0x17,0x16,0x14,0x11,0x16,0x1d,0x2b,0xc,0x19,0xc,0xd,0xb]

/*: "isNewUser" :*/
fileprivate let main_sizePath:[UInt8] = [0x7b,0x61,0x5c,0x77,0x65,0x47,0x61,0x77,0x60]

/*: "c2c_ :*/
fileprivate let dreamVideoIdent:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let kNumberName:String = "case value pic read borderIM获取"
fileprivate let notiShadowButtonText:String = "image mini viewnvId:"

/*: , code: :*/
fileprivate let user_frameData:String = "else extension self, code:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let notiValueLeadingViewStr:String = "make view label bottomIM插入"
fileprivate let show_iconStr:String = "argetIdpresentation half data view"
fileprivate let show_lessPlusMessage:String = ":"

/*: "changeDirection" :*/
fileprivate let dreamMakeTouchNewFormat:String = "bottoman"
fileprivate let mainInstanceFormat:String = "version"
fileprivate let show_withName:String = "efromion"

/*: "rYMsgType" :*/
fileprivate let show_propertyUserFormat:String = "return engine info falserYMsg"
fileprivate let userScreenNameEqualKey:String = "Typecount view equal observe fatal"

/*: "GJ:CallCustom" :*/
fileprivate let appIntervalimateKey:String = "g"
fileprivate let appReloadTitle:String = "J:Calprice share cover manager"

/*: "IM获取会话列表失败，code: :*/
fileprivate let user_dataTitle:String = "view else to make maskIM获取会话"
fileprivate let main_fullStr:String = "code:type time path"

/*: "msgInfo" :*/
fileprivate let main_viewMsg:String = "msgInfkey value self name"
fileprivate let k_clearStr:[Character] = ["o"]

/*: "needFold" :*/
fileprivate let m_lipMessage:[Character] = ["n","e","e","d","F"]
fileprivate let k_buttonPath:[Character] = ["o","l","d"]

/*: "group_ :*/
fileprivate let appShowFormat:String = "grouattention"
fileprivate let userValueKey:String = "_"

/*: "IM删除会话失败，convID: :*/
fileprivate let dream_textImageId:[Character] = ["I","M","删","\u{9664}","会","话","失","败","，","c","o","n","v"]
fileprivate let main_errFormat:[Character] = ["I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let k_centerUrl:[Character] = ["I","M","置","顶","会","话","失","败","\u{ff0c}","c","o","n","v"]
fileprivate let kLimitWiseTrueData:[Character] = ["I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let main_sendValue:[Character] = ["S","D","K"," ","正","在","连","接","到","\u{670d}","\u{52a1}"]
fileprivate let m_withKey:String = "器"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let kEqualRawSharedData:String = "false withSDK 已经成"
fileprivate let kDiscNameIdent:String = "功连接到服务器"

/*: "IM连接服务器失败，code: :*/
fileprivate let mPhotoSucceedToStr:[Character] = ["I","M","连","接","服","务","器","失","败","\u{ff0c}","c","o","d","e",":"]

/*: , error: :*/
fileprivate let appVoicePushIdent:String = ", errordetail info"
fileprivate let userLayerTargetValue:[Character] = [":"]

/*: "IM账号被挤下线。" :*/
fileprivate let showPathValue:String = "IM账\u{53f7}被挤"
fileprivate let noti_lineId:String = "下\u{7ebf}。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let userSizeSignKey:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let main_visibleId:[Character] = ["O","f","f","l"]
fileprivate let k_useMsg:[Character] = ["i","n","e"," ","N","o","t","i","f","i","c","a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let dream_infoToStr:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let m_colorClearFormat:String = "source normal socialRe "
fileprivate let appGroupFormat:String = "Loginselected self model use"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DistanceConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ModelObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func receive(data: [ReadConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func basic(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func topAt(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum SourceDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class DistanceConversationListener: NSObject {
@objcMembers public class DistanceConversationListener: NSObject {
    //: public static let shared = DistanceConversationListener()
    public static let shared = DistanceConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [ReadConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [ReadConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [ReadConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [ReadConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [ReadConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: ReadConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.mePull()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func byTing() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(kErrContent, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func mePull() {
        //: func__removeAllDelegate()
        camera()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.shirtButton()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func shirtButton() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = ReadConversationModel.warPartyModel(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = ReadConversationModel.warPartyModel(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func effectComponent() -> [ReadConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue {
        if ModeStageReactiveCompatible.share.appStatus == OfSum.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == MacroReactiveCompatible.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == MacroReactiveCompatible.picLine() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension DistanceConversationListener {
extension DistanceConversationListener {
    //: func func__removeAllDelegate() {
    func camera() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func destroyMake(_ delegate: ModelObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func throwAway(_ delegate: ModelObjectProtocol) {
        //: self.func__invokeDelegate {
        self.sizeRange {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func sizeRange(_ invocation: (ModelObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ModelObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension DistanceConversationListener {
extension DistanceConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func addOut() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
        }
        //: self.initialization()
        self.mePull()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func inward(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = ModeStageReactiveCompatible.share.appUserConfigMode.uSig
        let uSigStr = ModeStageReactiveCompatible.share.appUserConfigMode.uSig
        //: let uidStr = ModeStageReactiveCompatible.share.loginUserMode.userID
        let uidStr = ModeStageReactiveCompatible.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.methodReceive()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            ReadReactiveCompatible.equalOn(msg: (String(k_detailStr) + String(showDataText)) + "\(code)" + (String(dreamTableMoreViewTitle.suffix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func selectWith() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || ModeStageReactiveCompatible.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || ModeStageReactiveCompatible.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.actionKey(showMsg: kTimeLineValue)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension DistanceConversationListener {
extension DistanceConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func methodReceive() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            ReadReactiveCompatible.equalOn(msg: (String(show_hiddenUrl.prefix(5)) + String(m_modelText)) + "\(code)" + (String(dreamTableMoreViewTitle.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func recordMsg() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: PushModelReactiveCompatible.req_sysMessageList { _, result in
        PushModelReactiveCompatible.clickInfo { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(app_entryCountimateMessage.lowercased() + "hBadge")] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: k_conversationName, object: self, userInfo: [String(bytes: kLiveClickNameStr.map{modelSelf(top: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func countAggregation(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: PushModelReactiveCompatible.req_getChatRoomInfo { succeed, result, _ in
        PushModelReactiveCompatible.cardAvailable { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(m_titleEdgeName.replacingOccurrences(of: "selected", with: "l"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(m_sizeWillValue.suffix(5)) + String(noti_colorTitle))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = ReadConversationModel.warPartyModel(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(notiSharedTitle.replacingOccurrences(of: "top", with: "m"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(userAddMessage.prefix(6)) + String(k_playerText) + String(m_imageData))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension DistanceConversationListener: V2TIMAdvancedMsgListener {
extension DistanceConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(kActualIdent.suffix(5)) + "CHAT")) || msg.groupID.contains((mChangeData.replacingOccurrences(of: "scene", with: "A") + "Y-CHAT"))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            WorkAdThen.eldest().accountingData(msg: msg)
            //: return
            return
        }
        //: if msg.sender == ModeStageReactiveCompatible.share.loginUserMode.userID { return }
        if msg.sender == ModeStageReactiveCompatible.share.loginUserMode.userID { return }

        //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: msg.customElem.data)
        let dic = DistrictManagerOfReactiveCompatible.endTarget(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(showViewName))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? FromModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: k_eventName, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(k_valuePath.suffix(7)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(userDeadlineName.prefix(5)) + "gGift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kPathData, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if DistrictManagerOfReactiveCompatible.checkVoiceMessage(message: msg) {
            if DistrictManagerOfReactiveCompatible.inkpad(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = SizeChatManager.packShould(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                EvenThen.popHave(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.showBar(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            TextItemAnimatTool.shared.enableMake(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            TextItemAnimatTool.shared.dataBy(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(noti_giftData.prefix(9)) + String(dreamDetailText)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            EqualEffectReactiveCompatible.share.humanActivity(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: ErrorReactiveCompatible.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: ErrorReactiveCompatible.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != MacroReactiveCompatible.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != MacroReactiveCompatible.preemptSystemXiao() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.sizeRange { $0.topAt?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            TrademarkThen.shared.atLine(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: user_deviceStr.reversed(), encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: noti_makeMsg.map{$0^120}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: main_sizePath.map{$0^18}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: kTitleText,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            CuttingEdgeThen.addFillRunning(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            ErrorReactiveCompatible.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: k_keyData, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: PhoneMessageFromReactiveCompatible, isPrivete: Bool) {
    @objc public func effectRankAction(targetId: String, msgCellData: PhoneMessageFromReactiveCompatible, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.pointOfReferencePrivete(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(dreamVideoIdent)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.heliogram(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.constraintMake(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func pointOfReferencePrivete(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = ModeStageReactiveCompatible.share.loginUserMode.userID
        let senderStr = ModeStageReactiveCompatible.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func showBar(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(dreamMakeTouchNewFormat.replacingOccurrences(of: "bottom", with: "ch") + "geDi" + mainInstanceFormat.replacingOccurrences(of: "version", with: "r") + show_withName.replacingOccurrences(of: "from", with: "ct"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && SucceedInfoReactiveCompatible.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(show_propertyUserFormat.suffix(5)) + String(userScreenNameEqualKey.prefix(4)))].stringValue == (appIntervalimateKey.uppercased() + String(appReloadTitle.prefix(5)) + "lCustom") && isSelf && SucceedInfoReactiveCompatible.share.headUser()!.isKind(of: ModeViewDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kRequestValue) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kRequestValue)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension DistanceConversationListener: V2TIMConversationListener {
extension DistanceConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.heliogram(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.constraintMake(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.heliogram(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.constraintMake(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: ModeStageReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        ModeStageReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.sizeRange {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.basic?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func constraintMake(conVersationList: [ReadConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if CuttingEdgeThen.nearList().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                CuttingEdgeThen.nearList().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.sizeRange {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.receive?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func logCompletion(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [ReadConversationModel] = []

        // V2TIMConversation->ReadConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = ReadConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.mTheory(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [ReadConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = LabelFrameInfoTable.modeGet(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.halogen(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension DistanceConversationListener {
extension DistanceConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func tabTo(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.logCompletion(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.constraintMake(conVersationList: arr as? [ReadConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            ReadReactiveCompatible.equalOn(msg: (String(user_dataTitle.suffix(6)) + "列表\u{5931}\u{8d25}\u{ff0c}" + String(main_fullStr.prefix(5))) + "\(code)" + (String(dreamTableMoreViewTitle.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func addGetCacheModelRapTarget(targetId: String) -> ReadConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func heliogram(convList: [V2TIMConversation]) -> [ReadConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [ReadConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = ReadConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.mTheory(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.halogen(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func halogen(convModel: ReadConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.shirtButton()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: SourceDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - ModeStageReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - ModeStageReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = CuttingEdgeThen.sizeEnter(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: elemData)
            let dict = DistrictManagerOfReactiveCompatible.endTarget(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(showViewName))][(String(main_viewMsg.prefix(6)) + String(k_clearStr))][(String(m_lipMessage) + String(k_buttonPath))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.sort(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.sort(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func sort(convList: [ReadConversationModel], dataType: SourceDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func chapter(targetId: String, dataType: SourceDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(dreamVideoIdent)) + "\(targetId)" : (appShowFormat.replacingOccurrences(of: "attention", with: "p") + userValueKey.capitalized) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            CuttingEdgeThen.recognise(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(ModeStageReactiveCompatible.share.loginUserMode.userID, toUid: targetId)
            EvenThen.companyName(ModeStageReactiveCompatible.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.methodReceive()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            ReadReactiveCompatible.equalOn(msg: (String(dream_textImageId) + String(main_errFormat)) + "\(convID)" + (String(user_frameData.suffix(7))) + "\(code)" + (String(dreamTableMoreViewTitle.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func eventTop(convModel: ReadConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            ReadReactiveCompatible.equalOn(msg: (String(k_centerUrl) + String(kLimitWiseTrueData)) + "\(convModel.conversationID)" + (String(user_frameData.suffix(7))) + "\(code)" + (String(dreamTableMoreViewTitle.suffix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.sort(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.sort(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension DistanceConversationListener: V2TIMSDKListener {
extension DistanceConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(main_sendValue) + m_withKey.capitalized))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(kEqualRawSharedData.suffix(7)) + kDiscNameIdent.capitalized))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        ReadReactiveCompatible.equalOn(msg: (String(mPhotoSucceedToStr)) + "\(code)" + (String(appVoicePushIdent.prefix(7)) + String(userLayerTargetValue)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        ReadReactiveCompatible.equalOn(msg: (showPathValue + noti_lineId))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        EffectSocketDelegate.shared.falseNameOut(userLoginOut: true)
        //: DistanceConversationListener.shared.isConnection = false
        DistanceConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (AtInfoApplication.shared as! AtInfoApplication).editorDestroy()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: userSizeSignKey.reversed(), encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = SpaceAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if NorAddrTool.share.interfaceLang == LipogramLangType.ar.rawValue || NorAddrTool.share.interfaceLang == LipogramLangType.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            NorAddrTool.share.interfaceLang == LipogramLangType.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.dismissProgress(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        AlertInfoThen.nameConfig()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        AlertInfoThen.nameConfig(title: (String(main_visibleId) + String(k_useMsg)).localized, message: msg, leftBtnTitle: (String(dream_infoToStr)).localized, rightBtnTitle: (String(m_colorClearFormat.suffix(3)) + String(appGroupFormat.prefix(5))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LengthLiveManager.loadShared().failureResource()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            SizeEventHandler.frameShared().priceTagAdd()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            LibraryThen.atStateCell { t in
                //: ProgressHUD.dismiss()
                AnagrammatizeView.implant()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: k_dismissData, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            LibraryThen.request { t in
                //: if t {
                if t {
                    //: DistanceConversationListener.shared.func__LogingIn { _ in
                    DistanceConversationListener.shared.inward { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if LengthLiveManager.loadShared().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(LengthLiveManager.loadShared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    EffectSocketDelegate.shared.itemAfter()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
