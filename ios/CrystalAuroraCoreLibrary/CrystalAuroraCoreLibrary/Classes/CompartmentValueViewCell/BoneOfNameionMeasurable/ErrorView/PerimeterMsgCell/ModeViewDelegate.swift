
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userTipUrl:[UInt8] = [0x7f,0x84,0x7f,0x8a,0x3e,0x79,0x85,0x7a,0x7b,0x88,0x50,0x3f,0x36,0x7e,0x77,0x89,0x36,0x84,0x85,0x8a,0x36,0x78,0x7b,0x7b,0x84,0x36,0x7f,0x83,0x86,0x82,0x7b,0x83,0x7b,0x84,0x8a,0x7b,0x7a]

fileprivate func strikeOut(aspect num: UInt8) -> UInt8 {
    let value = Int(num) - 22
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "F5F5F5" :*/
fileprivate let main_endFormat:String = "FiconFiconF"
fileprivate let m_appearKey:String = "5"

/*: "#128CFF" :*/
fileprivate let showDataIdent:String = "#"
fileprivate let dream_cellStr:[Character] = ["1","2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let noti_actionFormat:String = "class page video model videoFeedbac"
fileprivate let userListMsg:[Character] = ["k"]

/*: "#8370FF" :*/
fileprivate let kHiddenKey:[Character] = ["#","8","3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let m_kitTargetViewFormat:[Character] = ["Q","u","i","c","k"," ","G","r","e"]
fileprivate let showPhotoSizeContent:[Character] = ["e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let user_objectMsg:[Character] = ["B","e","a","u","t","y"," ","P","h","o","t","o","s"]

/*: "url" :*/
fileprivate let app_playerStyleUrl:[Character] = ["u","r","l"]

/*: "unlockGift" :*/
fileprivate let user_rowStr:String = "unwillo"

/*: "%d" :*/
fileprivate let noti_cornerTitle:String = "name section%d"

/*: "isBurn" :*/
fileprivate let main_viewMessage:[UInt8] = [0xe9,0xf3,0xc2,0xf5,0xf2,0xee]

fileprivate func dogBiscuit(lab num: UInt8) -> UInt8 {
    let value = Int(num) - 128
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let noti_goldId:String = "giftIdyes name var name video"

/*: "rYMsgType" :*/
fileprivate let app_sendKey:String = "rYMsinterval title"
fileprivate let main_freshStr:[Character] = ["e"]

/*: "GJ:CallCustom" :*/
fileprivate let dreamFrameTitle:String = "gift view self female guardGJ:Call"
fileprivate let showBottomEqualFormat:String = "deadline input import forCustom"

/*: "changeDirection" :*/
fileprivate let appUpPath:String = "chaindex"
fileprivate let user_liveLabelFormat:String = "profileion"

/*: "extra" :*/
fileprivate let mainColorMsg:String = "extrkit"

/*: "msgInfo" :*/
fileprivate let m_pathKey:String = "msgInflet more object of"
fileprivate let kMarginData:String = "game"

/*: "toUid" :*/
fileprivate let userDatabaseButtonMsg:String = "toUidwrite recommend else self left"

/*: "wantGift" :*/
fileprivate let noti_iconIdent:String = "hidden skinwantGift"

/*: "jumpKey" :*/
fileprivate let mBlockText:String = "time index makejumpKe"
fileprivate let showNameStr:[Character] = ["y"]

/*: "truePersonAuth" :*/
fileprivate let user_sizeData:String = "suiterue"
fileprivate let user_actionColorText:String = "text size videoonAuth"

/*: "logType" :*/
fileprivate let k_customValue:String = "logTypeicon return"

/*: "msgType" :*/
fileprivate let noti_turnMessage:[UInt8] = [0xc7,0xd9,0xcd,0xfe,0xd3,0xda,0xcf]

/*: "txt" :*/
fileprivate let appLabelPath:String = "TXT"

/*: "type" :*/
fileprivate let showStrengthStr:String = "typmake"

/*: "QuickMsg" :*/
fileprivate let userReportName:String = "in type color selfQuickMs"
fileprivate let dream_oldFormat:[Character] = ["g"]

/*: "params" :*/
fileprivate let main_giftText:[UInt8] = [0x1,0x10,0x3,0x10,0x1c,0x2]

private func managerHundred(frame num: UInt8) -> UInt8 {
    return num ^ 113
}

/*: "content" :*/
fileprivate let showAcceptIdent:[UInt8] = [0xa4,0xa8,0xa9,0xb3,0xa2,0xa9,0xb3]

private func imaginationImage(manager num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "id" :*/
fileprivate let main_infoEqualIdent:String = "isize"

/*: "txtId" :*/
fileprivate let user_topId:String = "make else lock message elsetxtId"

/*: "message" :*/
fileprivate let dream_colorIdent:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let user_valueIdent:String = "name table true mainTxtMs"
fileprivate let noti_sharedTitle:String = "model"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let k_centerMsg:[UInt8] = [0xc8,0xf1,0xf7,0xfd,0xfb,0xbe,0xf6,0xfb,0xf2,0xf2,0xf1,0xbe,0xfa,0xf1,0xe9,0xf0,0xf2,0xf1,0xff,0xfa,0xbe,0xf8,0xff,0xf7,0xf2,0xfb,0xfa,0xb0,0xbe,0xce,0xec,0xf1,0xfd,0xfb,0xfa,0xeb,0xec,0xfb]

private func pathPlayer(video num: UInt8) -> UInt8 {
    return num ^ 158
}

/*: "length" :*/
fileprivate let m_subPath:String = "screen"
fileprivate let main_modelFormat:String = "engperson"

/*: "audio" :*/
fileprivate let notiMessagePopKey:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let dreamKitIdent:[Character] = ["a"]
fileprivate let dreamCenterIdent:String = "udioIdself path lab kit"

/*: "audioLength" :*/
fileprivate let user_backMsg:[UInt8] = [0x68,0x74,0x67,0x6e,0x65,0x4c,0x6f,0x69,0x64,0x75,0x61]

/*: "audioSandbox" :*/
fileprivate let showMaleControlPath:String = "aneed"
fileprivate let k_groupPath:String = "user actualioSa"

/*: "senderId" :*/
fileprivate let m_nowTitle:[UInt8] = [0x5e,0x48,0x43,0x49,0x48,0x5f,0x64,0x49]

private func analogDigitalConverter(max num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "AudioMsg" :*/
fileprivate let noti_valueMsg:String = "top being start in elseAudioMsg"

/*: "Can't send blank message" :*/
fileprivate let dreamButtonName:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"]
fileprivate let noti_requestMessage:[Character] = [" ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let mainMoveFormat:String = "view"
fileprivate let dream_actionMessage:[Character] = ["u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let notiFileHiddenName:String = "titleot"
fileprivate let mainWhiteId:[Character] = ["f","o"]

/*: "contentType" :*/
fileprivate let dream_makeUrl:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let kTitlePath:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let m_collectionValue:String = "ichooseg"

/*: "imageUri" :*/
fileprivate let app_finishPath:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let mCommentPath:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let appIconFormat:String = "atc"

/*: "VideoMsg" :*/
fileprivate let main_timeFormat:[Character] = ["V","i","d","e","o","M","s"]
fileprivate let userCropMessage:String = "equal"

/*: "video" :*/
fileprivate let userPathId:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let kErrorMessage:[UInt8] = [0x12,0x1e,0x7,0x14,0x3,0x38,0x1c,0x16]

private func currentTo(name num: UInt8) -> UInt8 {
    return num ^ 113
}

/*: "videoUrl" :*/
fileprivate let noti_flexibleKey:[UInt8] = [0x56,0x49,0x44,0x45,0x4f,0x35,0x52,0x4c]

fileprivate func reloadArea(of num: UInt8) -> UInt8 {
    let value = Int(num) + 32
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "duration" :*/
fileprivate let user_viewFormat:[UInt8] = [0x2c,0x3d,0x3a,0x29,0x3c,0x31,0x37,0x36]

fileprivate func indicatorLayer(value num: UInt8) -> UInt8 {
    let value = Int(num) - 200
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let notiMakeContent:[UInt8] = [0x6e,0x65,0x6c]

/*: "Documents/User/voice/" :*/
fileprivate let user_normalUrl:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let showCellStatusName:String = "Documsend count"
fileprivate let mainDatabaseMessage:String = "equal let self after/User"
fileprivate let user_momentImageMsg:String = "d/add view"

/*: "msgIncome" :*/
fileprivate let noti_listData:[Character] = ["m","s","g","I","n","c","o"]
fileprivate let kOpMaleStr:String = "mdata"

/*: "mfBean" :*/
fileprivate let k_errorFirstMessage:[UInt8] = [0xa,0x3,0xdf,0x2,0xfe,0xb]

fileprivate func labelMake(share num: UInt8) -> UInt8 {
    let value = Int(num) - 157
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let m_resultQuickManagerValue:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "WantGift" :*/
fileprivate let m_insideMessage:String = "model height view toWant"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let notiSelectedRowMData:String = "_Privview else height let add"
fileprivate let notiCenterFailName:[Character] = ["i","p","s","T","i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let showMinElementStr:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let show_scaleFormat:String = "voiceovoice"
fileprivate let userKeyValue:String = "name content viewalMf"

/*: "attachSysMsg" :*/
fileprivate let showShareSucceedKey:[Character] = ["a","t","t","a","c"]
fileprivate let notiTouchContent:String = "hSysMsgname gesture file"

/*: "intimatePhoto" :*/
fileprivate let mainQuitValue:String = "INTI"
fileprivate let dreamObjectInfoData:String = "scale modelPhot"
fileprivate let userTowardName:[Character] = ["o"]

/*: _ :*/
fileprivate let showMakeId:[Character] = ["_"]

/*: "totalIntimate" :*/
fileprivate let noti_stopMessage:String = "list careertotalInt"
fileprivate let mainVideoValue:[Character] = ["i","m","a","t","e"]

/*: "uid" :*/
fileprivate let notiInputFormat:[UInt8] = [0xb1,0xad,0xa0]

private func startLower(frame num: UInt8) -> UInt8 {
    return num ^ 196
}

/*: "intimate" :*/
fileprivate let mainSourceStr:[UInt8] = [0xa5,0xa2,0xb8,0xa5,0xa1,0xad,0xb8,0xa9]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let dreamIndexManagerPerformValue:[UInt8] = [0x0,0x22,0x2d,0x64,0x37,0x63,0x36,0x30,0x26,0x63,0x37,0x2b,0x26,0x63,0x25,0x36,0x2d,0x20,0x37,0x2a,0x2c,0x2d,0x63,0x27,0x36,0x26,0x63,0x37,0x2c,0x63,0x3a,0x2c,0x36,0x31,0x63,0x2f,0x22,0x20,0x28,0x63,0x2c,0x25,0x63,0x2a,0x2d,0x37,0x2a,0x2e,0x22,0x20,0x3a,0x63,0x2f,0x26,0x35,0x26,0x2f,0x6d]

private func meanSun(dialog num: UInt8) -> UInt8 {
    return num ^ 67
}

/*: "giftNum" :*/
fileprivate let show_eyeKey:String = "case valuegiftNum"

/*: "pkgItemsetId" :*/
fileprivate let mColorFormat:String = "pkgItemode self back size mode"
fileprivate let show_makeId:[Character] = ["m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let main_labelPath:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let kSharePath:[Character] = ["w","a","n","t","G","i","f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let k_belowActionName:String = "var kit label view heightWantG"
fileprivate let notiMakePath:String = "let should lab backgroundiftMsg"

/*: "gift" :*/
fileprivate let main_shareStr:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let user_labelMakeIdent:[Character] = ["V","i","e","w"," ","i","n","f"]
fileprivate let userEqualData:[Character] = ["o"]

/*: "Notes" :*/
fileprivate let m_whiteVoiceMsg:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let appToKey:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let dreamTextId:String = "model data type selfView "
fileprivate let app_leadershipFormat:String = "ceipts?model true"

/*: "He" :*/
fileprivate let show_sectionTitle:String = "Hesuper us"

/*: "She" :*/
fileprivate let showEqualViewFormat:String = "Shenormal message text date"

/*: "%@ is using too low a version" :*/
fileprivate let dreamBottomPath:[Character] = ["%","@"," ","i","s"," ","u","s","i","n","g"," ","t","o","o"," ","l","o","w"," ","a"]
fileprivate let kChangeId:String = "let index top version"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let mKeyMsg:String = "give quick message true showShoo"
fileprivate let kLineUrl:String = "value super component count framedeo a"
fileprivate let notiEqualKey:String = "st 5 equal index voice view"
fileprivate let main_fishTitle:[Character] = ["s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let m_infoPointMessage:String = "Uploadas for app now"
fileprivate let userImageBackgroundId:String = " failedyour title to array var"

/*: "播放状态: :*/
fileprivate let show_viewVideoId:String = "播放状态:"

/*: "c2c_" :*/
fileprivate let user_nameDefineMsg:[Character] = ["c","2","c","_"]

/*: "msg" :*/
fileprivate let app_statusName:String = "msnumber"

/*: "tips" :*/
fileprivate let main_labelNameFormat:String = "tiprelease"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let show_beanMsg:[UInt8] = [0x98,0xae,0xb4,0xe1,0xa2,0xa0,0xaf,0xe1,0xb4,0xb2,0xa4,0xe1,0xb5,0xa9,0xa4,0xe1,0xb7,0xa8,0xa5,0xa4,0xae,0xe1,0xa2,0xa9,0xa0,0xb5,0xe1,0xa7,0xa4,0xa0,0xb5,0xb4,0xb3,0xa4,0xe1,0xb5,0xae,0xe1,0xa6,0xa8,0xb7,0xa4,0xe1,0xe4,0x81,0xe1,0xa0,0xe1,0xa2,0xa0,0xad,0xad]

private func manageressImage(m num: UInt8) -> UInt8 {
    return num ^ 193
}

/*: "cellData" :*/
fileprivate let dream_styleId:String = "cellDatavar center"

/*: "System notification" :*/
fileprivate let showAddKey:String = "Systbar self var"
fileprivate let showFieldMsg:String = "ficollectiona"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ModeViewDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum InfiniteFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ModeViewDelegate: StateViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = ViewHandyJSON()
    private var userWrap = ViewHandyJSON() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = InfiniteFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ViewHandyJSON = ViewHandyJSON()) {
    init(chatID: String, isFrom: InfiniteFromEnum = .Normal, userWrap: ViewHandyJSON = ViewHandyJSON()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ModeViewDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        ErrorReactiveCompatible.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == MacroReactiveCompatible.getXiaoMiID() {
        if targetId == MacroReactiveCompatible.preemptSystemXiao() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == MacroReactiveCompatible.getCustomerServiceID() {
        } else if targetId == MacroReactiveCompatible.picLine() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userTipUrl.map{strikeOut(aspect: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
            if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = PercipientThen(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        center()
        //: createUI()
        voiceUp()
        //: createUIConstraints()
        outTitle()
        //: bindInteraction()
        holdFastEnable()
        //: addTapGestureRecognizer()
        dataImage()
        //: func__requestChatInfo()
        attribute()
        //: func__getFillDraftText()
        draftName()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        DetailPlayManager.shared.corner()
        //: func__addVoiceNotification()
        stint()
        //: addReadPeceiptNotification()
        everyObject()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            issue()
            //: func__TipsNotification()
            iconNotification()
            //: if ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue {
                //: func__setReplyTipsView()
                tips()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        belowPicture(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        belowPicture(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            enable()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: HeliogramTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = HeliogramTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (main_endFormat.replacingOccurrences(of: "icon", with: "5") + m_appearKey.capitalized))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(RowEffectMsgCell.self, forCellReuseIdentifier: RowEffectMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PathReactiveCompatible.self, forCellReuseIdentifier: PathReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(PetitionMsgCell.self, forCellReuseIdentifier: PetitionMsgCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(ModelValueViewCell.self, forCellReuseIdentifier: ModelValueViewCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(ValueSizeViewCell.self, forCellReuseIdentifier: ValueSizeViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(TerritorialDivisionReactiveCompatible.self, forCellReuseIdentifier: TerritorialDivisionReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(PerimeterMsgCell.self, forCellReuseIdentifier: PerimeterMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(SizeMsgCell.self, forCellReuseIdentifier: SizeMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(ErrorView.self, forCellReuseIdentifier: ErrorView.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: TitleViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(ViewerView.self, forCellReuseIdentifier: TitleViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: CountReactiveCompatible = {
        //: let m = TalkingCallMenuManager()
        let m = CountReactiveCompatible()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: ChemicalElementView = {
        //: var type: InputViewType = .PrivateChat
        var type: SignalViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = ChemicalElementView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (showDataIdent.capitalized + String(dream_cellStr)))!, .font: UIFont.fontFile(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_actionFormat.suffix(7)) + String(userListMsg)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(isFailure), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: TextSpaceReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TextSpaceReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: DecipherReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = DecipherReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.fontFile(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(kHiddenKey))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(m_kitTargetViewFormat) + String(showPhotoSizeContent)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.subject().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(topical), for: .touchUpInside)
        //: btn.isHidden = !ModeStageReactiveCompatible.share.appUserConfigMode.showDirectGreet || ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !ModeStageReactiveCompatible.share.appUserConfigMode.showDirectGreet || ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue || ModeStageReactiveCompatible.share.appStatus == OfSum.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: FormThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = FormThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: SupertitleTranslationView = {
        //: let v = TalkingSelectTranslationView()
        let v = SupertitleTranslationView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.pingAll(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: ResultThen = {
        //: let v = TalkingTranslationResultView()
        let v = ResultThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.withRouteModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.fontFile(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(kHiddenKey))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(user_objectMsg)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.subject().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(even), for: .touchUpInside)
        //: btn.isHidden = ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue || ModeStageReactiveCompatible.share.appStatus == OfSum.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: ToResultFlowLayout = {
        //: let v = TalkingLiveRoomGamesView()
        let v = ToResultFlowLayout()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: TitleReactiveCompatible = {
        //: let view = TalkingPrivateBannerView.init()
        let view = TitleReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: RescueRecognizerDelegate = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = RescueRecognizerDelegate()
        //: v.isHidden = (ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (ModeStageReactiveCompatible.share.appStatus == OfSum.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: NameTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = NameTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.nameSession()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ModeViewDelegate: TopicFromBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func fixing(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.keyMake(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ModeViewDelegate: WorkerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func topical() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        ErrorReactiveCompatible.warPartyPing(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.actionKey(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.happening(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func even() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        ErrorReactiveCompatible.futurism(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = DetailPhotoVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().giveAndTake(urlStr: dict?[(String(app_playerStyleUrl))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(user_rowStr.replacingOccurrences(of: "will", with: "l") + "ckGift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ViewModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: main_viewMessage.map{dogBiscuit(lab: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(noti_goldId.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.overInstanceModel(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> PhoneMessageFromReactiveCompatible! {
    func burn(_: HeliogramTableViewController!, from data: V2TIMMessage!) -> PhoneMessageFromReactiveCompatible! {
        //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = DistrictManagerOfReactiveCompatible.endTarget(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = SizeChatManager.frame(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: StickingPointReactiveCompatible.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(app_sendKey.prefix(4)) + "gTyp" + String(main_freshStr))].stringValue == (String(dreamFrameTitle.suffix(7)) + String(showBottomEqualFormat.suffix(6))) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(appUpPath.replacingOccurrences(of: "index", with: "n") + "geDire" + user_liveLabelFormat.replacingOccurrences(of: "profile", with: "ct"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(mainColorMsg.replacingOccurrences(of: "kit", with: "a"))][(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))][(String(userDatabaseButtonMsg.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = SizeCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = SizeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: DetailCellData.self) == true ||
        if cellData?.isKind(of: DetailCellData.self) == true ||
            //: cellData?.isKind(of: WriteCellData.self) == true ||
            cellData?.isKind(of: WriteCellData.self) == true ||
            //: cellData?.isKind(of: SizeCellData.self) == true ||
            cellData?.isKind(of: SizeCellData.self) == true ||
            //: cellData?.isKind(of: TvCellData.self) == true {
            cellData?.isKind(of: TvCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            ErrorReactiveCompatible.termsMap(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            ErrorReactiveCompatible.recordReload(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: PhoneMessageFromReactiveCompatible!) -> TitleViewCell! {
    func withVisible(_: HeliogramTableViewController!, can data: PhoneMessageFromReactiveCompatible!) -> TitleViewCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = externalBodyPart(data: data)

        //: if data.isKind(of: SizeCellData.self) {
        if data.isKind(of: SizeCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = RowEffectMsgCell(style: .default, reuseIdentifier: RowEffectMsgCell.className())
            //: let cellData = data as! SizeCellData
            let cellData = data as! SizeCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.scrapingMsg() {
                //: if cellData.msgModel.quoteMsgInfo.uid == ModeStageReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == ModeStageReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = ModeStageReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = ModeStageReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: WriteCellData.self) {
        if data.isKind(of: WriteCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PathReactiveCompatible(style: .default, reuseIdentifier: PathReactiveCompatible.className())
            //: let cellData = data as! WriteCellData
            let cellData = data as! WriteCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TvCellData.self) {
        if data.isKind(of: TvCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = PetitionMsgCell(style: .default, reuseIdentifier: PetitionMsgCell.className())
            //: let cellData = data as! TvCellData
            let cellData = data as! TvCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: EqualDowryCellData.self) {
        if data.isKind(of: EqualDowryCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = ModelValueViewCell(style: .default, reuseIdentifier: ModelValueViewCell.className())
            //: let cellData = data as! EqualDowryCellData
            let cellData = data as! EqualDowryCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: ResultCellData.self) {
        if data.isKind(of: ResultCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = ValueSizeViewCell(style: .default, reuseIdentifier: ValueSizeViewCell.className())
            //: let cellData = data as! ResultCellData
            let cellData = data as! ResultCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = NnwTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.closeAction(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(noti_iconIdent.suffix(8))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: DetailCellData.self) {
        if data.isKind(of: DetailCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = SizeMsgCell(style: .default, reuseIdentifier: SizeMsgCell.className())
            //: let cellData = data as! DetailCellData
            let cellData = data as! DetailCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: StickingPointReactiveCompatible.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = PerimeterMsgCell(style: .default, reuseIdentifier: PerimeterMsgCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! StickingPointReactiveCompatible
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TopicDetailCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = TerritorialDivisionReactiveCompatible(style: .default, reuseIdentifier: TerritorialDivisionReactiveCompatible.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TopicDetailCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: JumpContextReactiveCompatible.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = ErrorView(style: .default, reuseIdentifier: ErrorView.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! JumpContextReactiveCompatible
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TitleViewCell!) {
    func search(alongAvatarMessageController _: HeliogramTableViewController!, modelData cell: TitleViewCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != MacroReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != MacroReactiveCompatible.picLine() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: SucceedInfoReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
            SucceedInfoReactiveCompatible.share.duringState(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TitleViewCell!) {
    func windExposure(_: HeliogramTableViewController!, of cell: TitleViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PathReactiveCompatible.self) { // 图片
            //: let imageCellData = cell.messageData as! WriteCellData
            let imageCellData = cell.messageData as! WriteCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = ValueToViewController(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.voteModel(imageCellData, subtitleModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.header(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_beautyContent)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.viewWith(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.piece(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                imageColor()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(k_clickReplaceText)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = PicTransformable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = DetailFileHandyJSON()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [PicTransformable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = CloudViewDelegate(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                imageColor()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: PetitionMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! TvCellData
            let videoCellData = cell.messageData as! TvCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = CompassPointViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                imageColor()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = VideoViewController(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.panoramicView(videoCellData, framework: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.header(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_beautyContent)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.viewWith(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.piece(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                imageColor()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: SizeMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! SizeMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! DetailCellData, cellView: audioCell)
            pastAnswer(cellData: audioCell.cellData as! DetailCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: RowEffectMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! RowEffectMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = DistrictManagerOfReactiveCompatible.endTarget(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(mainColorMsg.replacingOccurrences(of: "kit", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(mBlockText.suffix(6)) + String(showNameStr))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(mBlockText.suffix(6)) + String(showNameStr))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (user_sizeData.replacingOccurrences(of: "suite", with: "t") + "Pers" + String(user_actionColorText.suffix(6))) {
                    //: if ModeStageReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if ModeStageReactiveCompatible.share.loginUserMode.isTPAuth == RequestSourceTableConvertible.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if ModeStageReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if ModeStageReactiveCompatible.share.loginUserMode.isTPAuth == RequestSourceTableConvertible.unknown.rawValue {
                        //: SucceedInfoReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                        SucceedInfoReactiveCompatible.share.skiagram(toast: nil)
                        //: } else if ModeStageReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if ModeStageReactiveCompatible.share.loginUserMode.isTPAuth == RequestSourceTableConvertible.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = TaskVerificationVc()
                        //: SucceedInfoReactiveCompatible.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        SucceedInfoReactiveCompatible.share.headUser()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: SucceedInfoReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                        SucceedInfoReactiveCompatible.share.skiagram(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ConstraintModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SucceedInfoReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SucceedInfoReactiveCompatible.share.videoSharedUp(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(app_sendKey.prefix(4)) + "gTyp" + String(main_freshStr))] as? String == (String(dreamFrameTitle.suffix(7)) + String(showBottomEqualFormat.suffix(6))) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))][(String(k_customValue.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    drown()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    appAdd()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TerritorialDivisionReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TerritorialDivisionReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = DistrictManagerOfReactiveCompatible.endTarget(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(mainColorMsg.replacingOccurrences(of: "kit", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(mBlockText.suffix(6)) + String(showNameStr))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ConstraintModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SucceedInfoReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SucceedInfoReactiveCompatible.share.videoSharedUp(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: PerimeterMsgCell.self) {
            //: SucceedInfoReactiveCompatible.share.func__pushToUserDetailVC(uid: targetId)
            SucceedInfoReactiveCompatible.share.duringState(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TitleViewCell!) {
    func detect(_: HeliogramTableViewController!, alongText cell: TitleViewCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: ChatCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PathReactiveCompatible.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! PathReactiveCompatible
            //: cellData = imageCell.cellData as! WriteCellData
            cellData = imageCell.cellData as! WriteCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: PetitionMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! PetitionMsgCell
            //: cellData = videoCell.cellData as! TvCellData
            cellData = videoCell.cellData as! TvCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: SizeMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! SizeMsgCell
            //: cellData = audioCell.cellData as! DetailCellData
            cellData = audioCell.cellData as! DetailCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: RowEffectMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! RowEffectMsgCell
            //: cellData = textCell.cellData as! SizeCellData
            cellData = textCell.cellData as! SizeCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: PhoneMessageFromReactiveCompatible? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: PhoneMessageFromReactiveCompatible? = SizeChatManager.infoData(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: TvCellData.self) {
                if reCellData!.isKind(of: TvCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    bindModel(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    infoOn(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: PhoneMessageFromReactiveCompatible!) {
    func table(_: HeliogramTableViewController!, session cellData: PhoneMessageFromReactiveCompatible!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = ErrorReactiveCompatible.popPhoto(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: PhoneMessageFromReactiveCompatible!) {
    func custom(_: HeliogramTableViewController!, with cellData: PhoneMessageFromReactiveCompatible!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        ErrorReactiveCompatible.meanSolarDay(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: PhoneMessageFromReactiveCompatible) -> PhoneMessageFromReactiveCompatible {
    private func externalBodyPart(data: PhoneMessageFromReactiveCompatible) -> PhoneMessageFromReactiveCompatible {
        //: let isSelf = data.msgModel.user.uid == Int(ModeStageReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(ModeStageReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if ModeStageReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if ModeStageReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = ModeStageReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = ModeStageReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = ModeStageReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = ModeStageReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func talk() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (appLabelPath.lowercased())]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(showStrengthStr.replacingOccurrences(of: "make", with: "e")): (String(userReportName.suffix(7)) + String(dream_oldFormat))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo, String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.infoOn(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func happening(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: showAcceptIdent.map{imaginationImage(manager: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: showAcceptIdent.map{imaginationImage(manager: $0)}, encoding: .utf8)!: content, (String(user_topId.suffix(5))): greetId, String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (appLabelPath.lowercased())]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: dream_colorIdent.reversed(), encoding: .utf8)!: content, (String(user_topId.suffix(5))): greetId, (showStrengthStr.replacingOccurrences(of: "make", with: "e")): (String(user_valueIdent.suffix(5)) + noti_sharedTitle.replacingOccurrences(of: "model", with: "g"))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo, String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.infoOn(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            PassDataDelegate.shared.milk(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.actionKey(showMsg: String(bytes: k_centerMsg.map{pathPlayer(video: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        ErrorReactiveCompatible.above(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(m_subPath.replacingOccurrences(of: "screen", with: "l") + main_modelFormat.replacingOccurrences(of: "person", with: "th"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (String(notiMessagePopKey)),
                               //: "audioId": greetId,
                               (String(dreamKitIdent) + String(dreamCenterIdent.prefix(6))): greetId,
                               //: "audioLength": length,
                               String(bytes: user_backMsg.reversed(), encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (showMaleControlPath.replacingOccurrences(of: "need", with: "ud") + String(k_groupPath.suffix(4)) + "ndbox"): model.unZipFilePath,
                               //: "senderId": ModeStageReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: m_nowTitle.map{analogDigitalConverter(max: $0)}, encoding: .utf8)!: ModeStageReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(dreamKitIdent) + String(dreamCenterIdent.prefix(6))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: user_backMsg.reversed(), encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (showMaleControlPath.replacingOccurrences(of: "need", with: "ud") + String(k_groupPath.suffix(4)) + "ndbox"): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (showStrengthStr.replacingOccurrences(of: "make", with: "e")): (String(noti_valueMsg.suffix(8)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo, String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.infoOn(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func withRouteModel(msgStr: String, quoteModel: ValueModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            actionKey(showMsg: (String(dreamButtonName) + String(noti_requestMessage)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: showAcceptIdent.map{imaginationImage(manager: $0)}, encoding: .utf8)!: temStr, String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (appLabelPath.lowercased())]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: dream_colorIdent.reversed(), encoding: .utf8)!: temStr, (showStrengthStr.replacingOccurrences(of: "make", with: "e")): (String(user_valueIdent.suffix(5)) + noti_sharedTitle.replacingOccurrences(of: "model", with: "g"))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(mainMoveFormat.replacingOccurrences(of: "view", with: "q") + String(dream_actionMessage))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(notiFileHiddenName.replacingOccurrences(of: "title", with: "qu") + "eMsgIn" + String(mainWhiteId))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? ValueModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            infoOn(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func overInstanceModel(img: UIImage, sendModel: ViewModel) {
        //: let imagePath = WriteCellData.getChatImagePath(image: img)
        let imagePath = WriteCellData.writeBlockImage(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: dream_makeUrl.reversed(), encoding: .utf8)!: (String(kTitlePath)), String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (m_collectionValue.replacingOccurrences(of: "choose", with: "m")), String(bytes: app_finishPath.reversed(), encoding: .utf8)!: imagePath, String(bytes: mCommentPath.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(appIconFormat.replacingOccurrences(of: "at", with: "pi")): imagePath,
                                        //: "type": "ImgMsg",
                                        (showStrengthStr.replacingOccurrences(of: "make", with: "e")): (String(kTitlePath)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_viewMessage.map{dogBiscuit(lab: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(noti_goldId.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo, String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            infoOn(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: PhoneMessageFromReactiveCompatible?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func bindModel(msgData: PhoneMessageFromReactiveCompatible?, sendModel: ViewModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: dream_makeUrl.reversed(), encoding: .utf8)!: (String(main_timeFormat) + userCropMessage.replacingOccurrences(of: "equal", with: "g")), String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (String(userPathId)), String(bytes: mCommentPath.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(showStrengthStr.replacingOccurrences(of: "make", with: "e")): (String(main_timeFormat) + userCropMessage.replacingOccurrences(of: "equal", with: "g")),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: kErrorMessage.map{currentTo(name: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: noti_flexibleKey.map{reloadArea(of: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: user_viewFormat.map{indicatorLayer(value: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_viewMessage.map{dogBiscuit(lab: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(noti_goldId.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo, String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = SizeChatManager.thenSenderSub(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.of(uploadCellData)
                self.msgView.of(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.counteraction(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.hide(cellData, britishismMsgStatus: .Msg_Status_Sending_2)
            self.msgView.hide(cellData, britishismMsgStatus: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.counteraction(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.title(dict, object: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        infoOn(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func activity(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (String(notiMessagePopKey)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: user_backMsg.reversed(), encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (showMaleControlPath.replacingOccurrences(of: "need", with: "ud") + String(k_groupPath.suffix(4)) + "ndbox"): voicePath,
                       //: "senderId": ModeStageReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: m_nowTitle.map{analogDigitalConverter(max: $0)}, encoding: .utf8)!: ModeStageReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(showMaleControlPath.replacingOccurrences(of: "need", with: "ud") + String(k_groupPath.suffix(4)) + "ndbox"): voicePath,
                         //: "type": "AudioMsg",
                         (showStrengthStr.replacingOccurrences(of: "make", with: "e")): (String(noti_valueMsg.suffix(8))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: notiMakeContent.reversed(), encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo, String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            infoOn(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: PhoneMessageFromReactiveCompatible, reSend: Bool) {
    private func infoOn(msgData: PhoneMessageFromReactiveCompatible, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            actionKey(showMsg: kTimeLineValue)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(userDatabaseButtonMsg.prefix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: TvCellData.self) == false && msgData.isKind(of: ResultCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: TvCellData.self) == false, msgData.isKind(of: ResultCellData.self) == false, params[(showStrengthStr.replacingOccurrences(of: "make", with: "e"))] as? String != (String(userReportName.suffix(7)) + String(dream_oldFormat)) {
            //: self.msgView.of(msgData)
            self.msgView.of(msgData)
        }
        //: let recordTime = DistrictManagerOfReactiveCompatible.getMessageInsertTime()
        let recordTime = DistrictManagerOfReactiveCompatible.finishInfo()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((appIconFormat.replacingOccurrences(of: "at", with: "pi"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(appIconFormat.replacingOccurrences(of: "at", with: "pi"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(appIconFormat.replacingOccurrences(of: "at", with: "pi"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(showStrengthStr.replacingOccurrences(of: "make", with: "e"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(noti_valueMsg.suffix(8))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((showMaleControlPath.replacingOccurrences(of: "need", with: "ud") + String(k_groupPath.suffix(4)) + "ndbox")) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(showMaleControlPath.replacingOccurrences(of: "need", with: "ud") + String(k_groupPath.suffix(4)) + "ndbox")] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (showMaleControlPath.replacingOccurrences(of: "need", with: "ud") + String(k_groupPath.suffix(4)) + "ndbox"))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(user_normalUrl))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kShowValue + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(showCellStatusName.prefix(5)) + "ents" + String(mainDatabaseMessage.suffix(5)) + "/recor" + String(user_momentImageMsg.prefix(2)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kSenseDismissScreenTitle + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(dreamKitIdent) + String(dreamCenterIdent.prefix(6)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(notiMessagePopKey))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: PushModelReactiveCompatible.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        PushModelReactiveCompatible.accept(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(userReportName.suffix(7)) + String(dream_oldFormat)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.point(ofMagnitudeersectionActive: msgData, changeToHighProfile: nil, pause: NSNumber(value: 0), beginning: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.header(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_barData)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(user_topId.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    ErrorReactiveCompatible.above(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.smallness(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(noti_listData) + kOpMaleStr.replacingOccurrences(of: "data", with: "e"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(noti_listData) + kOpMaleStr.replacingOccurrences(of: "data", with: "e"))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if LengthLiveManager.loadShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kWarnValue, object: nil, userInfo: [String(bytes: k_errorFirstMessage.map{labelMake(share: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.photoRoute(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(m_resultQuickManagerValue))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(userReportName.suffix(7)) + String(dream_oldFormat)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!] = (String(m_insideMessage.suffix(4)) + "Gift")
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kTopTableValue.audienceTap(eventID: k_topFlushName)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(userReportName.suffix(7)) + String(dream_oldFormat)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!] = (appLabelPath.lowercased())
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.of(updateMsgData)
                    self.msgView.of(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(ModeStageReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(ModeStageReactiveCompatible.share.loginUserMode.userID)" + (String(notiSelectedRowMData.prefix(5)) + "ateChatT" + String(notiCenterFailName)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue, type != (String(userReportName.suffix(7)) + String(dream_oldFormat)) {
                //: self.removeReplyTipsView()
                self.toolView()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.point(ofMagnitudeersectionActive: updateMsgData, changeToHighProfile: msgInfo[(String(showMinElementStr))] as? String, pause: msgIncome, beginning: .Msg_Status_Succ)


            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            ErrorReactiveCompatible.recordReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((show_scaleFormat.replacingOccurrences(of: "voice", with: "t") + String(userKeyValue.suffix(4)) + "Coin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(show_scaleFormat.replacingOccurrences(of: "voice", with: "t") + String(userKeyValue.suffix(4)) + "Coin")] as! NSNumber
                //: ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(showShareSucceedKey) + String(notiTouchContent.prefix(7)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(showShareSucceedKey) + String(notiTouchContent.prefix(7)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.anData(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((mainQuitValue.lowercased() + "mate" + String(dreamObjectInfoData.suffix(4)) + String(userTowardName))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(mainQuitValue.lowercased() + "mate" + String(dreamObjectInfoData.suffix(4)) + String(userTowardName))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? RestrictionModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.voteModel(updateMsgData, subtitleModel: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(userPathId))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(userPathId))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? VendorColorModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.panoramicView(updateMsgData, framework: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        meTo()

        //: updateSession()
        doObject()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func doObject() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        CuttingEdgeThen.drunk(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func header(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard ModeStageReactiveCompatible.share.loginUserMode.status != 1 else {
            guard ModeStageReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    actionKey(showMsg: errorStr)
                }
                //: return
                return
            }

            //: SucceedInfoReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            SucceedInfoReactiveCompatible.share.transition(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.whereabouts()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = TopicPopView(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText))
            //: view.show()
            view.changeShow()
            //: } else if errorCode == 60003, !ModeStageReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !ModeStageReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            DeviceReactiveCompatible.shared.sumry()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                actionKey(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func piece(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.photoRoute(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func paradigmView() {
        //: if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.tableName(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(ModeStageReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(ModeStageReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (k_inviteMessageOfData.array(forKey: k_noText)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.messageAnimation()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                k_inviteMessageOfData.set(intimateCacheData, forKey: k_noText)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func voiceNotification(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = DistrictManagerOfReactiveCompatible.endTarget(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(mainColorMsg.replacingOccurrences(of: "kit", with: "a"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(noti_stopMessage.suffix(8)) + String(mainVideoValue))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.perspective(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func photoRoute(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(noti_stopMessage.suffix(8)) + String(mainVideoValue))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(noti_stopMessage.suffix(8)) + String(mainVideoValue))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.perspective(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func perspective(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = CuttingEdgeThen.sizeEnter(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.tableName(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.progressState(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.offspring(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.offspring(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        CuttingEdgeThen.skinColor(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: notiInputFormat.map{startLower(frame: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: mainSourceStr.map{$0^204}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kOutputValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ModeViewDelegate: AdThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func allFor() {
        //: guard ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.signaling(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func rubric(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func wrap(actionType: PathHashable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    funcVoiceEqualRemove()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    pause()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                funcVoiceEqualRemove()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if ModeStageReactiveCompatible.share.loginUserMode.loungePlus {
                if ModeStageReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.stopChange(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                pause()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    addAcrossVideo()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    pause()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                addAcrossVideo()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.viewTitle(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func pause() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.actionKey(showMsg: String(bytes: dreamIndexManagerPerformValue.map{meanSun(dialog: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func magnitudeeractionName(giftModel _: NnwTransformable, giftNum _: String, model _: WriteMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func pan(giftModel: NnwTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        closeAction(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: ResultCellData?, fromStr: String?) {
    func closeAction(giftModel: NnwTransformable, giftNum: String, cellData: ResultCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            actionKey(showMsg: kTimeLineValue)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(userDatabaseButtonMsg.prefix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(noti_goldId.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(show_eyeKey.suffix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(mColorFormat.prefix(6)) + String(show_makeId))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(main_labelPath))] = fromStr
        }
        // 送礼接口
        //: PushModelReactiveCompatible.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        PushModelReactiveCompatible.unsubtleCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.header(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kPointData)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.errorOf(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.viewWith(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.doObject()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(noti_stopMessage.suffix(8)) + String(mainVideoValue))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(noti_stopMessage.suffix(8)) + String(mainVideoValue))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.perspective(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.isVideoMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kTopTableValue.audienceTap(eventID: kTableData)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func noBar(giftModel: NnwTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kTopTableValue.audienceTap(eventID: k_managerTitle)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            actionKey(showMsg: kTimeLineValue)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= ModeStageReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            pause()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(kSharePath))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(showStrengthStr.replacingOccurrences(of: "make", with: "e"))] = (String(k_belowActionName.suffix(5)) + String(notiMakePath.suffix(6)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (String(m_insideMessage.suffix(4)) + "Gift")]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o")): msgInfo, String(bytes: main_giftText.map{managerHundred(frame: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            infoOn(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func recordWith(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kPlayContent - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.device(true)
            self.msgView.device(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func narrow(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.device(true)
            self.msgView.device(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func outBlockRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.outBlockRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                go(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func marque() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if DetailPlayManager.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            DetailPlayManager.shared.totaleractionView()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func viewWith(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!] = (String(main_shareStr))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(m_pathKey.prefix(6)) + kMarginData.replacingOccurrences(of: "game", with: "o"))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = SizeChatManager.thenSenderSub(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.of(cellData)
        self.msgView.of(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(noti_listData) + kOpMaleStr.replacingOccurrences(of: "data", with: "e"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(noti_listData) + kOpMaleStr.replacingOccurrences(of: "data", with: "e"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.point(ofMagnitudeersectionActive: cellData, changeToHighProfile: msgInfoDic[(String(showMinElementStr))] as? String, pause: msgIncome, beginning: .Msg_Status_Succ)

        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        capture(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((show_scaleFormat.replacingOccurrences(of: "voice", with: "t") + String(userKeyValue.suffix(4)) + "Coin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(show_scaleFormat.replacingOccurrences(of: "voice", with: "t") + String(userKeyValue.suffix(4)) + "Coin")] as! NSNumber
            //: ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: ModeStageReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.doingInfo(needReload: false, mf_coin: ModeStageReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func issue() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.showAcross(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func capture(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.rawData(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func modelConstraint(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.backgroundArr(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func invite() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func drown() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.nameEqual { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.doObject()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func appAdd() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.alongName { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.doObject()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func isFailure() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = WorkFeedbackViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        imageColor()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func digitiserLevel() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = FromInfoView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.someSearch(cellTitleList: [(String(user_labelMakeIdent) + String(userEqualData)).localized, (String(m_whiteVoiceMsg)).localized, (String(appToKey)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .startMake()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: SucceedInfoReactiveCompatible.share.func__pushToUserDetailVC(uid: self.targetId)
                SucceedInfoReactiveCompatible.share.duringState(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                nameSession()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = VideoUserDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.wallop(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func nameSession() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = AtNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.ignitionKeyUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.stripped(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func volleyballPlayer() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            go(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            imageColor()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func withFace(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ViewTransformable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.sum(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func everyObject() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(no(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: k_keyData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(equalAfter),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: kMarginContent,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func no(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard ModeStageReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard ModeStageReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func equalAfter() {
        //: ModeStageReactiveCompatible.share.loginUserMode.loungePlus = true
        ModeStageReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func nameUp() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? PhoneMessageFromReactiveCompatible else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? PhoneMessageFromReactiveCompatible else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if ErrorReactiveCompatible.quantityerpret(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.exceptAddLocal(tipStr: (String(dreamTextId.suffix(5)) + "read re" + String(app_leadershipFormat.prefix(7))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ModeViewDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func funcVoiceEqualRemove() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        PounderReactiveCompatible.cellBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SpaceFilePickTool.showVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.bringHomeWithSkiagram(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func camara() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        PounderReactiveCompatible.funcOrIngot(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.bringHomeWithSkiagram(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func bringHomeWithSkiagram(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ViewModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.overInstanceModel(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = CloudViewDelegate(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ViewModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.overInstanceModel(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        imageColor()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func addAcrossVideo() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard ErrorReactiveCompatible.closing(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ToClusterLiteral.male.rawValue ? "He".localized : (String(showEqualViewFormat.prefix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.actionKey(showMsg: (String(dreamBottomPath) + String(kChangeId.suffix(8))).localizedDataArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.unbarOf()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func unbarOf() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = SpaceFilePickTool.showVideo(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            SpaceFilePickTool.toError(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.focusUrl(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.actionKey(showMsg: (String(mKeyMsg.suffix(4)) + "t vi" + String(kLineUrl.suffix(5)) + "t lea" + String(notiEqualKey.prefix(5)) + String(main_fishTitle)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func focusUrl(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: AtEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = RegisterEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ViewModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.bindModel(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        AnagrammatizeView.implant()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        imageColor()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: PhoneMessageFromReactiveCompatible, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func counteraction(msgData: PhoneMessageFromReactiveCompatible, sendModel: ViewModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        StateSourceSaveListener.shared.messageTo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.actionKey(showMsg: (String(m_infoPointMessage.prefix(6)) + String(userImageBackgroundId.prefix(7))).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.point(ofMagnitudeersectionActive: msgData, changeToHighProfile: nil, pause: NSNumber(value: 0), beginning: .Msg_Status_Fail)

                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.bindModel(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    //: func func__addVoiceNotification() {
    func stint() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(addWith),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(cellAndLatissimusDorsi),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(wisecrackSource(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(penetrate(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kWindowValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(voiceNotification(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: k_eventName,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func belowPicture(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            DetailPlayManager.shared.corner()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            DetailPlayManager.shared.deleteWithdraw()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func addWith() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        belowPicture(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func cellAndLatissimusDorsi() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        belowPicture(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func wisecrackSource(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            belowPicture(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            belowPicture(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: DetailCellData, cellView: TalkingChatAudioMsgCell) {
    func pastAnswer(cellData: DetailCellData, cellView: SizeMsgCell) {
        //: guard TalkingSocketManager.shared.isWork == false else {
        guard EffectSocketDelegate.shared.isWork == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.actionKey(showMsg: kNearbyData)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? PhoneMessageFromReactiveCompatible)!.isKind(of: DetailCellData.self) == false {
            if (indexModel as? PhoneMessageFromReactiveCompatible)!.isKind(of: DetailCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! DetailCellData
            let audioModel = indexModel as! DetailCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = CuttingEdgeThen.subTo(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        DetailPlayManager.shared.totaleractionView()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if DetailPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (show_viewVideoId.capitalized) + "\(DetailPlayManager.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if DetailPlayManager.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                DetailPlayManager.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        DetailPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        DetailPlayManager.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: PhoneMessageFromReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! PhoneMessageFromReactiveCompatible
                let subModel: PhoneMessageFromReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! PhoneMessageFromReactiveCompatible
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: DetailCellData.self) {
                if subModel.isKind(of: DetailCellData.self) {
                    //: let audioModel = subModel as! DetailCellData
                    let audioModel = subModel as! DetailCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = CuttingEdgeThen.subTo(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    EvenThen.phonation(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        DetailPlayManager.shared.lodestarBasic(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func attribute() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.buttonWith()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.happening(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? PhoneMessageFromReactiveCompatible
                let indexModel = model as? PhoneMessageFromReactiveCompatible
                //: if indexModel?.isKind(of: DetailCellData.self) == true || indexModel?.isKind(of: WriteCellData.self) == true || indexModel?.isKind(of: SizeCellData.self) == true || indexModel?.isKind(of: TvCellData.self) == true {
                if indexModel?.isKind(of: DetailCellData.self) == true || indexModel?.isKind(of: WriteCellData.self) == true || indexModel?.isKind(of: SizeCellData.self) == true || indexModel?.isKind(of: TvCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.meTo()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.crushEqualFingertip()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.nameUp()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func buttonWith() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.placeOf(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.cardFor(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.paradigmView()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == ModeStageReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == ModeStageReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == ModeStageReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == ModeStageReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.stripped(userWrap)
    }

    //: func func__getFillDraftText() {
    func draftName() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_nameDefineMsg)) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.allFor()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func enable() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_nameDefineMsg)) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    //: func func__TipsNotification() {
    func iconNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(circumcise(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kSizeValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func circumcise(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TopicDetailCellData = userinfo[(app_statusName.replacingOccurrences(of: "number", with: "g"))] as! TopicDetailCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.device(true)
        self.msgView.device(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func exceptAddLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: showAcceptIdent.map{imaginationImage(manager: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: noti_turnMessage.map{$0^170}, encoding: .utf8)!: (main_labelNameFormat.replacingOccurrences(of: "release", with: "s"))]
        //: var message: PhoneMessageFromReactiveCompatible?
        var message: PhoneMessageFromReactiveCompatible?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = SizeChatManager.feed(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = SizeChatManager.thenSenderSub(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.device(true)
        self.msgView.device(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func crushEqualFingertip() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        ErrorReactiveCompatible.profile { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.exceptAddLocal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func anData(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.exceptAddLocal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func meTo() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        ErrorReactiveCompatible.pageBackstage(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.exceptAddLocal(tipStr: String(bytes: show_beanMsg.map{manageressImage(m: $0)}, encoding: .utf8)!.localizedDataArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func penetrate(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: PhoneMessageFromReactiveCompatible = dict?["cellData"] as! PhoneMessageFromReactiveCompatible
        let cellData: PhoneMessageFromReactiveCompatible = dict?[(String(dream_styleId.prefix(8)))] as! PhoneMessageFromReactiveCompatible
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.device(true)
        self.msgView.device(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ModeViewDelegate: MenuTipsProtocol {
    //: func func__setReplyTipsView() {
    func tips() {
        //: let key =  "\(ModeStageReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(ModeStageReactiveCompatible.share.loginUserMode.userID)" + (String(notiSelectedRowMData.prefix(5)) + "ateChatT" + String(notiCenterFailName)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.rootShared()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            fixing(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            toolView()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func stopOkPacket(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        withRouteModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        toolView()
    }

    //: func removeReplyTipsView() {
    func toolView() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            fixing(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func center() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kErrorName, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kHalfText - kPlayContent, width: kBlockRateValue, height: kHalfText)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kBlockRateValue,
                                     //: height: ScreenHeight)
                                     height: kHalfText)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func go(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.startScreen() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if SizeEventHandler.frameShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: k_sessionClickText, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: ProductDataSource.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kHalfText - kPlayContent,
                                     //: width: ScreenWidth,
                                     width: kBlockRateValue,
                                     //: height: ScreenHeight)
                                     height: kHalfText)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if SizeEventHandler.frameShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: k_sessionClickText, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ModeViewDelegate {
    /// 创建UI
    //: private func createUI() {
    private func voiceUp() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(showAddKey.prefix(4)) + "em noti" + showFieldMsg.replacingOccurrences(of: "collection", with: "c") + "tion").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func outTitle() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kPlayContent : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.attributeAt(width: contentView.width,
                                    //: height: contentView.height,
                                    height: contentView.height,
                                    //: corners: [.topLeft, .topRight],
                                    corners: [.topLeft, .topRight],
                                    //: cornerRadii: .init(width: 8, height: 8))
                                    cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_screenData : k_limitNoScreenData)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + k_videoName)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_screenData : k_limitNoScreenData)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + k_videoName)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + k_videoName)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func holdFastEnable() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.volleyballPlayer()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.digitiserLevel()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.wrap(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kTopTableValue.audienceTap(eventID: kDeviceValue, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
