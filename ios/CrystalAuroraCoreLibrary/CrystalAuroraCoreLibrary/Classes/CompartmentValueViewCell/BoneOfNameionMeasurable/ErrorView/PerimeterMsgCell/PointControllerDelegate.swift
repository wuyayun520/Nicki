
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let app_playerMessage:[UInt8] = [0x2f,0x28,0x2f,0x32,0x6e,0x25,0x29,0x22,0x23,0x34,0x7c,0x6f,0x66,0x2e,0x27,0x35,0x66,0x28,0x29,0x32,0x66,0x24,0x23,0x23,0x28,0x66,0x2f,0x2b,0x36,0x2a,0x23,0x2b,0x23,0x28,0x32,0x23,0x22]

private func errorLab(user num: UInt8) -> UInt8 {
    return num ^ 70
}

/*: "roomId" :*/
fileprivate let dream_sessionContent:String = "roomIdroute color show view in"

/*: "type" :*/
fileprivate let main_photoKey:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let noti_imageKey:String = "extindexa"

/*: "jumpKey" :*/
fileprivate let app_userValue:[UInt8] = [0xef,0xf0,0xe8,0xf5,0xce,0xe0,0xfc]

private func holdAspect(view num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "Send Gift" :*/
fileprivate let m_videoName:[Character] = ["S","e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let userVoicePath:[Character] = ["V"]
fileprivate let mainAddFileReplaceText:[Character] = ["i","e","w"," ","i","n","f","o"]

/*: "Report" :*/
fileprivate let kToData:String = "Reportdata application face if give"

/*: "uid" :*/
fileprivate let dream_arrayIdent:[UInt8] = [0x64,0x69,0x75]

/*: "name" :*/
fileprivate let mKeyMessage:[UInt8] = [0x99,0x8c,0x98,0x90]

fileprivate func cellA(voice num: UInt8) -> UInt8 {
    let value = Int(num) + 213
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't send blank message" :*/
fileprivate let main_valueFormat:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let notiPointData:[Character] = ["u","s","e","r"]

/*: "content" :*/
fileprivate let main_hiddenButtonUrl:[UInt8] = [0x8a,0x86,0x87,0x9d,0x8c,0x87,0x9d]

private func nameCloud(lab num: UInt8) -> UInt8 {
    return num ^ 233
}

/*: "msgType" :*/
fileprivate let app_upData:[Character] = ["m","s","g","T","y","p","e"]

/*: "txt" :*/
fileprivate let k_sourceSignValue:[Character] = ["t","x","t"]

/*: "isGroup" :*/
fileprivate let m_removeTitleMessage:String = "isGrotop position pair model value"
fileprivate let user_captureName:String = "UP"

/*: "jumps" :*/
fileprivate let show_cleanImageName:String = "countumps"

/*: "message" :*/
fileprivate let dreamPathUrl:[UInt8] = [0x7b,0x73,0x65,0x65,0x77,0x71,0x73]

private func actualTableShare(size num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "TxtMsg" :*/
fileprivate let app_labMsg:[Character] = ["T","x","t","M","s","g"]

/*: "toUid" :*/
fileprivate let mModelKey:String = "range line statustoUid"

/*: "msgInfo" :*/
fileprivate let userObjectMessage:[Character] = ["m","s","g","I","n","f","o"]

/*: "params" :*/
fileprivate let user_fileItemLineKey:[UInt8] = [0x1a,0xb,0x18,0xb,0x7,0x19]

/*: "contentType" :*/
fileprivate let mainColorMsg:[UInt8] = [0xaf,0xa3,0xa2,0xb8,0xa9,0xa2,0xb8,0x98,0xb5,0xbc,0xa9]

private func planeAngle(model num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "ImgMsg" :*/
fileprivate let showLeadingToTitle:String = "ImgMsgbag path user value"

/*: "img" :*/
fileprivate let show_customValue:String = "hideg"

/*: "imageUri" :*/
fileprivate let userEffectIdent:[UInt8] = [0xa8,0xac,0xa0,0xa6,0xa4,0x94,0xb3,0xa8]

private func scutWork(after num: UInt8) -> UInt8 {
    return num ^ 193
}

/*: "pic" :*/
fileprivate let main_reIdent:String = "pifirst"

/*: "msgId" :*/
fileprivate let mainAppName:String = "self value var letmsgId"

/*: "totalMfCoin" :*/
fileprivate let noti_insideId:String = "totphotol"

/*: "giftId" :*/
fileprivate let mAtData:String = "self super path view whitegiftId"

/*: "giftNum" :*/
fileprivate let main_withoutValue:[Character] = ["g","i"]
fileprivate let main_imageFormat:[Character] = ["f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let notiVideoToTitle:String = "table keypkgIt"
fileprivate let user_makeStr:[Character] = ["t","I","d"]

/*: "msg" :*/
fileprivate let mainLabelFormat:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let app_viewStartData:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let m_valueId:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let show_keyId:String = "nito"
fileprivate let k_countModelPath:String = "knamitem"

/*: "headPic" :*/
fileprivate let mainCookieMessage:[UInt8] = [0xfc,0xf1,0xf5,0xf0,0xc4,0xfd,0xf7]

private func centerData(block num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "All Numbers" :*/
fileprivate let show_videoMessage:[Character] = ["A","l","l"," ","N","u","m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let m_equalDotFormat:[Character] = ["s","e","n","d","N"]
fileprivate let kManagerKey:String = "utouch"

/*: "toUser" :*/
fileprivate let m_sizeValue:String = "equal icontoUser"

/*: "msgIncome" :*/
fileprivate let noti_genderIdent:String = "script view model model emptymsgIncome"

/*: "isConnection" :*/
fileprivate let k_matchFormat:[Character] = ["i","s","C","o","n","n","e","c","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PointControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class PointControllerDelegate: StateViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = LandHandyJSON() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = ReadConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = LandHandyJSON.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: app_playerMessage.map{errorLab(user: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
            if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = PercipientThen(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        gestureSex()
        //: createUIConstraints()
        requestConstraints()
        //: bindInteraction()
        handleUpMagnitudeeraction()
        //: addTapGestureRecognizer()
        dataImage()
        //: func__chatRoom_insertTipMsg()
        dismissAllowPause()
        //: chatSettings()
        imaginationImage()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            showData()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.addSocial(join: false)
        //: self.destroyTipsTimer()
        self.laughDoing()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: HeliogramTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = HeliogramTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(TopicReadView.self, forCellReuseIdentifier: TopicReadView.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PathReactiveCompatible.self, forCellReuseIdentifier: PathReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(HouseThen.self, forCellReuseIdentifier: HouseThen.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(TerritorialDivisionReactiveCompatible.self, forCellReuseIdentifier: TerritorialDivisionReactiveCompatible.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: TitleViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(ViewerView.self, forCellReuseIdentifier: TitleViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: ChemicalElementView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = ChemicalElementView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: TextSpaceReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TextSpaceReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: GasFixtureTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = GasFixtureTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: DecipherReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = DecipherReactiveCompatible(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: CloudUserReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = CloudUserReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension PointControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func imaginationImage() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        EqualEffectReactiveCompatible.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        addSocial(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func addSocial(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(dream_sessionContent.prefix(6))): self.conversationData.groupID, String(bytes: main_photoKey.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: PushModelReactiveCompatible.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        PushModelReactiveCompatible.chatShowCompletion(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - WorkerDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension PointControllerDelegate: WorkerDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> PhoneMessageFromReactiveCompatible! {
    func burn(_: HeliogramTableViewController!, from data: V2TIMMessage!) -> PhoneMessageFromReactiveCompatible! {
        //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = DistrictManagerOfReactiveCompatible.endTarget(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = SizeChatManager.frame(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: PhoneMessageFromReactiveCompatible!) -> TitleViewCell! {
    func withVisible(_: HeliogramTableViewController!, can data: PhoneMessageFromReactiveCompatible!) -> TitleViewCell! {
        //: if data.isKind(of: ProduceReactiveCompatible.self) {
        if data.isKind(of: ProduceReactiveCompatible.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = TopicReadView(style: .default, reuseIdentifier: TopicReadView.className())
            //: let cellData = data as! ProduceReactiveCompatible
            let cellData = data as! ProduceReactiveCompatible
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.wearer(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.award(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: WriteCellData.self) {
        if data.isKind(of: WriteCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PathReactiveCompatible(style: .default, reuseIdentifier: PathReactiveCompatible.className())
            //: let cellData = data as! WriteCellData
            let cellData = data as! WriteCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: RowCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = HouseThen(style: .default, reuseIdentifier: HouseThen.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! RowCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TopicDetailCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = TerritorialDivisionReactiveCompatible(style: .default, reuseIdentifier: TerritorialDivisionReactiveCompatible.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TopicDetailCellData
            //: cell.fill(with: cellData)
            cell.modify(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TitleViewCell!) {
    func search(alongAvatarMessageController _: HeliogramTableViewController!, modelData cell: TitleViewCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(ModeStageReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(ModeStageReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                wearer(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                clickPost(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TitleViewCell!) {
    func windExposure(_: HeliogramTableViewController!, of cell: TitleViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PathReactiveCompatible.self) { // 图片
            //: let imageCellData = cell.messageData as! WriteCellData
            let imageCellData = cell.messageData as! WriteCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(k_clickReplaceText)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = PicTransformable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = DetailFileHandyJSON()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [PicTransformable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = CloudViewDelegate(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.startScreen()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: TopicReadView.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! TopicReadView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = DistrictManagerOfReactiveCompatible.endTarget(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(noti_imageKey.replacingOccurrences(of: "index", with: "r"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: app_userValue.map{holdAspect(view: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ConstraintModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SucceedInfoReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SucceedInfoReactiveCompatible.share.videoSharedUp(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TerritorialDivisionReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TerritorialDivisionReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = DistrictManagerOfReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = DistrictManagerOfReactiveCompatible.endTarget(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(noti_imageKey.replacingOccurrences(of: "index", with: "r"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: app_userValue.map{holdAspect(view: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ConstraintModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SucceedInfoReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SucceedInfoReactiveCompatible.share.videoSharedUp(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TitleViewCell!) {
    func detect(_: HeliogramTableViewController!, alongText cell: TitleViewCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        itemBar(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension PointControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func laughDoing() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func clickPost(user: StreamColorModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = FromInfoView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.someSearch(cellTitleList: [(String(m_videoName)).localized, "@", (String(userVoicePath) + String(mainAddFileReplaceText)).localized, (String(kToData.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.popLessUser(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.atUser(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.wearer(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.menuInCarryingOut(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func popLessUser(user: StreamColorModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.equalShow()
        //: let model = TalkingChatRoomMemberModel()
        let model = WriteMemberModel()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.terrace(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func atUser(user: StreamColorModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: dream_arrayIdent.reversed(), encoding: .utf8)!: String(user.uid), String(bytes: mKeyMessage.map{cellA(voice: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.increaseEnd(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func wearer(uid: Int) {
        //: SucceedInfoReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
        SucceedInfoReactiveCompatible.share.duringState(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func menuInCarryingOut(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = VideoUserDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.wallop(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension PointControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func withRouteModel(msgStr: String, quoteModel _: ValueModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            actionKey(showMsg: (String(main_valueFormat)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: app_userValue.map{holdAspect(view: $0)}, encoding: .utf8)!: (String(notiPointData)), String(bytes: dream_arrayIdent.reversed(), encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: main_hiddenButtonUrl.map{nameCloud(lab: $0)}, encoding: .utf8)!: temStr, (String(app_upData)): (String(k_sourceSignValue)), (String(m_removeTitleMessage.prefix(5)) + user_captureName.lowercased()): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(show_cleanImageName.replacingOccurrences(of: "count", with: "j"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: dreamPathUrl.map{actualTableShare(size: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: main_photoKey.reversed(), encoding: .utf8)!: (String(app_labMsg)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(dream_sessionContent.prefix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(mModelKey.suffix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userObjectMessage)): msgInfo, String(bytes: user_fileItemLineKey.map{$0^106}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            itemBar(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func need(img: UIImage) {
        //: let imagePath = WriteCellData.getChatImagePath(image: img)
        let imagePath = WriteCellData.writeBlockImage(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: mainColorMsg.map{planeAngle(model: $0)}, encoding: .utf8)!: (String(showLeadingToTitle.prefix(6))), (String(app_upData)): (show_customValue.replacingOccurrences(of: "hide", with: "im")), String(bytes: userEffectIdent.map{scutWork(after: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(main_reIdent.replacingOccurrences(of: "first", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: main_photoKey.reversed(), encoding: .utf8)!: (String(showLeadingToTitle.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(dream_sessionContent.prefix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userObjectMessage)): msgInfo, String(bytes: user_fileItemLineKey.map{$0^106}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SizeChatManager.thenSenderSub(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            itemBar(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: PhoneMessageFromReactiveCompatible, reSend: Bool) {
    private func itemBar(msgData: PhoneMessageFromReactiveCompatible, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            actionKey(showMsg: kTimeLineValue)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.of(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((main_reIdent.replacingOccurrences(of: "first", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(main_reIdent.replacingOccurrences(of: "first", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(main_reIdent.replacingOccurrences(of: "first", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: PushModelReactiveCompatible.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        PushModelReactiveCompatible.namePic(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.point(ofMagnitudeersectionActive: msgData, changeToHighProfile: nil, pause: NSNumber(value: 0), beginning: .Msg_Status_Fail)

                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.tabGift(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(userObjectMessage))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.point(ofMagnitudeersectionActive: msgData, changeToHighProfile: msgInfo[(String(mainAppName.suffix(5)))] as? String, pause: NSNumber(value: 0), beginning: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((noti_insideId.replacingOccurrences(of: "photo", with: "a") + "MfCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(noti_insideId.replacingOccurrences(of: "photo", with: "a") + "MfCoin")] as! NSNumber
                //: ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func tabGift(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard ModeStageReactiveCompatible.share.loginUserMode.status != 1 else {
            guard ModeStageReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    actionKey(showMsg: errorStr)
                }
                //: return
                return
            }

            //: SucceedInfoReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            SucceedInfoReactiveCompatible.share.transition(clickEvent: kPointData, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.whereabouts()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            liveLocal(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = TopicPopView(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText))
            //: view.show()
            view.changeShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                actionKey(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension PointControllerDelegate: AdThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func noBar(giftModel _: NnwTransformable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func allFor() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func rubric(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = BorderUserDataSource()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.increaseEnd(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func wrap(actionType: PathHashable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            course()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func pan(giftModel _: NnwTransformable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func magnitudeeractionName(giftModel: NnwTransformable, giftNum: String, model: WriteMemberModel) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            actionKey(showMsg: kTimeLineValue)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(mModelKey.suffix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(mAtData.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(main_withoutValue) + String(main_imageFormat))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(dream_sessionContent.prefix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(notiVideoToTitle.suffix(5)) + "emse" + String(user_makeStr))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: PushModelReactiveCompatible.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        PushModelReactiveCompatible.atTitle(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.tabGift(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.errorOf(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.observerSecond(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func recordWith(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.device(true)
            self.msgView.device(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func narrow(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.device(true)
            self.msgView.device(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func outBlockRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.outBlockRecognizer(tap)
    }

    //: func func__starRecordBtnDown() {}
    func marque() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func activity(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension PointControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func course() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        PounderReactiveCompatible.cellBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SpaceFilePickTool.showVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: SucceedInfoReactiveCompatible.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                SucceedInfoReactiveCompatible.share.headUser()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.need(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func buttonTitle() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        PounderReactiveCompatible.funcOrIngot(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.need(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension PointControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func appeal(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(dream_sessionContent.prefix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension PointControllerDelegate {
    //: func func__TipsNotification() {
    func startPush() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(eventModel(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kSizeValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func eventModel(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TopicDetailCellData = userinfo[(String(mainLabelFormat))] as! TopicDetailCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.device(true)
        self.msgView.device(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func liveLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: main_hiddenButtonUrl.map{nameCloud(lab: $0)}, encoding: .utf8)!: tipStr, (String(app_upData)): (String(app_viewStartData))]
        //: var message: PhoneMessageFromReactiveCompatible?
        var message: PhoneMessageFromReactiveCompatible?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = SizeChatManager.thenSenderSub(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.device(true)
        self.msgView.device(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func dismissAllowPause() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard ModeStageReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard ModeStageReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: ModeStageReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.liveLocal(tipStr: ModeStageReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension PointControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func observerSecond(extral: [String: Any?], model: WriteMemberModel, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(userObjectMessage))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(m_removeTitleMessage.prefix(5)) + user_captureName.lowercased())] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(app_upData))] = (String(m_valueId))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(userObjectMessage))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: dream_arrayIdent.reversed(), encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (show_keyId.replacingOccurrences(of: "to", with: "c") + k_countModelPath.replacingOccurrences(of: "item", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: mainCookieMessage.map{centerData(block: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(show_videoMessage)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(m_valueId))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(m_valueId))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(show_keyId.replacingOccurrences(of: "to", with: "c") + k_countModelPath.replacingOccurrences(of: "item", with: "e"))] = "\(giftdic[(String(m_equalDotFormat) + kManagerKey.replacingOccurrences(of: "touch", with: "m"))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(m_sizeValue.suffix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = SizeChatManager.thenSenderSub(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.of(cellData)
        self.msgView.of(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(noti_genderIdent.suffix(9)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(noti_genderIdent.suffix(9)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.point(ofMagnitudeersectionActive: cellData, changeToHighProfile: msgInfoDic[(String(mainAppName.suffix(5)))] as? String, pause: msgIncome, beginning: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        small(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((noti_insideId.replacingOccurrences(of: "photo", with: "a") + "MfCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(noti_insideId.replacingOccurrences(of: "photo", with: "a") + "MfCoin")] as! NSNumber
            //: ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            ModeStageReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: ModeStageReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.doingInfo(needReload: false, mf_coin: ModeStageReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.rescueWithinMembers()
    }

    //: func initCachEffectData() {
    func showData() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.showAcross(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func small(giftDic: [String: Any], model: WriteMemberModel) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.bestowalModel(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in TrivialityThen.share.showMessage() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [ValueHandyJSON]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.outSign(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.rawData(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func lineTag(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in TrivialityThen.share.showMessage() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [ValueHandyJSON]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.outSign(model: giftModel as! BoneOfNameionMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.backgroundArr(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension PointControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func gestureSex() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func requestConstraints() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(k_limitNoScreenData)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(k_limitNoScreenData)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - k_videoName)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func handleUpMagnitudeeraction() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = BorderUserDataSource()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! PhoneMessageFromReactiveCompatible).isKind(of: ProduceReactiveCompatible.self) {
                    if (cellData as! PhoneMessageFromReactiveCompatible).isKind(of: ProduceReactiveCompatible.self) {
                        //: let groupCellData = cellData as! ProduceReactiveCompatible
                        let groupCellData = cellData as! ProduceReactiveCompatible
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(mainAppName.suffix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: DistanceConversationListener.shared.rx
        DistanceConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(k_matchFormat)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
