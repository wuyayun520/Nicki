
//: Declare String Begin

/*: "msgType" :*/
fileprivate let noti_titleErrorMessagePath:String = "click in image frontmsgType"

/*: "tips" :*/
fileprivate let userModelMsg:String = "tipview"

/*: "extra" :*/
fileprivate let main_backgroundTitle:String = "statusxtra"

/*: "msgInfo" :*/
fileprivate let kLabAppTryValue:String = "equal equalmsgInf"
fileprivate let mainLikeVideoName:String = "cell"

/*: "user" :*/
fileprivate let main_clickPlayerMsg:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let noti_sizeTitle:String = "uneed"

/*: "nickname" :*/
fileprivate let appCollectionData:[UInt8] = [0x13,0x14,0x1e,0x16,0x13,0x1c,0x10,0x18]

private func mTheory(tool num: UInt8) -> UInt8 {
    return num ^ 125
}

/*: "headPic" :*/
fileprivate let userCurrentPath:[UInt8] = [0x0,0xd,0x9,0xc,0x38,0x1,0xb]

private func pathFrom(content num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "age" :*/
fileprivate let notiVideoStr:[UInt8] = [0x65,0x67,0x61]

/*: "gift" :*/
fileprivate let show_strokeKey:String = "currentift"

/*: "isGroup" :*/
fileprivate let appLeadingTablePath:String = "isGrouptransform to"

/*: "WantGift" :*/
fileprivate let app_userValue:String = "app for in number tipWantGif"
fileprivate let dream_lastTitle:[Character] = ["t"]

/*: "txt" :*/
fileprivate let dream_styleFormat:[Character] = ["t","x","t"]

/*: "img" :*/
fileprivate let app_equalData:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let dreamFileName:[Character] = ["v","i","d","e","o"]

/*: "audio" :*/
fileprivate let show_greetTitle:String = "downudio"

/*: "messageType" :*/
fileprivate let app_labelFormat:[Character] = ["m","e","s","s","a"]
fileprivate let mainLayerStr:[Character] = ["g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let kObserverMessage:String = "contentTyview self message height extension"
fileprivate let show_imageId:String = "view"

/*: "ImgMsg" :*/
fileprivate let mainDataUrl:[Character] = ["I","m","g","M","s","g"]

/*: "imageUri" :*/
fileprivate let app_frameDeviceText:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let main_ofText:String = "tlocale"

/*: "pic" :*/
fileprivate let mainJumpValue:[Character] = ["p","i","c"]

/*: "isBurn" :*/
fileprivate let m_userAppData:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let kDeleteVoiceFormat:String = "name"
fileprivate let user_lessValue:String = "gift kindiftId"

/*: "params" :*/
fileprivate let user_goText:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let userItemName:[Character] = ["a","u","d","i","o","L","e"]
fileprivate let mainEqualPath:[Character] = ["n","g","t","h"]

/*: "len" :*/
fileprivate let mainModelZoneData:String = "leat"

/*: "audioSandbox" :*/
fileprivate let userPersonId:String = "AUDI"

/*: "senderId" :*/
fileprivate let appArrayMessage:String = "pop"
fileprivate let mainAddStyleFalseValue:String = "player row add star datanderId"

/*: "AudioMsg" :*/
fileprivate let app_menuFormat:String = "true data searchAudioMs"
fileprivate let app_hiddenTitle:String = "line"

/*: "content" :*/
fileprivate let dreamAvailableFormat:[Character] = ["c","o","n","t","e","n","t"]

/*: "TxtMsg" :*/
fileprivate let user_mapValue:String = "TxtMsgstring value for"

/*: "message" :*/
fileprivate let dream_theData:String = "mtitlessagtitle"

/*: "quoteMsgId" :*/
fileprivate let kVoicePath:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let userStartIdent:String = "quoteMserror var"
fileprivate let appSuccessKey:String = "gInfostatus progress touch"

/*: "rYMsgType" :*/
fileprivate let userColorData:String = "rYMscurrent deadline"
fileprivate let user_iconTitle:String = "view"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let m_arrayEqualIdent:[Character] = ["G","J",":","S","e","s","s",":","A","u","t","o","G","r","e","e","t",":","T","x","t"]
fileprivate let mToBottomText:String = "Customlet view"

/*: "changeDirection" :*/
fileprivate let dream_logTextName:String = "changeDclass color down make size"
fileprivate let appLoadValue:[Character] = ["i","r","e"]
fileprivate let mViewSinceName:[Character] = ["c","t","i","o","n"]

/*: "system" :*/
fileprivate let user_frameName:String = "backgroundybackgroundtem"

/*: "title" :*/
fileprivate let dream_followKey:String = "TITLE"

/*: "id" :*/
fileprivate let user_rawValue:[Character] = ["i","d"]

/*: "icon" :*/
fileprivate let mainAddValue:[Character] = ["i","c","o","n"]

/*: "all" :*/
fileprivate let app_wantPath:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let m_resignData:[Character] = ["i","c","o","n","_","c","h","a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let show_sharedGroupFormat:String = "slabtu"
fileprivate let main_adjustmentValue:[Character] = ["s"]

/*: "msgIncome" :*/
fileprivate let dreamPlayerOfIdent:String = "msgIntap face"

/*: "msgId" :*/
fileprivate let mWrapLabStr:String = "msgIdend var return up"

/*: "DetailTotalUnsignedMagnitude" :*/
fileprivate let kModelPath:String = "at nor var text touchTMsgT"
fileprivate let showMakeMessage:String = "ranslato"
fileprivate let main_ofName:String = "eStatusam model user left"

/*: "TMsgTranslatedContent" :*/
fileprivate let kOnValueInMessage:String = "TMsgTbottom in add random"
fileprivate let notiFieldToolTitle:String = "nearby other messagetedCo"

/*: "formatType" :*/
fileprivate let mainUserUrl:[Character] = ["f","o","r","m","a","t","T","y"]
fileprivate let user_errorStylePath:String = "pindex"

/*: "plain" :*/
fileprivate let main_eventContent:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let kRangeData:[Character] = ["i","m","a","g","e","D","a","t","a"]

/*: "VideoMsg" :*/
fileprivate let showViewId:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let noti_managerName:[Character] = ["m","a","t","c","h"]

/*: "mf" :*/
fileprivate let kIndexUrl:String = "tof"

/*: "userIntro" :*/
fileprivate let mAppButtonTitle:[Character] = ["u","s","e","r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let m_timeText:String = "taratet"
fileprivate let app_pathContent:String = "Idpic label field frame"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let show_titleName:String = "yyyy-Mtext any equal"
fileprivate let app_fileModelMessage:String = "shared else status featureHH:mm:"
fileprivate let user_sexEqualMsg:String = "mentalmental"

/*: "sendTime" :*/
fileprivate let mainListName:[Character] = ["s","e","n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let main_pathStr:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let k_rowUrl:[Character] = ["a","u","d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let showDetailName:[Character] = ["a"]
fileprivate let user_managerMessage:[Character] = ["u","d","i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SizeChatManager.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum TaskNameConvertible: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - SizeChatManager

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class SizeChatManager: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func tot() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> PhoneMessageFromReactiveCompatible? {
    public class func feed(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> PhoneMessageFromReactiveCompatible? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = thenSenderSub(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: DistanceConversationListener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            DistanceConversationListener.shared.effectRankAction(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> PhoneMessageFromReactiveCompatible? {
    public class func thenSenderSub(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> PhoneMessageFromReactiveCompatible? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(noti_titleErrorMessagePath.suffix(7)))] = (userModelMsg.replacingOccurrences(of: "view", with: "s"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(main_backgroundTitle.replacingOccurrences(of: "status", with: "e")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = captureColor(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TopicDetailCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = FromModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? QuantityeractionModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(main_clickPlayerMsg))] = [
            //: "uid": ModeStageReactiveCompatible.share.loginUserMode.userID,
            (noti_sizeTitle.replacingOccurrences(of: "need", with: "id")): ModeStageReactiveCompatible.share.loginUserMode.userID,
            //: "nickname": ModeStageReactiveCompatible.share.loginUserMode.nickname,
            String(bytes: appCollectionData.map{mTheory(tool: $0)}, encoding: .utf8)!: ModeStageReactiveCompatible.share.loginUserMode.nickname,
            //: "headPic": ModeStageReactiveCompatible.share.loginUserMode.headPic,
            String(bytes: userCurrentPath.map{pathFrom(content: $0)}, encoding: .utf8)!: ModeStageReactiveCompatible.share.loginUserMode.headPic,
            //: "age": ModeStageReactiveCompatible.share.loginUserMode.age,
            String(bytes: notiVideoStr.reversed(), encoding: .utf8)!: ModeStageReactiveCompatible.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(main_backgroundTitle.replacingOccurrences(of: "status", with: "e")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = captureColor(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (show_strokeKey.replacingOccurrences(of: "current", with: "g")) {
            //: var data: PhoneMessageFromReactiveCompatible
            var data: PhoneMessageFromReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(appLeadingTablePath.prefix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = RowCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = EqualDowryCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = EqualDowryCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? FromModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (String(app_userValue.suffix(7)) + String(dream_lastTitle)) {
            //: let data = ResultCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ResultCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? FromModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (String(dream_styleFormat)) {
            //: var data: PhoneMessageFromReactiveCompatible
            var data: PhoneMessageFromReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(appLeadingTablePath.prefix(7)))] as? String == "1" { // 群聊
                //: data = ProduceReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ProduceReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = SizeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = SizeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? FromModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (String(app_equalData)) {
            //: let data = WriteCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = WriteCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? FromModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (String(dreamFileName)) {
            //: let data = TvCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TvCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? FromModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (show_greetTitle.replacingOccurrences(of: "down", with: "a")) {
            //: let data = DetailCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DetailCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? FromModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(show_strokeKey.replacingOccurrences(of: "current", with: "g"))] != nil && (msgInfo[(String(app_labelFormat) + String(mainLayerStr))] as? Int) == 4 {
            //: let data = SizeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SizeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? FromModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: PhoneMessageFromReactiveCompatible) -> PhoneMessageFromReactiveCompatible? {
    public class func infoData(withData cellData: PhoneMessageFromReactiveCompatible) -> PhoneMessageFromReactiveCompatible? {
        // 图片
        //: if let reCellData = cellData as? WriteCellData {
        if let reCellData = cellData as? WriteCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(MacroReactiveCompatible.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(MacroReactiveCompatible.giftThumb())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(kObserverMessage.prefix(9)) + show_imageId.replacingOccurrences(of: "view", with: "pe"))] = (String(mainDataUrl))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] = (String(app_equalData))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(app_frameDeviceText))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(main_ofText.replacingOccurrences(of: "local", with: "yp"))] = (String(mainDataUrl))
            //: params["pic"] = filePath
            params[(String(mainJumpValue))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(m_userAppData))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(m_userAppData))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(kDeleteVoiceFormat.replacingOccurrences(of: "name", with: "g") + String(user_lessValue.suffix(5)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(kDeleteVoiceFormat.replacingOccurrences(of: "name", with: "g") + String(user_lessValue.suffix(5)))] = "0"
            }

            //: dic["params"] = params
            dic[(String(user_goText))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = thenSenderSub(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? TvCellData {
        if let reCellData = cellData as? TvCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? DetailCellData {
        if let reCellData = cellData as? DetailCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = CuttingEdgeThen.subTo(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(userItemName) + String(mainEqualPath))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(mainModelZoneData.replacingOccurrences(of: "at", with: "n"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(userPersonId.lowercased() + "oSandbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(userPersonId.lowercased() + "oSandbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] = (show_greetTitle.replacingOccurrences(of: "down", with: "a"))
            //: msgInfo["senderId"] = ModeStageReactiveCompatible.share.loginUserMode.userID
            msgInfo[(appArrayMessage.replacingOccurrences(of: "pop", with: "se") + String(mainAddStyleFalseValue.suffix(6)))] = ModeStageReactiveCompatible.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(main_ofText.replacingOccurrences(of: "local", with: "yp"))] = (String(app_menuFormat.suffix(7)) + app_hiddenTitle.replacingOccurrences(of: "line", with: "g"))
            //: dic["params"] = params
            dic[(String(user_goText))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = thenSenderSub(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? SizeCellData {
        if let reCellData = cellData as? SizeCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(dreamAvailableFormat))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(noti_titleErrorMessagePath.suffix(7)))] = (String(dream_styleFormat))
            //: dic["msgInfo"] = msgInfo
            dic[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(main_ofText.replacingOccurrences(of: "local", with: "yp"))] = (String(user_mapValue.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(dream_theData.replacingOccurrences(of: "title", with: "e"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.scrapingMsg() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(kVoicePath))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(userStartIdent.prefix(7)) + String(appSuccessKey.prefix(5)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(user_goText))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = thenSenderSub(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> PhoneMessageFromReactiveCompatible? {
    public class func frame(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> PhoneMessageFromReactiveCompatible? {
        //: var cellData: PhoneMessageFromReactiveCompatible?
        var cellData: PhoneMessageFromReactiveCompatible?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(main_backgroundTitle.replacingOccurrences(of: "status", with: "e"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(main_clickPlayerMsg))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? FromModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == ModeStageReactiveCompatible.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == ModeStageReactiveCompatible.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == ModeStageReactiveCompatible.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == ModeStageReactiveCompatible.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(userColorData.prefix(4)) + "gTyp" + user_iconTitle.replacingOccurrences(of: "view", with: "e"))] as? String == (String(m_arrayEqualIdent) + String(mToBottomText.prefix(6))) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(dream_logTextName.prefix(7)) + String(appLoadValue) + String(mViewSinceName))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = compartmentVersionMessage(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (user_frameName.replacingOccurrences(of: "background", with: "s")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = FromModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? QuantityeractionModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TopicDetailCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(dreamAvailableFormat))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = WriteCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = WriteCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = TvCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TvCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = RowCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = EqualDowryCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = EqualDowryCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = buildLimit(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(main_backgroundTitle.replacingOccurrences(of: "status", with: "e"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? WithModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = ResultCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ResultCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = DetailCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DetailCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? DetailCellData {
            if let audioCellData = cellData as? DetailCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = inviteModel(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = EvenThen.toAdd(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = EvenThen.popHave(packShould(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = StickingPointReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(dream_followKey.lowercased())] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(String(dreamAvailableFormat))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(app_equalData))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = JumpContextReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ProduceReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ProduceReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = SizeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SizeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? StreamColorModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(String(mainAddValue))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: ModeStageReactiveCompatible.share.loginUserMode.headPic ?? ""
                        ModeStageReactiveCompatible.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = WithModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(dreamAvailableFormat))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(main_backgroundTitle.replacingOccurrences(of: "status", with: "e"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = ShowTempModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(app_wantPath))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(m_resignData))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.styleObject(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(show_sharedGroupFormat.replacingOccurrences(of: "lab", with: "ta") + String(main_adjustmentValue))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(dreamPlayerOfIdent.prefix(5)) + "come")] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(app_labelFormat) + String(mainLayerStr))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(mWrapLabStr.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(kModelPath.suffix(5)) + showMakeMessage.replacingOccurrences(of: "to", with: "t") + String(main_ofName.prefix(7)))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = DetailTotalUnsignedMagnitude(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(kOnValueInMessage.prefix(5)) + "ransla" + String(notiFieldToolTitle.suffix(5)) + "ntent")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = RestrictionModel.message()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = RestrictionModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = VendorColorModel.soberUp()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = VendorColorModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = AmbitionModel.pic()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func compartmentVersionMessage(_ extraDic: [String: Any]) -> TaskNameConvertible {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(main_backgroundTitle.replacingOccurrences(of: "status", with: "e"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? FromModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(mainUserUrl) + user_errorStylePath.replacingOccurrences(of: "index", with: "e"))] as? String == (String(main_eventContent)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(app_frameDeviceText))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(kRangeData))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(showViewId)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = singingVoiceTo(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(noti_managerName))] as? Int) == 1 || (msgInfo?[(String(noti_managerName))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (userModelMsg.replacingOccurrences(of: "view", with: "s")) || (dic?[(String(app_labelFormat) + String(mainLayerStr))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(mainUserUrl) + user_errorStylePath.replacingOccurrences(of: "index", with: "e"))] as? String == "mf") && ((dic?[(String(app_labelFormat) + String(mainLayerStr))] as? Int) == 15 || (dic?[(String(app_labelFormat) + String(mainLayerStr))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (String(mAppButtonTitle)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(noti_titleErrorMessagePath.suffix(7)))] as? String == (String(app_userValue.suffix(7)) + String(dream_lastTitle)) || (msgInfo?[(String(app_labelFormat) + String(mainLayerStr))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func packShould(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.styleObject(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(main_backgroundTitle.replacingOccurrences(of: "status", with: "e"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(kLabAppTryValue.suffix(6)) + mainLikeVideoName.replacingOccurrences(of: "cell", with: "o"))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(main_clickPlayerMsg))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(appArrayMessage.replacingOccurrences(of: "pop", with: "se") + String(mainAddStyleFalseValue.suffix(6)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == ModeStageReactiveCompatible.share.loginUserMode.userID {
        if senderId == ModeStageReactiveCompatible.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(appArrayMessage.replacingOccurrences(of: "pop", with: "se") + String(mainAddStyleFalseValue.suffix(6)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(m_timeText.replacingOccurrences(of: "at", with: "g") + String(app_pathContent.prefix(2)))] = message.userID ?? (user?[(m_timeText.replacingOccurrences(of: "at", with: "g") + String(app_pathContent.prefix(2)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(appArrayMessage.replacingOccurrences(of: "pop", with: "se") + String(mainAddStyleFalseValue.suffix(6)))] = "\(msgInfo?[(noti_sizeTitle.replacingOccurrences(of: "need", with: "id"))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(m_timeText.replacingOccurrences(of: "at", with: "g") + String(app_pathContent.prefix(2)))] = "\(msgInfo?[(noti_sizeTitle.replacingOccurrences(of: "need", with: "id"))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(mWrapLabStr.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(mWrapLabStr.prefix(5)))] = "\(msgInfo?[(String(mWrapLabStr.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.styleObject(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(mWrapLabStr.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(mWrapLabStr.prefix(5)))] = "\(customDic[(String(mWrapLabStr.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(mWrapLabStr.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((userPersonId.lowercased() + "oSandbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(userPersonId.lowercased() + "oSandbox")] = "\(msgInfo?[(userPersonId.lowercased() + "oSandbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(userItemName) + String(mainEqualPath))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(userItemName) + String(mainEqualPath))] = "\(msgInfo?[(String(userItemName) + String(mainEqualPath))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(show_titleName.prefix(6)) + "M-dd " + String(app_fileModelMessage.suffix(6)) + user_sexEqualMsg.replacingOccurrences(of: "mental", with: "s"))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(mainListName))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == ModeStageReactiveCompatible.share.loginUserMode.userID ? "1" : "0"
        info?[(String(main_pathStr))] = senderId == ModeStageReactiveCompatible.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func inviteModel(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = packShould(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(mWrapLabStr.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func buildLimit(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func captureColor(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func singingVoiceTo(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(noti_titleErrorMessagePath.suffix(7)))] as? String, msgType == (show_greetTitle.replacingOccurrences(of: "down", with: "a")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(kObserverMessage.prefix(9)) + show_imageId.replacingOccurrences(of: "view", with: "pe"))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(app_menuFormat.suffix(7)) + app_hiddenTitle.replacingOccurrences(of: "line", with: "g"))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(k_rowUrl))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(showDetailName) + String(user_managerMessage))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
