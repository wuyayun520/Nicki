
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dream_feeArrayTitle:[UInt8] = [0xfb,0xfc,0xfb,0xe6,0xba,0xf1,0xfd,0xf6,0xf7,0xe0,0xa8,0xbb,0xb2,0xfa,0xf3,0xe1,0xb2,0xfc,0xfd,0xe6,0xb2,0xf0,0xf7,0xf7,0xfc,0xb2,0xfb,0xff,0xe2,0xfe,0xf7,0xff,0xf7,0xfc,0xe6,0xf7,0xf6]

/*: "uid" :*/
fileprivate let userEmptyManagerUrl:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let m_enterStr:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let notiItemPhotoValue:String = "nprogress"
fileprivate let userInfoCollectionMsg:String = "cknamback"

/*: "birthday" :*/
fileprivate let dream_sharedVideoUrl:String = "birtequal"
fileprivate let mSuiteText:[Character] = ["a","y"]

/*: "videoUrl" :*/
fileprivate let mSumFormat:String = "vdataeo"
fileprivate let app_textValue:[Character] = ["U","r","l"]

/*: "cover" :*/
fileprivate let user_femaleKey:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let showEqualNumberLabelIdent:String = "locnameion"

/*: "cmd" :*/
fileprivate let appLineId:[UInt8] = [0x7d,0x73,0x7a]

/*: "refuseCall" :*/
fileprivate let user_playMainTitle:String = "R"
fileprivate let m_nameValue:[Character] = ["e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let noti_viewMessage:[UInt8] = [0xb9,0xbc,0xa9,0xbc]

private func pathEqual(manager num: UInt8) -> UInt8 {
    return num ^ 221
}

/*: "Call interruption" :*/
fileprivate let dreamStartPostData:String = "error container letCall i"
fileprivate let user_mainMessage:[Character] = ["p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let notiGiftMsg:String = "context file match instopCall"

/*: "logId" :*/
fileprivate let dream_imageKey:[UInt8] = [0x65,0x66,0x6e,0x40,0x6d]

private func powerTool(task num: UInt8) -> UInt8 {
    return num ^ 9
}

/*: "cancelCall" :*/
fileprivate let m_pushPath:String = "cmediumnce"

/*: "No answer" :*/
fileprivate let user_infoTextIdent:String = "start deadline returnNo a"
fileprivate let kSucceedPathMsg:String = "NSWER"

/*: "duration" :*/
fileprivate let noti_moreTitle:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let user_startValueKey:String = "She hthan self edit price"
fileprivate let show_courseMsg:String = "add to color view color up"

/*: "cancelSex" :*/
fileprivate let user_makePath:String = "CANC"

/*: "He has hung up" :*/
fileprivate let m_taskId:String = "He hasmake record view size white"

/*: "endType" :*/
fileprivate let showTimeId:String = "table"
fileprivate let noti_errorContent:String = "view than ifndType"

/*: "stopSex" :*/
fileprivate let user_textId:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let main_elementArrayText:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x73,0x6e,0x69,0x6f,0x63,0x20,0x64,0x6c,0x6f,0x67,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let dream_timeValue:String = "child view title if imageThe oth"
fileprivate let mainTotalTitle:String = "er path re view self"
fileprivate let main_itemTitleStr:String = "post filehung u"
fileprivate let main_makeFormat:String = "plabone"

/*: "type" :*/
fileprivate let userAtIdent:String = "TYPE"

/*: "jumpUrl" :*/
fileprivate let notiViewPath:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let show_modelUrl:[Character] = ["C","a","n","c","e"]
fileprivate let mainHeadName:String = "L"

/*: "Get gold coins" :*/
fileprivate let kLikeValue:[Character] = ["G","e","t"," ","g"]
fileprivate let kBottomIdent:[Character] = ["o","l","d"," ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let mKeyUrl:String = "data return make headaddBean"

/*: "refuseSex" :*/
fileprivate let appButtonPath:String = "refupic"

/*: "onAcceptCall" :*/
fileprivate let dreamTitleValue:String = "onAcindex center return"
fileprivate let m_backgroundMsg:String = "to"

/*: "acceptCall" :*/
fileprivate let show_makeMsg:String = "in lab if wantacceptC"
fileprivate let app_colorFormat:String = "agesture"

/*: "Calls are cancelled" :*/
fileprivate let show_fromUrl:String = "Callsextension corner"
fileprivate let dream_tempDomainCellId:[Character] = ["n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let dream_frameUrl:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LightErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let k_partyText = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class LightErrorDelegate: StateViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: LibraryChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PushPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: EqualValueView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: InfoTitleScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        EffectSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        EffectSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        EffectSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dream_feeArrayTitle.map{$0^146}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! ToNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.betweenLine()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            messageDown()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            listGift(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.effectOff()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        errorRequest()
        //: setupSubViewsConstraint()
        notList()
        //: initNotificationObserver()
        toUponWitness()
        //: req_callGetUserInfo()
        consumerInfo()
        //: timeoutRules()
        equalUp()
        //: setUpBGMPlayer()
        resumeBy()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.isBreakIn()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
            if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = PercipientThen(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isWork = false
        EffectSocketDelegate.shared.isWork = false
        //: self.stopVCAllActivties()
        self.beActivties()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: ConstraintVideoView = {
        //: let v = TalkingBGVideoView()
        let v = ConstraintVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.whenSkip(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: EffectManagerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = EffectManagerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: FromReactiveCompatible = {
        //: let v = TalkingVideoWindowView()
        let v = FromReactiveCompatible()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kBlockRateValue - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if ManagerThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kPositionName + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.betweenVideo()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ConstraintChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = ConstraintChatManager()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension LightErrorDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func consumerInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ConstraintChatManager.miniInstance(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: userEmptyManagerUrl.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(m_enterStr))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(notiItemPhotoValue.replacingOccurrences(of: "progress", with: "i") + userInfoCollectionMsg.replacingOccurrences(of: "back", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(dream_sharedVideoUrl.replacingOccurrences(of: "equal", with: "hd") + String(mSuiteText))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(mSumFormat.replacingOccurrences(of: "data", with: "id") + String(app_textValue))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(user_femaleKey))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(showEqualNumberLabelIdent.replacingOccurrences(of: "name", with: "at"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.getHold()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.sole()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func sole() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = ErrorCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = ErrorCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.errorBackground(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.roundQuery(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension LightErrorDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func messageDown() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        PounderReactiveCompatible.goOverDown { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.actionKey(showMsg: kAskValue)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (user_playMainTitle.lowercased() + String(m_nameValue)), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: userEmptyManagerUrl.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.panoramicView()
                //: return
                return
            }

            //: self.startPreview()
            self.isBreakIn()
            //: self.stopBGM()
            self.textWith()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PushPlayListener.untilPage(type: .Response)
            //: self.startBGM()
            self.betweenLine()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func isBreakIn() {
        //: self.videoManager.startPreview()
        self.videoManager.clickOf()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func beActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.textWith()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        RestraintThen.danmuSocial()
        //: TalkingSocketManager.shared.callerDelegate = nil
        EffectSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        EffectSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        EffectSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func panoramicView(animated: Bool = true) {
        //: self.stopBGM()
        self.textWith()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func betweenVideo() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.sizeView()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.sole()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func edition() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.colorApp()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.sole()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.instanceDetail()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func instanceDetail() {
        //: guard ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = InfoTitleScreenView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.photoContent()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension LightErrorDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func toUponWitness() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(appRequestNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(cameraRow(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: k_statusRecordScreenData,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(viewNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kConnectValue,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        k_partyText.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func appRequestNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            panoramicView()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func viewNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard EffectSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        EffectSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        EffectSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        EffectSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.haveAvailable()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func cameraRow(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.sole()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension LightErrorDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.actionKey(showMsg: (String(dreamStartPostData.suffix(6)) + "nterru" + String(user_mainMessage)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (String(notiGiftMsg.suffix(8))), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (m_pushPath.replacingOccurrences(of: "medium", with: "a") + "lCall"), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (user_playMainTitle.lowercased() + String(m_nameValue)), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: userEmptyManagerUrl.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.vanishHolder()
                //: TalkingSocketManager.shared.currWorkVC = nil
                EffectSocketDelegate.shared.currWorkVC = nil
            }
            //: popCurrentViewController()
            panoramicView()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension LightErrorDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func resumeBy() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DetailSignedNumeric.Call : DetailSignedNumeric.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PushPlayListener.untilPage(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func betweenLine() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.behindMenu()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func textWith() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.dominoStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension LightErrorDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func equalUp() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kDomainMarginContent : self.chatModel?.popularAnswer()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            imageTo()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.imageTo()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func imageTo() {
        //: guard !TalkingSocketManager.shared.isWork else { return }
        guard !EffectSocketDelegate.shared.isWork else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.actionKey(showMsg: (String(user_infoTextIdent.suffix(4)) + kSucceedPathMsg.lowercased()).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (m_pushPath.replacingOccurrences(of: "medium", with: "a") + "lCall"), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (user_playMainTitle.lowercased() + String(m_nameValue)), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: userEmptyManagerUrl.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        EffectSocketDelegate.shared.crosscutNameReason(info: socketDict)
        //: popCurrentViewController()
        panoramicView()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension LightErrorDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func requestVideo(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(noti_moreTitle))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.administrativeDivision(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.viaViewsExhibitStage()
            //: createTalkRoom()
            edition()
            //: self.stopBGM()
            self.textWith()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func listGift(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.sizeView()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.whenSkip(show: false)
        //: self.stopBGM()
        self.textWith()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.viaViewsExhibitStage()
        //: self.createTalkRoom()
        self.edition()
    }
}

// MARK: - ErrorCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension LightErrorDelegate: ErrorCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func nameData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(user_startValueKey.prefix(5)) + "as hung" + String(show_courseMsg.suffix(3))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(user_makePath.lowercased() + "elSex")].stringValue == ToClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(m_taskId.prefix(6)) + " hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.actionKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.vanishHolder()
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }

        //: popCurrentViewController()
        panoramicView()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func actualizeObject(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        listGift(logId: json[String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func variety(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(showTimeId.replacingOccurrences(of: "table", with: "e") + String(noti_errorContent.suffix(6)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(user_startValueKey.prefix(5)) + "as hung" + String(show_courseMsg.suffix(3))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(user_textId))].stringValue == ToClusterLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(m_taskId.prefix(6)) + " hung up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: main_elementArrayText.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(dream_timeValue.suffix(7)) + String(mainTotalTitle.prefix(3)) + String(main_itemTitleStr.suffix(6)) + "p the " + main_makeFormat.replacingOccurrences(of: "lab", with: "h")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.actionKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.vanishHolder()
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.largess()
        //: popCurrentViewController()
        panoramicView()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func honkyTonk(msg: String, data: [String: Any]) {
        //: guard ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(userAtIdent.lowercased())] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(notiViewPath))] as? String else { return }
            //: let httpsUrl = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = BarbConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: SucceedInfoReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            SucceedInfoReactiveCompatible.share.untilEqual(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            AlertInfoThen.nameConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(show_modelUrl) + mainHeadName.lowercased()).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(kLikeValue) + String(kBottomIdent)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                AlertInfoThen.showAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: SucceedInfoReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                SucceedInfoReactiveCompatible.share.transition(clickEvent: k_informationValue, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func fullback(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.labelMake(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func enableKey(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(mKeyUrl.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.snapShow(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.alongAnima(point: addBean)
        }
    }
}

// MARK: - CountelligenceObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension LightErrorDelegate: CountelligenceObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func clickData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(user_startValueKey.prefix(5)) + "as hung" + String(show_courseMsg.suffix(3))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(appButtonPath.replacingOccurrences(of: "pic", with: "s") + "eSex")].stringValue == ToClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(m_taskId.prefix(6)) + " hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.actionKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.vanishHolder()
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }

        //: popCurrentViewController()
        panoramicView()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func judge(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        listGift(logId: json[String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - SizeObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension LightErrorDelegate: SizeObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func ordinalColorObserver(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (user_playMainTitle.lowercased() + String(m_nameValue)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.actionKey(showMsg: (String(user_infoTextIdent.suffix(4)) + kSucceedPathMsg.lowercased()).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.vanishHolder()
                //: TalkingSocketManager.shared.currWorkVC = nil
                EffectSocketDelegate.shared.currWorkVC = nil
            }
            //: popCurrentViewController()
            panoramicView()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(dreamTitleValue.prefix(4)) + "ceptCa" + m_backgroundMsg.replacingOccurrences(of: "to", with: "ll")) && errorNo == 101 { // 接听通话时，余额不足
            //: guard ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue else { return }
            //: SucceedInfoReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            SucceedInfoReactiveCompatible.share.transition(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(dreamTitleValue.prefix(4)) + "ceptCa" + m_backgroundMsg.replacingOccurrences(of: "to", with: "ll")) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.actionKey(showMsg: msg)
            //: popCurrentViewController()
            panoramicView()
        }
    }
}

// MARK: - FileObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension LightErrorDelegate: FileObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func voiceShared() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (String(show_makeMsg.suffix(7)) + app_colorFormat.replacingOccurrences(of: "gesture", with: "ll")), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: userEmptyManagerUrl.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func atBackground() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.actionKey(showMsg: (String(show_fromUrl.prefix(5)) + " are ca" + String(dream_tempDomainCellId)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (m_pushPath.replacingOccurrences(of: "medium", with: "a") + "lCall"), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.actionKey(showMsg: (String(show_fromUrl.prefix(5)) + " are ca" + String(dream_tempDomainCellId)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (user_playMainTitle.lowercased() + String(m_nameValue)), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: userEmptyManagerUrl.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.actionKey(showMsg: (String(dream_frameUrl)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: appLineId.map{$0^30}, encoding: .utf8)!: (String(notiGiftMsg.suffix(8))), String(bytes: noti_viewMessage.map{pathEqual(manager: $0)}, encoding: .utf8)!: [String(bytes: dream_imageKey.map{powerTool(task: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        panoramicView()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func counteractionFront(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.objectModel(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func ofEvent(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.whenSkip(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.betweenPhotographicCamera(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.sole()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func doPath() {
        //: TalkingSocketManager.shared.currWorkVC = self
        EffectSocketDelegate.shared.currWorkVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = EqualValueView.listColor()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.takeTheFieldMale()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.resource()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.startScreen()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        panoramicView()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func reportSucceedPerspectiveQuantityeraction() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension LightErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func errorRequest() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.collectionView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func notList() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
