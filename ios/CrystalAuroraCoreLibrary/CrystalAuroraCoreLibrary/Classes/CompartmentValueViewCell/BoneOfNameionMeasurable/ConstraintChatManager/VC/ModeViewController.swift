
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let m_leadingText:[UInt8] = [0x40,0x47,0x40,0x5d,0x1,0x4a,0x46,0x4d,0x4c,0x5b,0x13,0x0,0x9,0x41,0x48,0x5a,0x9,0x47,0x46,0x5d,0x9,0x4b,0x4c,0x4c,0x47,0x9,0x40,0x44,0x59,0x45,0x4c,0x44,0x4c,0x47,0x5d,0x4c,0x4d]

private func layerSave(cell num: UInt8) -> UInt8 {
    return num ^ 41
}

/*: "uid" :*/
fileprivate let main_ratePath:[UInt8] = [0x41,0x5d,0x50]

private func skipTown(scale num: UInt8) -> UInt8 {
    return num ^ 52
}

/*: "headPic" :*/
fileprivate let mainDataContent:String = "to"
fileprivate let main_sizeText:[Character] = ["e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let k_transitionMsg:String = "communicationckname"

/*: "birthday" :*/
fileprivate let app_labelPath:[Character] = ["b","i","r","t","h","d"]
fileprivate let kModelPlayerText:String = "AY"

/*: "videoUrl" :*/
fileprivate let noti_rankStr:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let k_areaId:[Character] = ["l","o","c"]
fileprivate let mainAppIdent:String = "atioadd"

/*: "cmd" :*/
fileprivate let main_equalMessage:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let userLabelContent:[Character] = ["r","e","f","u","s","e","C","a","l"]
fileprivate let showPathUrl:String = "limit"

/*: "data" :*/
fileprivate let show_succeedData:[UInt8] = [0xb3,0xb6,0xa3,0xb6]

private func mSelf(aspect num: UInt8) -> UInt8 {
    return num ^ 215
}

/*: "Call interruption" :*/
fileprivate let mainPlayKey:String = "Call library video"
fileprivate let noti_contentManagerValue:String = "rrusep"

/*: "stopCall" :*/
fileprivate let show_targetUrl:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let mainBottomWindowStr:[UInt8] = [0x95,0x98,0x90,0x72,0x8d]

fileprivate func varMake(photo num: UInt8) -> UInt8 {
    let value = Int(num) + 215
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let main_showStr:String = "catalk"

/*: "No answer" :*/
fileprivate let mTitlePathModelUrl:String = "No if lab moment model"
fileprivate let kEqualAddData:String = "answelabel"

/*: "duration" :*/
fileprivate let appRefreshText:String = "duratiolike"

/*: "She has hung up" :*/
fileprivate let mainCurKey:String = "She hmodel model add user status"
fileprivate let mainImageId:[Character] = ["u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let dream_userPath:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let mScreenData:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let app_seekData:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let m_clickName:[Character] = ["s","t","o","p","S"]
fileprivate let notiViewTitle:[Character] = ["e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let kDateValue:[UInt8] = [0xcf,0xe8,0xf5,0xf3,0xe0,0xe0,0xef,0xe5,0xef,0xe3,0xe8,0xf2,0xa6,0xe1,0xe9,0xea,0xe2,0xa6,0xe5,0xe9,0xef,0xe8,0xf5,0xaa,0xa6,0xe5,0xe7,0xea,0xea,0xa6,0xee,0xe7,0xf5,0xa6,0xe3,0xe8,0xe2,0xe3,0xe2]

private func harvestMoon(result num: UInt8) -> UInt8 {
    return num ^ 134
}

/*: "The other hung up the phone" :*/
fileprivate let appHiddenName:String = "share else else mode varThe "
fileprivate let showSoundViewKey:String = "r hunred in equal size"
fileprivate let userErrorSourceEqualId:String = "image tothe "
fileprivate let m_fileName:String = "phonseat"

/*: "type" :*/
fileprivate let notiTextId:String = "tlengthpe"

/*: "jumpUrl" :*/
fileprivate let noti_emptyValue:String = "hidden"
fileprivate let dream_lastName:[Character] = ["u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let mainModelKey:[Character] = ["C","a","n","c","e"]
fileprivate let showEarnContent:[Character] = ["l"]

/*: "Get gold coins" :*/
fileprivate let appIconUrl:String = "Get gwrap icon"
fileprivate let appObjectIdent:[Character] = ["o","l","d"," ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let userModelMessage:String = "app load menu equal destroyaddBean"

/*: "refuseSex" :*/
fileprivate let mUserData:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let user_makeName:String = "color indexonAccept"
fileprivate let m_normalId:String = "Callcase model gift"

/*: "acceptCall" :*/
fileprivate let k_giftStr:String = "acceptCaneed self style pending make"
fileprivate let user_attentionFormat:[Character] = ["l","l"]

/*: "Calls are cancelled" :*/
fileprivate let main_needFormat:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c"]
fileprivate let mQuitPath:String = "ANCELLED"

/*: "The call has ended" :*/
fileprivate let k_countId:String = "true ok type length totalThe ca"
fileprivate let kFramePath:String = "feature tops ended"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ModeViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let k_domainData = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class ModeViewController: StateViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: LibraryChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PushPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: FileMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        EffectSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        EffectSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        EffectSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: m_leadingText.map{layerSave(cell: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! ToNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.tapAt()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            searchOnChildAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.lessImage()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        notOf()
        //: setupSubViewsConstraint()
        videoSex()
        //: initNotificationObserver()
        destroyObserver()
        //: req_callGetUserInfo()
        rank()
        //: timeoutRules()
        titleView()
        //: resumeTalk()
        exHusband()
        //: setUpBGMPlayer()
        plumpDown()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
            if ModeStageReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = PercipientThen(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isWork = false
        EffectSocketDelegate.shared.isWork = false
        //: self.stopVCAllActivties()
        self.brake()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: TotaleractionCounteractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = TotaleractionCounteractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: CreatePresentReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = CreatePresentReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension ModeViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func rank() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ConstraintChatManager.miniInstance(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: main_ratePath.map{skipTown(scale: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(mainDataContent.replacingOccurrences(of: "to", with: "h") + String(main_sizeText))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(k_transitionMsg.replacingOccurrences(of: "communication", with: "ni"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(app_labelPath) + kModelPlayerText.lowercased())].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(noti_rankStr))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(k_areaId) + mainAppIdent.replacingOccurrences(of: "add", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.gold()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension ModeViewController {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func searchOnChildAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        PounderReactiveCompatible.withBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.actionKey(showMsg: kAskValue)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (String(userLabelContent) + showPathUrl.replacingOccurrences(of: "limit", with: "l")), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: main_ratePath.map{skipTown(scale: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.messUp()
                //: return
                return
            }

            //: self.stopBGM()
            self.solarDay()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PushPlayListener.untilPage(type: .Response)
            //: self.startBGM()
            self.tapAt()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func brake() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.solarDay()
        //: TalkingSocketManager.shared.callerDelegate = nil
        EffectSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        EffectSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        EffectSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func messUp(animated: Bool = true) {
        //: self.stopBGM()
        self.solarDay()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func dogmatize() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.beginId(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func exHusband() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            dogmatize()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension ModeViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func destroyObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(matchOff(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(timeNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kConnectValue,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        k_domainData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func matchOff(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            messUp()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func timeNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard EffectSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        EffectSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        EffectSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        EffectSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.adLive(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension ModeViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.actionKey(showMsg: (String(mainPlayKey.prefix(5)) + "inte" + noti_contentManagerValue.replacingOccurrences(of: "use", with: "u") + "tion").localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (String(show_targetUrl)), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (main_showStr.replacingOccurrences(of: "talk", with: "nc") + "elCall"), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (String(userLabelContent) + showPathUrl.replacingOccurrences(of: "limit", with: "l")), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: main_ratePath.map{skipTown(scale: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.vanishHolder()
                //: TalkingSocketManager.shared.currWorkVC = nil
                EffectSocketDelegate.shared.currWorkVC = nil
            }
            //: popCurrentViewController()
            messUp()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.observeAlter(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension ModeViewController {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func plumpDown() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DetailSignedNumeric.Call : DetailSignedNumeric.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PushPlayListener.untilPage(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func tapAt() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.behindMenu()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func solarDay() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.dominoStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension ModeViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func titleView() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kDomainMarginContent : self.chatModel?.popularAnswer()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            kindTime()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.kindTime()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func kindTime() {
        //: guard !TalkingSocketManager.shared.isWork else { return }
        guard !EffectSocketDelegate.shared.isWork else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.actionKey(showMsg: (String(mTitlePathModelUrl.prefix(3)) + kEqualAddData.replacingOccurrences(of: "label", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (main_showStr.replacingOccurrences(of: "talk", with: "nc") + "elCall"), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (String(userLabelContent) + showPathUrl.replacingOccurrences(of: "limit", with: "l")), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: main_ratePath.map{skipTown(scale: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        EffectSocketDelegate.shared.crosscutNameReason(info: socketDict)
        //: popCurrentViewController()
        messUp()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension ModeViewController {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func valueDataFormatting(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(appRefreshText.replacingOccurrences(of: "like", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.correlationTable(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.betweenArea()
            //: createTalkRoom()
            dogmatize()
            //: self.stopBGM()
            self.solarDay()
        }
    }
}

// MARK: - ErrorCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension ModeViewController: ErrorCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func nameData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(mainCurKey.prefix(5)) + "as h" + String(mainImageId)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(dream_userPath))].stringValue == ToClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mScreenData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.actionKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.vanishHolder()
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }

        //: popCurrentViewController()
        messUp()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func actualizeObject(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.solarDay()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.betweenArea()
        //: self.createTalkRoom()
        self.dogmatize()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func variety(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(app_seekData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(mainCurKey.prefix(5)) + "as h" + String(mainImageId)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(m_clickName) + String(notiViewTitle))].stringValue == ToClusterLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(mScreenData)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: kDateValue.map{harvestMoon(result: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(appHiddenName.suffix(4)) + "othe" + String(showSoundViewKey.prefix(5)) + "g up " + String(userErrorSourceEqualId.suffix(4)) + m_fileName.replacingOccurrences(of: "seat", with: "e")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.actionKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.vanishHolder()
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.invitationVideo()
        //: self.popCurrentViewController()
        self.messUp()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kViewData, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func honkyTonk(msg: String, data: [String: Any]) {
        //: guard ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(notiTextId.replacingOccurrences(of: "length", with: "y"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(noti_emptyValue.replacingOccurrences(of: "hidden", with: "j") + String(dream_lastName))] as? String else { return }
            //: let httpsUrl = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = BarbConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: SucceedInfoReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            SucceedInfoReactiveCompatible.share.untilEqual(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            AlertInfoThen.nameConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(mainModelKey) + String(showEarnContent)).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(appIconUrl.prefix(5)) + String(appObjectIdent)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                AlertInfoThen.showAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: SucceedInfoReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                SucceedInfoReactiveCompatible.share.transition(clickEvent: k_informationValue, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func enableKey(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(userModelMessage.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.candid(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.standIn(point: addBean)
        }
    }
}

// MARK: - CountelligenceObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ModeViewController: CountelligenceObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func clickData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(mainCurKey.prefix(5)) + "as h" + String(mainImageId)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(mUserData))].stringValue == ToClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mScreenData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.actionKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.vanishHolder()
            //: TalkingSocketManager.shared.currWorkVC = nil
            EffectSocketDelegate.shared.currWorkVC = nil
        }
        //: popCurrentViewController()
        messUp()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func judge(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.solarDay()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.betweenArea()
        //: self.createTalkRoom()
        self.dogmatize()
    }
}

// MARK: - SizeObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension ModeViewController: SizeObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func ordinalColorObserver(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(userLabelContent) + showPathUrl.replacingOccurrences(of: "limit", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.actionKey(showMsg: (String(mTitlePathModelUrl.prefix(3)) + kEqualAddData.replacingOccurrences(of: "label", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.vanishHolder()
                //: TalkingSocketManager.shared.currWorkVC = nil
                EffectSocketDelegate.shared.currWorkVC = nil
            }
            //: self.popCurrentViewController()
            self.messUp()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(user_makeName.suffix(8)) + String(m_normalId.prefix(4))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue else { return }
            //: SucceedInfoReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            SucceedInfoReactiveCompatible.share.transition(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(user_makeName.suffix(8)) + String(m_normalId.prefix(4))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.actionKey(showMsg: msg)
            //: self.popCurrentViewController()
            self.messUp()
        }
    }
}

// MARK: - TaskCloudViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension ModeViewController: TaskCloudViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func revoke() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (String(k_giftStr.prefix(8)) + String(user_attentionFormat)), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: main_ratePath.map{skipTown(scale: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func currentOf() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.actionKey(showMsg: (String(main_needFormat) + mQuitPath.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (main_showStr.replacingOccurrences(of: "talk", with: "nc") + "elCall"), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.actionKey(showMsg: (String(main_needFormat) + mQuitPath.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (String(userLabelContent) + showPathUrl.replacingOccurrences(of: "limit", with: "l")), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: main_ratePath.map{skipTown(scale: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.actionKey(showMsg: (String(k_countId.suffix(6)) + "ll ha" + String(kFramePath.suffix(7))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            EffectSocketDelegate.shared.crosscutNameReason(info: [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: (String(show_targetUrl)), String(bytes: show_succeedData.map{mSelf(aspect: $0)}, encoding: .utf8)!: [String(bytes: mainBottomWindowStr.map{varMake(photo: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.invitationVideo()
        //: self.popCurrentViewController()
        self.messUp()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func shouldMute(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.reposeBlockEqual(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func keyTitle(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.shouldHandsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func currentBag() {
        //: TalkingSocketManager.shared.currWorkVC = self
        EffectSocketDelegate.shared.currWorkVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = FileMiniView.viewMessage()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.startScreen()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.attainUpwardsSubExample()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.messageModify(talkingTime)
        //: popCurrentViewController()
        messUp()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func process(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.messageModify(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func televisionArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.tableDHote(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension ModeViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func notOf() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func videoSex() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
