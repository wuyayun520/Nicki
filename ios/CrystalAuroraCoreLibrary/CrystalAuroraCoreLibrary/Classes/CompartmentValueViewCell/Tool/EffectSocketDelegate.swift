
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let dreamGestureMakeTitle:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let notiMessageName:[UInt8] = [0x94,0x88,0x84,0x8c,0x82,0x93,0x98,0x91,0x8e,0x83,0x82,0x88,0x84,0x8f,0x86,0x93,0x98,0x95,0x82,0x96,0x92,0x82,0x94,0x93,0x84,0x86,0x8b,0x8b,0x98,0x89,0x88,0x93,0x8e,0x81,0x8e,0x84,0x86,0x93,0x8e,0x88,0x89]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let notiResponseName:[UInt8] = [0x6d,0x7a,0x7c,0x76,0x69,0x7a,0x60,0x79,0x73,0x70,0x7e,0x6b,0x6c,0x7c,0x6d,0x7a,0x7a,0x71,0x60,0x71,0x70,0x6b,0x76,0x79,0x76,0x7c,0x7e,0x6b,0x76,0x70,0x71]

private func bottomInfo(object num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "isCalling" :*/
fileprivate let user_reportStr:String = "isCalwith frame"
 
/*: "SOCKET 连接成功." :*/
fileprivate let dreamTopText:[Character] = ["S","O","C","K","E","T"," ","连","接","成","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let dreamManagerUrl:String = "clear bottom need for shareSOCKET 连"
fileprivate let userTitleUrl:[Character] = ["接","\u{5931}","败",","," ","e","r","r","o","r",":"]

/*: ." :*/
fileprivate let mLeadingCornerData:String = "icon"

/*: "SOCKET 失败 :*/
fileprivate let user_sizeCenterStr:[Character] = ["S","O","C","K","E","T"," ","失"]
fileprivate let appPlayMsg:String = "败"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let show_transformToLoadPath:String = "\u{6b21}, 切换\u{8fc7}"
fileprivate let mainTableLayerReturnUrl:String = "let attribute view view contentSock"
fileprivate let show_titleStr:String = "\u{91cd}新尝试"

/*: "SOCKET 已关闭, code: :*/
fileprivate let m_countTitle:String = "adjustCKE"
fileprivate let main_ofStr:[Character] = [" ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let appLocationMessage:String = ", reato view return"
fileprivate let k_toolKey:String = "translation transform to block elseson:"

/*: "ws:// :*/
fileprivate let m_moreFirstModeData:String = "manager start var control levelws://"

/*: /?sid= :*/
fileprivate let mainCurrentPath:[Character] = ["/","?","s","i","d","="]

/*: &uid= :*/
fileprivate let notiEqualPath:String = "model true social index by&uid="

/*: &platform=iphone&version= :*/
fileprivate let noti_statusTicketForStr:[Character] = ["&","p","l","a","t","f"]
fileprivate let show_touchStr:String = "float to extensionorm=ip"
fileprivate let dreamMenuId:String = "&version=user in add next type"

/*: &packageId= :*/
fileprivate let notiBagData:[Character] = ["&","p","a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let userModelKey:[Character] = ["1","v","1","s","e"]
fileprivate let dream_numberPath:String = "make viewrver."

/*: .com:7766" :*/
fileprivate let dreamCellBottomContent:String = ".com:7data add tag"
fileprivate let dreamConstraintMsg:String = "touch66"

/*: "43.129.185.194:7767" :*/
fileprivate let dream_effectFormat:[Character] = ["4","3",".","1","2","9"]
fileprivate let mCaptureIdent:String = ".18transaction.1"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let showClickId:[UInt8] = [0x2e,0xad,0x97,0xe9,0xb3,0x85,0xe5,0x91,0xbd,0xe7,0xad,0x96,0xe6,0x8c,0xbc,0xef,0xac,0x90,0xe5,0x91,0x9b,0xe7,0x64,0x65,0x67,0x6e,0x61,0x68,0x43,0x79,0x74,0x69,0x6c,0x69,0x62,0x61,0x68,0x63,0x61,0x65,0x72,0x9c,0xbb,0xe7,0x91,0xbd,0xe7,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "cmd" :*/
fileprivate let m_alwaysUpgradeVarMessage:String = "CMD"

/*: "ping" :*/
fileprivate let dreamArrayKey:String = "PING"

/*: "callHeartbeat" :*/
fileprivate let kExtentKey:[Character] = ["c","a","l","l","H","e","a"]
fileprivate let showAtText:String = "rwhitebeawhite"

/*: "recievedDate" :*/
fileprivate let m_appealMessage:String = "RECIEVE"
fileprivate let dream_dataText:String = "rate view point layer centerdDate"

/*: "liveHeartBeat" :*/
fileprivate let mainInfoMessage:[Character] = ["l","i","v","e","H","e","a"]
fileprivate let app_centerName:[Character] = ["r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let show_viewColorValueKey:String = "var content hidden🔗 关闭 s"
fileprivate let showConstraintFormat:String = "ocketo"

/*: "The user logs out" :*/
fileprivate let app_transformRequestUrl:String = "The uspath shadow record view var"
fileprivate let user_infoMessage:String = "osize"

/*: "SOCKET  :*/
fileprivate let m_pathStr:[Character] = ["S","O","C","K","E"]
fileprivate let showSendFieldText:[Character] = ["T"," "]

/*: 重连. 重试次数： :*/
fileprivate let show_aboutName:[Character] = ["重","连","."," ","重","试","次","\u{6570}","\u{ff1a}"]

/*: , 地址： :*/
fileprivate let show_weightName:String = ", 地址："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let dreamSectionMessage:String = "SOCKtalkT"
fileprivate let m_lockId:String = "\u{5e38} 不需\u{8981}重\u{65b0}连"
fileprivate let noti_thirdName:[Character] = ["接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let show_appStr:String = "SOCmakeET"
fileprivate let noti_myText:String = " 不需要"

/*: "&resume=1" :*/
fileprivate let userBottomPath:String = "view as view return&res"

/*: "&lang= :*/
fileprivate let userIndexUrl:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let mTextMessage:[Character] = ["S","O","C","K","E","T"," ","开","始","连","接",","," "]
fileprivate let show_shareBlockPath:String = "地\u{5740}："

/*: "onNetDisabled" :*/
fileprivate let user_kitMsg:String = "onNesize third data"
fileprivate let main_sharedMsg:String = "lapp"

/*: "errno" :*/
fileprivate let user_playerMsg:String = "errshowo"

/*: "data" :*/
fileprivate let userCompletePartyIdent:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let kFilterData:String = "insideCKE"
fileprivate let noti_pickTextStr:String = "from start活cmd: p"
fileprivate let showImageData:String = "ing.file total data"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let user_leadingUrl:[UInt8] = [0x3d,0x21,0x2d,0x25,0x2b,0x3a,0x4e,0x1a,0x1c,0x1a,0xd,0x8a,0xd1,0xf3,0x88,0xda,0xd5,0xd,0x3,0xa,0x54,0x4e,0xd,0xf,0x2,0x2,0x26,0xb,0xf,0x1c,0x1a,0xc,0xb,0xf,0x1a,0x40]

private func cityCentre(head num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let user_lastMessage:[UInt8] = [0x1f,0x3,0xf,0x7,0x9,0x18,0x6c,0x20,0x25,0x3a,0x29,0xa8,0xf3,0xd1,0xaa,0xf8,0xf7,0x2f,0x21,0x28,0x76,0x6c,0x20,0x25,0x3a,0x29,0x4,0x29,0x2d,0x3e,0x38,0xe,0x29,0x2d,0x38,0x62]

private func textRecord(player num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "onP" :*/
fileprivate let app_indexIdent:String = "onPvar model try mode"

/*: "msg" :*/
fileprivate let k_sizeIdent:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let noti_mineValue:String = "\u{0001f517} \u{5904}理\u{6d88}息："
fileprivate let user_arrayTitle:String = "format casecmdName:"

/*: , errNo: :*/
fileprivate let mainShouldMsg:[Character] = [","," ","e","r","r","N","o",":"]

/*: , msg: :*/
fileprivate let user_toPath:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let k_statusUrl:String = "view action guard, data:"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let show_acceptTitle:[Character] = ["S","O","C","K","E","T"," ","接","收"]
fileprivate let appInviteModelUrl:[Character] = ["异","常"]
fileprivate let m_randomMessage:String = "消息：cmdlet false feature view"

/*: "onReceivedGift" :*/
fileprivate let mBeautyPath:String = "path touch to message timeonRe"
fileprivate let notiGestureIdent:String = "dGiftright range for right"

/*: "onVideoMatch" :*/
fileprivate let kRecordText:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let mainCoverId:String = "onRequemanager gift var string server"
fileprivate let show_managerUrl:String = "add log kind letstCall"

/*: "onCancelCall" :*/
fileprivate let main_appText:String = "onCantext height class in main"
fileprivate let k_stateMessage:String = "celCallsize view"

/*: "onRefuseCall" :*/
fileprivate let appStartPath:String = "self main letonRefu"

/*: "onAcceptCall" :*/
fileprivate let dream_dataName:[Character] = ["o","n","A","c","c","e"]
fileprivate let m_userScaleMsg:String = "cell post message back titleptCall"

/*: "onStopCall" :*/
fileprivate let showFrameMsg:[Character] = ["o","n"]
fileprivate let kTableName:String = "size type cellStop"

/*: "freeCallTimes" :*/
fileprivate let main_layerKey:[Character] = ["f","r","e","e","C","a","l","l","T","i"]
fileprivate let showDisplayValue:[Character] = ["m","e","s"]

/*: "mfCoin" :*/
fileprivate let mChangeStr:String = "mfCoinlabel self else"

/*: "onMissCoin" :*/
fileprivate let user_modelScreenData:String = "return corner shared selfonMiss"

/*: "acceptCall" :*/
fileprivate let appListData:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "stopCall" :*/
fileprivate let app_coverIconLimitTitle:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let dreamThenOfSystemName:String = "REQU"
fileprivate let mMakeData:String = "ashowshow"

/*: "[Invite you to a video call...]" :*/
fileprivate let user_shId:[UInt8] = [0xd6,0xc4,0xe9,0xf1,0xe4,0xef,0xe0,0x9b,0xf4,0xea,0xf0,0x9b,0xef,0xea,0x9b,0xdc,0x9b,0xf1,0xe4,0xdf,0xe0,0xea,0x9b,0xde,0xdc,0xe7,0xe7,0xa9,0xa9,0xa9,0xd8]

fileprivate func effectHidden(center num: UInt8) -> UInt8 {
    let value = Int(num) - 123
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let mainFaceFormat:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let main_visibleUpMsg:[UInt8] = [0x7f,0x6d,0x4a,0x52,0x4d,0x50,0x41,0x4,0x5d,0x4b,0x51,0x4,0x50,0x4b,0x4,0x45,0x4,0x52,0x4b,0x4d,0x47,0x41,0x4,0x47,0x45,0x48,0x48,0xa,0xa,0xa,0x79]

/*: "uid" :*/
fileprivate let mainBlockTitle:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let show_formKey:String = "nicknvalueme"

/*: "headPic" :*/
fileprivate let kAddFormat:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let k_infoMsg:[Character] = ["o","n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let k_toData:String = "methodanmethod"

/*: "refuseCall" :*/
fileprivate let main_appStr:String = "refuseCaladd time start make"
fileprivate let k_giftUserMsg:String = "L"

/*: "onCallMotivation" :*/
fileprivate let main_equalId:[Character] = ["o","n","C","a","l","l","M","o","t","i","v"]
fileprivate let show_regularText:String = "atimanager"

/*: "onLiveWarning" :*/
fileprivate let m_maxFormat:String = "opoint"
fileprivate let userWaitStr:String = "if suite to gift keyLiveWa"

/*: "reason" :*/
fileprivate let m_layerMsg:String = "topeason"

/*: "time" :*/
fileprivate let kPathMsg:String = "tisizee"

/*: "onStopLive" :*/
fileprivate let appLimitValue:String = "to"
fileprivate let app_sectionValue:[Character] = ["n","S","t","o","p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let noti_successValue:String = "model you write to leftonSend"
fileprivate let kHalfTitle:String = "model equal in modeGift"

/*: "onGameFs" :*/
fileprivate let noti_cornerTitle:[Character] = ["o","n","G","a","m","e","F"]
fileprivate let dreamSendData:[Character] = ["s"]

/*: "broadStatus" :*/
fileprivate let appIndicatorId:String = "equal attribute tool class countbroadS"
fileprivate let showAppCenterFormat:[Character] = ["t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let appColorData:[Character] = ["o","n","C","a","l","l"]
fileprivate let main_cellAwakeMsg:String = "height show inCh"
fileprivate let dreamFileWaitData:String = "video enter status viewatMsg"

/*: "onNotificationPopupWin" :*/
fileprivate let notiViewReplyData:String = "position downonNot"
fileprivate let main_addData:String = "modeo"
fileprivate let mTheId:String = "nPopupWinview user"

/*: "onUserBlock" :*/
fileprivate let mLabelId:[Character] = ["o","n","U","s","e"]
fileprivate let mainModelData:String = "index source betweenrBlock"

/*: "freeCallWin" :*/
fileprivate let dreamSeekImageValue:String = "fhalfee"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EffectSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let k_groupValue = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let k_statusMaxData = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let k_noContent = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kDomainValue = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kClickScreenValue = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let k_serverInfoValue = (String(dreamGestureMakeTitle))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let k_bottomContent = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let k_dailyText = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let k_saveQuantityData = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let k_pointText = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let k_infoTitle = NSNotification.Name(rawValue: String(bytes: notiMessageName.map{$0^199}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let kTransformName = NSNotification.Name(rawValue: String(bytes: notiResponseName.map{bottomInfo(object: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol SizeObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func ordinalColorObserver(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol DetailMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func somebody(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func rename(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ErrorCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func actualizeObject(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func variety(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func nameData(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func honkyTonk(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol CountelligenceObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func clickData(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func judge(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func enableKey(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class EffectSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum TopicType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currWorkVC: AnyObject?
    var currWorkVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ManagerThen.shared.direction == .rightToLeft ? 0 : kBlockRateValue,
        //: y: StatusBarHeight+190
        y: kPositionName + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ManagerThen.shared.direction == .rightToLeft ? 0 : kBlockRateValue,
        //: y: StatusBarHeight+190
        y: kPositionName + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ManagerThen.shared.direction == .rightToLeft ? 0 : kBlockRateValue,
        //: y: StatusBarHeight+500
        y: kPositionName + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: SizeObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: DetailMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ErrorCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: CountelligenceObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    @objc dynamic var isWork = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = EffectSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(smallOf(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(addWebsocket),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(user_reportStr.prefix(5)) + "ling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (AtInfoApplication.shared as! AtInfoApplication).timer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isWork")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (AtInfoApplication.shared as! AtInfoApplication).timer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension EffectSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        ReadReactiveCompatible.equalOn(msg: (String(dreamTopText)))
        //: sendPing()
        gap()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kConnectValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: k_serverInfoValue)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        firstView()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        ReadReactiveCompatible.equalOn(msg: (String(dreamManagerUrl.suffix(8)) + String(userTitleUrl)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = ModeStageReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = ModeStageReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kDomainValue {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = rowAcross(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            ReadReactiveCompatible.equalOn(msg: (String(user_sizeCenterStr) + appPlayMsg.capitalized) + "\(kDomainValue)" + (show_transformToLoadPath + String(mainTableLayerReturnUrl.suffix(4)) + "et域\u{540d}" + show_titleStr + "，地\u{5740}：") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.executeItem(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.ofMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        ReadReactiveCompatible.equalOn(msg: (m_countTitle.replacingOccurrences(of: "adjust", with: "SO") + "T 已关闭," + String(main_ofStr)) + "\(code)" + (String(appLocationMessage.prefix(5)) + String(k_toolKey.suffix(4))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.falseNameOut(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == k_pointText {
            //: return
            return
        }
        //: if let status = ModeStageReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = ModeStageReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.executeItem(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func rowAcross(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = ModeStageReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = ModeStageReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = isVoice()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: k_serverInfoValue)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = ModeStageReactiveCompatible.share.loginUid
        let uid = ModeStageReactiveCompatible.share.loginUid
        //: let sid = ModeStageReactiveCompatible.share.loginSessionId
        let sid = ModeStageReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(mainCurrentPath)) + "\(sid)" + (String(notiEqualPath.suffix(5))) + "\(uid)" + (String(noti_statusTicketForStr) + String(show_touchStr.suffix(6)) + "hone" + String(dreamMenuId.prefix(9))) + "\(k_screenValue)" + (String(notiBagData)) + "\(k_messageFlushValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func isVoice() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kAppValue ? [(String(userModelKey) + String(dream_numberPath.suffix(5))) + "\(k_screenRecordData)" + (String(dreamCellBottomContent.prefix(6)) + dreamConstraintMsg.replacingOccurrences(of: "touch", with: "7"))] : [(String(dream_effectFormat) + mCaptureIdent.replacingOccurrences(of: "transaction", with: "5") + "94:7767")]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension EffectSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func smallOf(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            ReadReactiveCompatible.equalOn(msg: String(bytes: showClickId.reversed(), encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.falseNameOut(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.executeItem(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func addWebsocket() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.executeItem(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.canMetadata()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension EffectSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func gap() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: k_groupValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.crosscutNameReason(info: [(m_alwaysUpgradeVarMessage.lowercased()): (dreamArrayKey.lowercased())])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func seem() {
        //: endTalkingPing()
        springtime()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: k_statusMaxData, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isWork || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currWorkVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currWorkVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isWork || (self.startScreen()?.isKind(of: LightErrorDelegate.self) == false && self.startScreen()?.isKind(of: ModeViewController.self) == false) && (self.currWorkVC?.isKind(of: LightErrorDelegate.self) == false && self.currWorkVC?.isKind(of: ModeViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.crosscutNameReason(info: [(m_alwaysUpgradeVarMessage.lowercased()): (String(kExtentKey) + showAtText.replacingOccurrences(of: "white", with: "t"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func springtime() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func canMetadata() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(m_appealMessage.lowercased() + String(dream_dataText.suffix(5)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < k_tableText {
                //: ModeStageReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                ModeStageReactiveCompatible.share.bindInfoName(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension EffectSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func betweenUser() {
        //: endLivePing()
        succeed()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: k_noContent, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.crosscutNameReason(info: [(m_alwaysUpgradeVarMessage.lowercased()): (String(mainInfoMessage) + String(app_centerName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func succeed() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension EffectSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func firstView() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = StageDeviceView()
        //: let keyWindow = MacroReactiveCompatible.getWindow()
        let keyWindow = MacroReactiveCompatible.colorForDestroy()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isWork
        self.containerView?.isHidden = !self.isWork
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func miniModel() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard LengthLiveManager.loadShared().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard SizeEventHandler.frameShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension EffectSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func falseNameOut(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(show_viewColorValueKey.suffix(6)) + showConstraintFormat.replacingOccurrences(of: "to", with: "t")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: k_pointText, reason: (String(app_transformRequestUrl.prefix(6)) + "er logs " + user_infoMessage.replacingOccurrences(of: "size", with: "ut")))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        springtime()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            miniModel()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func itemAfter() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = EffectSocketDelegate.shared.rowAcross(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.executeItem(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func executeItem(timeInterval: TimeInterval, type: TopicType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            ReadReactiveCompatible.equalOn(msg: (String(m_pathStr) + String(showSendFieldText)) + "\(type.rawValue)" + (String(show_aboutName)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.active(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func active(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                ReadReactiveCompatible.equalOn(msg: (dreamSectionMessage.replacingOccurrences(of: "talk", with: "E") + " 连\u{63a5}\u{6b63}" + m_lockId + String(noti_thirdName)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                ReadReactiveCompatible.equalOn(msg: (show_appStr.replacingOccurrences(of: "make", with: "K") + " \u{6b63}在连接" + noti_myText.capitalized + "重新连接."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isWork {
        if self.isWork {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(userBottomPath.suffix(4)) + "ume=1"))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(userIndexUrl)) + "\(NorAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        falseNameOut(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kClickScreenValue
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        ReadReactiveCompatible.equalOn(msg: (String(mTextMessage) + show_shareBlockPath) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func crosscutNameReason(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if ModeStageReactiveCompatible.share.reachability?.connection == nil || ModeStageReactiveCompatible.share.reachability?.connection == .unavailable {
        if ModeStageReactiveCompatible.share.reachability?.connection == nil || ModeStageReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(m_alwaysUpgradeVarMessage.lowercased()): (String(user_kitMsg.prefix(4)) + "tDisab" + main_sharedMsg.replacingOccurrences(of: "app", with: "ed")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (user_playerMsg.replacingOccurrences(of: "show", with: "n")): "\(k_saveQuantityData)",
                                       //: "data": [:]]
                                       (String(userCompletePartyIdent)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            ofMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(m_alwaysUpgradeVarMessage.lowercased())] as? String == (dreamArrayKey.lowercased()) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                ReadReactiveCompatible.equalOn(msg: (kFilterData.replacingOccurrences(of: "inside", with: "SO") + "T 连接保" + String(noti_pickTextStr.suffix(7)) + String(showImageData.prefix(4))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(m_alwaysUpgradeVarMessage.lowercased())] as? String == (String(kExtentKey) + showAtText.replacingOccurrences(of: "white", with: "t")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                ReadReactiveCompatible.equalOn(msg: String(bytes: user_leadingUrl.map{cityCentre(head: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(m_alwaysUpgradeVarMessage.lowercased())] as? String == (String(mainInfoMessage) + String(app_centerName)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                ReadReactiveCompatible.equalOn(msg: String(bytes: user_lastMessage.map{textRecord(player: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(m_alwaysUpgradeVarMessage.lowercased())] as? String == (dreamArrayKey.lowercased()) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(m_alwaysUpgradeVarMessage.lowercased()): (String(app_indexIdent.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (user_playerMsg.replacingOccurrences(of: "show", with: "n")): "\(k_bottomContent)",
                                           //: "data": [:]]
                                           (String(userCompletePartyIdent)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                ofMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.executeItem(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func ofMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(m_alwaysUpgradeVarMessage.lowercased())].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(user_playerMsg.replacingOccurrences(of: "show", with: "n"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(k_sizeIdent))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(userCompletePartyIdent))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (noti_mineValue + String(user_arrayTitle.suffix(8))) + "\(cmdName)" + (String(mainShouldMsg)) + "\(errorNo)" + (String(user_toPath)) + "\(msg)" + (String(k_statusUrl.suffix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        colorNeighborAdd(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            ReadReactiveCompatible.equalOn(msg: (String(show_acceptTitle) + String(appInviteModelUrl) + String(m_randomMessage.prefix(6)) + "Name:") + "\(cmdName)" + (String(mainShouldMsg)) + "\(errorNo)" + (String(user_toPath)) + "\(msg)" + (String(k_statusUrl.suffix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !ModeStageReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !ModeStageReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                DeviceReactiveCompatible.shared.sumry()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.ordinalColorObserver(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(mBeautyPath.suffix(4)) + "ceive" + String(notiGestureIdent.prefix(5))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kLikeName, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(kRecordText)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.somebody(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(mainCoverId.prefix(7)) + String(show_managerUrl.suffix(6))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.rename(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(main_appText.prefix(5)) + String(k_stateMessage.prefix(7))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(appStartPath.suffix(6)) + "seCall") { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(dream_dataName) + String(m_userScaleMsg.suffix(6))) { // 接收方发送acceptCall回执
            //: self.isWork = true
            self.isWork = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            seem()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.judge(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(showFrameMsg) + String(kTableName.suffix(4)) + "Call") { // 接收方发送stopCall回执
            //: self.isWork = false
            self.isWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            springtime()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(main_layerKey) + String(showDisplayValue))] as? Int {
                //: ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: ModeStageReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            ModeStageReactiveCompatible.share.loginUserMode.mf_coin = json[(String(userCompletePartyIdent))][(String(mChangeStr.prefix(6)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kViewData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (AtInfoApplication.shared as! AtInfoApplication).contentEqual()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(user_modelScreenData.suffix(6)) + "Coin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.honkyTonk(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(appListData)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isWork = true
            self.isWork = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            seem()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.actualizeObject(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(app_coverIconLimitTitle)) { // 接收方/拨打方 挂断了通话
            //: self.isWork = false
            self.isWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            springtime()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.variety(data: data)
            // 更新余额
            //: ModeStageReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            ModeStageReactiveCompatible.share.loginUserMode.mf_coin = json[(String(userCompletePartyIdent))][(String(mChangeStr.prefix(6)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            quantityerval(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kViewData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (AtInfoApplication.shared as! AtInfoApplication).contentEqual()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (dreamThenOfSystemName.lowercased() + "estC" + mMakeData.replacingOccurrences(of: "show", with: "l")) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: k_infoTitle, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: ModeStageReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                ModeStageReactiveCompatible.share.bindInfoName(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(m_appealMessage.lowercased() + String(dream_dataText.suffix(5)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: user_shId.map{effectHidden(center: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(userCompletePartyIdent))][(String(mainFaceFormat))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: main_visibleUpMsg.map{$0^36}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                SourceReactiveCompatible.nameUrl(uid: String(json[(String(userCompletePartyIdent))][(String(mainBlockTitle))].intValue),
                                          //: title: data["nickname"] as? String,
                                          title: data[(show_formKey.replacingOccurrences(of: "value", with: "a"))] as? String,
                                          //: body: body,
                                          body: body,
                                          //: imageUrl: data["headPic"] as? String)
                                          imageUrl: data[(String(kAddFormat))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                SourceReactiveCompatible.shared.colorful()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(k_infoMsg)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isWork = true
            self.isWork = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            seem()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(k_infoMsg))] = "1"
            //: ModeStageReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            ModeStageReactiveCompatible.share.theWhoCell(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (AtInfoApplication.shared as! AtInfoApplication).timer()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (k_toData.replacingOccurrences(of: "method", with: "c") + "elCall") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isWork = false
            self.isWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            springtime()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.nameData(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                SourceReactiveCompatible.takeObject(identifier: String(json[(String(userCompletePartyIdent))][(String(mainBlockTitle))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                SourceReactiveCompatible.shared.part()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(main_appStr.prefix(9)) + k_giftUserMsg.lowercased()) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isWork = false
            self.isWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            springtime()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.clickData(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(main_equalId) + show_regularText.replacingOccurrences(of: "manager", with: "on")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.enableKey?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (m_maxFormat.replacingOccurrences(of: "point", with: "n") + String(userWaitStr.suffix(6)) + "rning") { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            LengthLiveManager.overColor(content: msg, reason: json[(String(userCompletePartyIdent))][(m_layerMsg.replacingOccurrences(of: "top", with: "r"))].stringValue, time: json[(String(userCompletePartyIdent))][(kPathMsg.replacingOccurrences(of: "size", with: "m"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (appLimitValue.replacingOccurrences(of: "to", with: "o") + String(app_sectionValue)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LengthLiveManager.loadShared().failureResource()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(noti_successValue.suffix(6)) + "Program" + String(kHalfTitle.suffix(4))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.logArea(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(noti_cornerTitle) + String(dreamSendData)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(appIndicatorId.suffix(6)) + String(showAppCenterFormat))] as? Int ?? -1
            //: if ModeStageReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if ModeStageReactiveCompatible.share.appStatus == OfSum.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kTransformName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kLikeName, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kTransformName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(appColorData) + String(main_cellAwakeMsg.suffix(2)) + String(dreamFileWaitData.suffix(5))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            RestraintThen.clickDesign().smallBeer(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(notiViewReplyData.suffix(5)) + "ifica" + main_addData.replacingOccurrences(of: "mode", with: "ti") + String(mTheId.prefix(9))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            DeviceReactiveCompatible.shared.nextDic(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(mLabelId) + String(mainModelData.suffix(6))) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LengthLiveManager.loadShared().failureResource()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            SizeEventHandler.frameShared().priceTagAdd()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: k_dismissData, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension EffectSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func colorNeighborAdd(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (dreamThenOfSystemName.lowercased() + "estC" + mMakeData.replacingOccurrences(of: "show", with: "l")) || cmdName == (String(mainCoverId.prefix(7)) + String(show_managerUrl.suffix(6))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if LengthLiveManager.loadShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                k_likeData = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                LengthLiveManager.modeClick(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                LengthLiveManager.loadShared().failureResource()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard k_likeData == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (k_toData.replacingOccurrences(of: "method", with: "c") + "elCall") || cmdName == (String(main_appText.prefix(5)) + String(k_stateMessage.prefix(7))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(main_appStr.prefix(9)) + k_giftUserMsg.lowercased()) || cmdName == (String(appStartPath.suffix(6)) + "seCall") ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(app_coverIconLimitTitle)) || cmdName == (String(showFrameMsg) + String(kTableName.suffix(4)) + "Call")
        {
            //: NEED_RESTORE_LIVE = false
            k_likeData = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = CloudThen(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                LengthLiveManager.loadShared().commentAdd()
            }
            //: alert.show()
            alert.generate()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func quantityerval(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(main_layerKey) + String(showDisplayValue))] as? Int {
            //: ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            ModeStageReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(dreamSeekImageValue.replacingOccurrences(of: "half", with: "r") + "CallWin")] as? Bool
        //: if freeCallWin == true && ModeStageReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, ModeStageReactiveCompatible.share.loginUserMode.sex == ToClusterLiteral.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ItemErrorDelegate()
            //: view.show()
            view.appShow()
            //: view.setShowData(data: data)
            view.buttonData(data: data)
        }
    }
}
