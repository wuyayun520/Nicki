
//: Declare String Begin

/*: "http://" :*/
fileprivate let dream_cellData:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let mFileId:String = "hminip"

/*: "http:// :*/
fileprivate let mainAddStr:String = "import mode shared succeedhttp://"

/*: "serverSelctIndex =  :*/
fileprivate let main_toStr:[Character] = ["s","e","r","v","e","r","S","e"]
fileprivate let notiBottomTitle:[Character] = ["l","c","t","I","n","d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let dreamStartText:String = "SERV"
fileprivate let k_agePath:[Character] = ["t","r"," ","="," "]

/*: "app. :*/
fileprivate let mLocationFrameText:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let dream_clearFormat:String = ".comimage name pan true"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let k_beanValue:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let mainDataContent:[Character] = ["o","m","/"]
fileprivate let dream_tooImageIdent:String = "sendGiftlab data video let title"

/*: "recharge/applePayNotify" :*/
fileprivate let mainArrayUrl:[Character] = ["r","e","c","h","a","r","g","e","/","a","p"]
fileprivate let dreamContentData:String = "plePcase edit true to"
fileprivate let main_backgroundMessage:String = "tieffecty"

/*: "user/appleLogin" :*/
fileprivate let m_moreFormat:[Character] = ["u","s","e","r","/","a","p","p","l","e","L","o","g","i","n"]

/*: "IM/getMyBlackList" :*/
fileprivate let main_wrapId:String = "on picIM/get"
fileprivate let dream_imageMessage:String = "to player view item labelckList"

/*: "mf/business/sendMsg" :*/
fileprivate let m_equalMessage:[Character] = ["m","f","/","b","u"]
fileprivate let dreamFileStreetMessage:String = "sifinishe"
fileprivate let noti_toData:String = "endMsgmask color"

/*: "mf/business/sendGift" :*/
fileprivate let notiEqualPath:String = "equal if at titlemf/bus"
fileprivate let k_indexStr:[Character] = ["n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let showFoundationName:[Character] = ["m","f","/"]
fileprivate let main_colorUrl:String = "path qualitychatRo"
fileprivate let main_titlePath:String = "endMsglevel lab visible make"

/*: "en" :*/
fileprivate let noti_beautyViewFormat:String = "eat"

/*: "ar" :*/
fileprivate let mSexUrl:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let mAppFormat:String = "styles"

/*: "pt" :*/
fileprivate let main_beginLeadingValue:String = "pinfo"

/*: "GJ21FB" :*/
fileprivate let m_cancelIndexContent:String = "gj21fb"

/*: "完成了检测,请求状态正常" :*/
fileprivate let kWayPath:[Character] = ["完","成"]
fileprivate let m_viewName:[Character] = ["\u{4e86}","检","测",",","请","求","状","态","正","常"]

/*: "接口请求失败" :*/
fileprivate let mFirstValue:String = "接\u{53e3}请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let appAgentKeyFormat:String = "完成了检\u{6d4b}"
fileprivate let notiPointName:String = "if else return title colorerver url"

/*: "HTTPProxy" :*/
fileprivate let app_centerKey:String = "Hbackback"
fileprivate let appReplyMessage:[Character] = ["P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let show_partyTitle:String = "__"
fileprivate let userPathStr:String = "SCOPED__leading image"

/*: "tap" :*/
fileprivate let user_iconId:String = "ttablep"

/*: "tun" :*/
fileprivate let m_voiceResignName:String = "permissionun"

/*: "ipsec" :*/
fileprivate let mainInfoIdent:String = "IPSEC"

/*: "ppp" :*/
fileprivate let noti_toName:String = "PPP"

/*: "func__updateTalkingCookies =  :*/
fileprivate let m_colorCellIdent:String = "array table view borderfunc_"
fileprivate let user_toolUrl:String = "user data cellateTal"
fileprivate let dream_modelTableDestroyName:String = "return share fromkingCoo"
fileprivate let kErrorFormat:String = "self modelkies = "

/*: "https:// :*/
fileprivate let dreamNorFormat:String = "https:/manager place"
fileprivate let notiColorValue:String = "/"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let mainCellMsg:[UInt8] = [0x20,0x3d,0x20,0x6c,0x72,0x55,0x68,0x74,0x69,0x57,0x73,0x65,0x69,0x6b,0x6f,0x6f,0x43,0x65,0x6d,0x61,0x53,0x65,0x74,0x65,0x6c,0x65,0x64,0x5f,0x5f,0x63,0x6e,0x75,0x66]

/*: "UID" :*/
fileprivate let appMakeStr:String = "checkD"

/*: "PHPSESSID" :*/
fileprivate let noti_birthRequestId:String = "PHPsignEsignsignID"

/*: "func__saveAllCookies =  :*/
fileprivate let mIndexUrl:String = "model more make app insidefunc__"
fileprivate let m_inputFormat:[Character] = ["l","l","C","o"]
fileprivate let m_lengthMessage:[Character] = ["o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let dreamDirectionUrl:String = "listunc"
fileprivate let dreamCornerBlockValue:String = "user level remove frame cellveAl"
fileprivate let app_viewBorderUrl:String = "kies  estatus else to"
fileprivate let k_bottomKey:[Character] = ["r","r","o","r"]

/*: "🍪 删除 :*/
fileprivate let showPlainStr:[Character] = ["🍪"," ","\u{5220}","除"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NorAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum LipogramLangType: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class NorAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = NorAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        toUser()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(dream_cellData))) || newValue.hasPrefix((mFileId.replacingOccurrences(of: "mini", with: "tt") + "s://")) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kAppValue && !kVideoTitle {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(mainAddStr.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(mainAddStr.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    orientation(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func toUser() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = k_inviteMessageOfData.integer(forKey: kEndMineValue)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(main_toStr) + String(notiBottomTitle)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = underAwake()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dream_cellData))
        //: if Environment_Formal && !Encryption_Request {
        if kAppValue && !kVideoTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(dream_cellData))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (dreamStartText.lowercased() + "erUrlS" + String(k_agePath)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func underAwake() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func funcTalkingSaveSommelierRadicalServer(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(mLocationFrameText)) + "\(k_screenRecordData)" + (String(dream_clearFormat.prefix(4))), (String(mLocationFrameText)) + "\(k_screenRecordData)" + (String(dream_clearFormat.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(k_beanValue) + String(mainDataContent) + String(dream_tooImageIdent.prefix(8))),
         //: "recharge/applePayNotify",
         (String(mainArrayUrl) + String(dreamContentData.prefix(4)) + "ayNo" + main_backgroundMessage.replacingOccurrences(of: "effect", with: "f")),
         //: "user/appleLogin"] + slowPathArr1
         (String(m_moreFormat))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(main_wrapId.suffix(6)) + "MyBla" + String(dream_imageMessage.suffix(6))),
         //: "mf/business/sendMsg",
         (String(m_equalMessage) + dreamFileStreetMessage.replacingOccurrences(of: "finish", with: "n") + "ss/s" + String(noti_toData.prefix(6))),
         //: "mf/business/sendGift",
         (String(notiEqualPath.suffix(6)) + "iness/se" + String(k_indexStr)),
         //: "mf/chatRoom/sendMsg"]
         (String(showFoundationName) + String(main_colorUrl.suffix(6)) + "om/s" + String(main_titlePath.prefix(6)))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = MacroReactiveCompatible.getSystemLangCode()
        let lang = MacroReactiveCompatible.langVideo()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .sumimateBy(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (m_cancelIndexContent.uppercased()) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension NorAddrTool {
    //: func checkServerUrl() {
    func pathUrl() {
        //: if ModeStageReactiveCompatible.share.networkStatus != .Unavailable {
        if ModeStageReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            equalHost()
            //: } else {
        } else {
            //: func_starCodeTime()
            compare()
        }
    }

    //: func func_starCodeTime() {
    func compare() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.equalHost()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func plainspokenInsideTime() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func equalHost() {
        //: if ModeStageReactiveCompatible.share.networkStatus == .Unavailable {
        if ModeStageReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        plainspokenInsideTime()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ConversationManagerRequest.straitAndNarrow { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(kWayPath) + String(m_viewName)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (mFirstValue))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.compare()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (appAgentKeyFormat + ",需要更换s" + String(notiPointName.suffix(9))))
                    //: self.func__changeHostUrl()
                    self.messageUponHost()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func messageUponHost() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        plainspokenInsideTime()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        k_inviteMessageOfData.set(serverSelctIndex, forKey: kEndMineValue)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dream_cellData))
        //: if Environment_Formal && !Encryption_Request {
        if kAppValue, !kVideoTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(dream_cellData))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension NorAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func utilized() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(app_centerKey.replacingOccurrences(of: "back", with: "T") + String(appReplyMessage))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func startFinish() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(show_partyTitle.capitalized + String(userPathStr.prefix(8)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((user_iconId.replacingOccurrences(of: "table", with: "a"))) || keyStr.contains((m_voiceResignName.replacingOccurrences(of: "permission", with: "t"))) || keyStr.contains((mainInfoIdent.lowercased())) || keyStr.contains((noti_toName.lowercased())) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension NorAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func orientation(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(m_colorCellIdent.suffix(5)) + "_upd" + String(user_toolUrl.suffix(6)) + String(dream_modelTableDestroyName.suffix(7)) + String(kErrorFormat.suffix(7))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = byBlock()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(dream_cellData)), with: (mFileId.replacingOccurrences(of: "mini", with: "tt") + "s://"))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((mFileId.replacingOccurrences(of: "mini", with: "tt") + "s://")) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(dreamNorFormat.prefix(7)) + notiColorValue.capitalized) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            upUrl(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        tennisPlayer()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func upUrl(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: mainCellMsg.reversed(), encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func byBlock() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dream_cellData))
        //: if Environment_Formal && !Encryption_Request {
        if kAppValue, !kVideoTitle {
            //: serverPrefix = "https://"
            serverPrefix = (mFileId.replacingOccurrences(of: "mini", with: "tt") + "s://")
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(mLocationFrameText)) + "\(k_screenRecordData)" + (String(dream_clearFormat.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func ofEnd(note _: Notification) {
        //: if ModeStageReactiveCompatible.share.loginUid.isEmptyString {
        if ModeStageReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            emptyVersion()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            viewAppear()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func emptyVersion() {
        //: var htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (appMakeStr.replacingOccurrences(of: "check", with: "UI")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(appMakeStr.replacingOccurrences(of: "check", with: "UI"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (noti_birthRequestId.replacingOccurrences(of: "sign", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (appMakeStr.replacingOccurrences(of: "check", with: "UI")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(appMakeStr.replacingOccurrences(of: "check", with: "UI"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (noti_birthRequestId.replacingOccurrences(of: "sign", with: "S")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((appMakeStr.replacingOccurrences(of: "check", with: "UI"))), h5Cookie.keys.contains((appMakeStr.replacingOccurrences(of: "check", with: "UI"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(appMakeStr.replacingOccurrences(of: "check", with: "UI"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(appMakeStr.replacingOccurrences(of: "check", with: "UI"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))), h5Cookie.keys.contains((noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func viewAppear() {
        //: let htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == ModeStageReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (appMakeStr.replacingOccurrences(of: "check", with: "UI")), cookie.value == ModeStageReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (noti_birthRequestId.replacingOccurrences(of: "sign", with: "S")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (appMakeStr.replacingOccurrences(of: "check", with: "UI")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(appMakeStr.replacingOccurrences(of: "check", with: "UI"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (noti_birthRequestId.replacingOccurrences(of: "sign", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((appMakeStr.replacingOccurrences(of: "check", with: "UI"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(appMakeStr.replacingOccurrences(of: "check", with: "UI"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(noti_birthRequestId.replacingOccurrences(of: "sign", with: "S"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func tennisPlayer() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(mIndexUrl.suffix(6)) + "saveA" + String(m_inputFormat) + String(m_lengthMessage)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            k_inviteMessageOfData.set(cookieData, forKey: kPageContent)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (dreamDirectionUrl.replacingOccurrences(of: "list", with: "f") + "__sa" + String(dreamCornerBlockValue.suffix(4)) + "lCoo" + String(app_viewBorderUrl.prefix(7)) + String(k_bottomKey)))
        }
    }

    //: func func__setAppCookies() {
    func clickSendCookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = k_inviteMessageOfData.object(forKey: kPageContent)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func nameState() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        k_inviteMessageOfData.removeObject(forKey: kPageContent)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
