
//: Declare String Begin

/*: / :*/
fileprivate let userSizeName:[Character] = ["/"]

/*:  ( :*/
fileprivate let mBlockName:String = " ("

/*: ; build: :*/
fileprivate let mBottomUrl:String = "add else var equal search; buil"
fileprivate let show_talkKeyMsg:[Character] = ["d",":"]

/*: ; iOS  :*/
fileprivate let mModeUrl:String = "; iOS strength size space self"

/*: ;  :*/
fileprivate let appLoadName:String = ";limit"

/*: )" :*/
fileprivate let k_acceptContent:String = "value"

/*: "audio" :*/
fileprivate let mPlayerId:String = "audframe"

/*: + :*/
fileprivate let appLayerStr:String = "method"

/*: .wav" :*/
fileprivate let showSectionNameTitle:String = ".wavby self official color lab"

/*: "audio/wav" :*/
fileprivate let kArrayValue:[Character] = ["a","u","d","i","o","/","w","a"]
fileprivate let show_shareKey:[Character] = ["v"]

/*: .jpg" :*/
fileprivate let dream_infoStr:String = ".jpgname transform color add path"

/*: "image/jpeg" :*/
fileprivate let mGiftValue:String = "imsex"
fileprivate let noti_kitName:[Character] = ["e","g"]

/*: "://app." :*/
fileprivate let m_labValue:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let app_dayTopFormat:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let noti_itemIdent:String = "open limit:null"

/*: ":{}" :*/
fileprivate let showEqualSumFormat:String = ":{}"

/*: "Request failed, Try again later" :*/
fileprivate let mainValuePath:[UInt8] = [0xcc,0xfb,0xef,0xeb,0xfb,0xed,0xea,0xbe,0xf8,0xff,0xf7,0xf2,0xfb,0xfa,0xb2,0xbe,0xca,0xec,0xe7,0xbe,0xff,0xf9,0xff,0xf7,0xf0,0xbe,0xf2,0xff,0xea,0xfb,0xec]

private func clickArea(transaction num: UInt8) -> UInt8 {
    return num ^ 158
}

/*: "json error" :*/
fileprivate let mRefreshName:[Character] = ["j","s","o","n"," ","e","r"]
fileprivate let userContentStr:[Character] = ["r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let user_actionTitle:String = "plscalefo"
fileprivate let k_reMsg:String = "hone&vlet first any"

/*: &packageId= :*/
fileprivate let mAppStr:String = "&packaarray last extension database index"

/*: &bundleId= :*/
fileprivate let noti_picSexUserUrl:String = "&bundview item path"
fileprivate let user_labName:String = "leId=var equal image model left"

/*: &lang= :*/
fileprivate let main_leadingValue:String = "super all label image origin&lang"
fileprivate let mainHiddenContent:[Character] = ["="]

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let kBuildIdent:String = "?user@#$"
fileprivate let userModelFormat:String = "^&%"
fileprivate let app_statusPath:[Character] = ["*","+",",",":",";","=","\'"]

/*: "`<>()[]{}/\\| " :*/
fileprivate let noti_aMessage:String = "`<>("
fileprivate let showFatalValue:[Character] = ["|"," "]

/*: "/route/slowindex" :*/
fileprivate let userShowValueHeightKey:[Character] = ["/","r","o","u","t","e"]
fileprivate let dream_resultValue:[Character] = ["/","s","l","o","w"]
fileprivate let dreamAcceptKey:String = "inmodelex"

/*: "/route/index" :*/
fileprivate let user_clickPhotoMessage:String = "cell record name/rou"
fileprivate let dreamFeatureUrl:[Character] = ["n","d","e","x"]

/*: "?path= :*/
fileprivate let notiEqualName:String = "file share size?path"
fileprivate let noti_commitName:String = "="

/*: "&GJNonceStr= :*/
fileprivate let kCustomPointMessage:[Character] = ["&","G","J","N","o","n","c","e","S","t"]
fileprivate let show_valueMessage:String = "if tool flexibler="

/*: "& :*/
fileprivate let user_randomTitle:[Character] = ["&"]

/*: = :*/
fileprivate let noti_captureKey:String = "="

/*: "&params= :*/
fileprivate let showShareTitle:String = "size add&par"

/*: "/ :*/
fileprivate let dreamActualUrl:String = "/"

/*: "? :*/
fileprivate let appContainerMsg:String = "name"

/*: "token" :*/
fileprivate let m_giftFrameContent:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let appModelMessage:[Character] = ["\u{8bf7}","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let noti_arrayId:String = "tototoA"
fileprivate let k_windowData:[Character] = ["A"]

/*: "AA" :*/
fileprivate let mFillMessage:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let dreamValueMessage:String = "bb"

/*: "下载完成 -  :*/
fileprivate let app_colorMessage:String = "下载\u{5b8c}成 - "

/*: "/Documents/" :*/
fileprivate let app_currentName:[Character] = ["/","D","o","c","u"]
fileprivate let k_errorUrl:[Character] = ["m","e","n","t","s","/"]

/*: ".mp4" :*/
fileprivate let mainColorTitle:String = ".mp4view value table self title"

/*: "移动地址: :*/
fileprivate let main_titleId:String = "移动地\u{5740}:"

/*: "后台任务下载回来" :*/
fileprivate let appLayerImageText:String = "\u{540e}台\u{4efb}务下载回"
fileprivate let main_fileContent:String = "来"

/*: "下载成功 : :*/
fileprivate let showDownId:String = "下载time :"

/*: "Net Error" :*/
fileprivate let mainOriginPathText:[Character] = ["N","e","t"," ","E","r","r","o"]
fileprivate let app_cellMakeId:String = "R"

/*: "下载失败 : :*/
fileprivate let show_datePath:String = "\u{4e0b}载失\u{8d25} "
fileprivate let user_pathStr:String = ":"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SucceedSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kSkipValue = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kIdentityName = "\(k_domainValue)/\(k_likeValue) (\(kSystemName)" + (String(mBottomUrl.suffix(6)) + String(show_talkKeyMsg)) + "\(k_filterName)" + (String(mModeUrl.prefix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: DetailTopicErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let kUseSenseName = SucceedSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class SucceedSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func underway(model: SmallnessThen, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.animaAdd() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.pushRange(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.liquidEcstasy(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func pushRange(model: SmallnessThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.reinInModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.juristModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    formatEqual(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.NetError.rawValue, errorMsg: kTimeLineValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        actionKey(showMsg: kTimeLineValue)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (mPlayerId.replacingOccurrences(of: "frame", with: "io")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.shadiness())+\(k_messageFlushValue)" + (String(showSectionNameTitle.prefix(4))), mimeType: (String(kArrayValue) + String(show_shareKey)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(dream_infoStr.prefix(4))), mimeType: (mGiftValue.replacingOccurrences(of: "sex", with: "ag") + "e/jp" + String(noti_kitName)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    formatEqual(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.NetError.rawValue, errorMsg: kTimeLineValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        actionKey(showMsg: kTimeLineValue)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func liquidEcstasy(model: SmallnessThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.reinInModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.juristModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: kVideoTitle ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kSkipValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    formatEqual(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.NetError.rawValue, errorMsg: kTimeLineValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        actionKey(showMsg: kTimeLineValue)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kSkipValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    formatEqual(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.NetError.rawValue, errorMsg: kTimeLineValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        actionKey(showMsg: kTimeLineValue)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func formatEqual(model: SmallnessThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            NorAddrTool.share.orientation(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if kVideoTitle, model.requestServer.contains((String(m_labValue))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.adjustColor(key: NorAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(app_dayTopFormat)) + "\"" + (String(noti_itemIdent.suffix(5))), with: "" + "\"" + (String(app_dayTopFormat)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !kVideoTitle {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(noti_itemIdent.suffix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<PresentMeasurable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == InfoBinaryCount.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, DetailTopicErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    actionKey(showMsg: responseModel.msg ?? String(bytes: mainValuePath.map{clickArea(transaction: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case InfoBinaryCount.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: k_dismissData, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.NetError.rawValue, errorMsg: (String(mRefreshName) + String(userContentStr)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func reinInModel(model: SmallnessThen) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (user_actionTitle.replacingOccurrences(of: "scale", with: "at") + "rm=ip" + String(k_reMsg.prefix(6)) + "ersion=") + "\(k_screenValue)" + (String(mAppStr.prefix(6)) + "geId=") + "\(k_messageFlushValue)" + (String(noti_picSexUserUrl.prefix(5)) + String(user_labName.prefix(5))) + "\(kSystemName)" + (String(main_leadingValue.suffix(5)) + String(mainHiddenContent)) + "\(NorAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if kVideoTitle, serverUrl.contains((String(m_labValue))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if NorAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(userShowValueHeightKey) + String(dream_resultValue) + dreamAcceptKey.replacingOccurrences(of: "model", with: "d")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(user_clickPhotoMessage.suffix(4)) + "te/i" + String(dreamFeatureUrl))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(notiEqualName.suffix(5)) + noti_commitName.capitalized) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(kCustomPointMessage) + String(show_valueMessage.suffix(2))) + "\(NorAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.liveSendKey(key: NorAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(showShareTitle.suffix(4)) + "ams=") + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func juristModel(model: SmallnessThen) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kIdentityName)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(m_giftFrameContent)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func obscure(model: SmallnessThen) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.reinInModel(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(appModelMessage)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (noti_arrayId.replacingOccurrences(of: "to", with: "AA") + String(k_windowData))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (app_colorMessage) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(app_currentName) + String(k_errorUrl)) + NSDate.shadiness() + (String(mainColorTitle.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (main_titleId) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (appLayerImageText + main_fileContent.capitalized))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = SourceReactiveCompatible.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func buttonSend(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: kVoiceName.format(), with: k_screenRecordData)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (showDownId.replacingOccurrences(of: "time", with: "成功")) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.Normal.rawValue, errorMsg: (String(mainOriginPathText) + app_cellMakeId.lowercased()).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.NetError.rawValue, errorMsg: (String(mainOriginPathText) + app_cellMakeId.lowercased()).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (show_datePath + user_pathStr.capitalized) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func appearanceIn(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: kVoiceName.format(), with: k_screenRecordData)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (showDownId.replacingOccurrences(of: "time", with: "成功")) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.Normal.rawValue, errorMsg: (String(mainOriginPathText) + app_cellMakeId.lowercased()).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, DetailTopicErrorResponse(errorCode: InfoBinaryCount.NetError.rawValue, errorMsg: (String(mainOriginPathText) + app_cellMakeId.lowercased()).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (show_datePath + user_pathStr.capitalized) + "\(errMsg)")
            }
        }
    }
}
