
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_maxPath:[UInt8] = [0xfc,0xfb,0xfc,0xe1,0xbd,0xf6,0xfa,0xf1,0xf0,0xe7,0xaf,0xbc,0xb5,0xfd,0xf4,0xe6,0xb5,0xfb,0xfa,0xe1,0xb5,0xf7,0xf0,0xf0,0xfb,0xb5,0xfc,0xf8,0xe5,0xf9,0xf0,0xf8,0xf0,0xfb,0xe1,0xf0,0xf1]

/*: "Load..." :*/
fileprivate let m_viewKey:String = "to"
fileprivate let dreamBackgroundUrl:String = "oad...male model of to if"

/*: "Skip" :*/
fileprivate let m_closeTitle:String = "Skipreturn data center mini"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let mainValueContent:[UInt8] = [0x1,0x3d,0x3d,0x39,0x1d,0x26,0x26,0x25,0x67,0x7,0x28,0x3d,0x20,0x3f,0x2c,0x1d,0x26,0x3,0x3a,0x61,0x6e,0x3b,0x2c,0x2a,0x21,0x28,0x3b,0x2e,0x2c,0x6e,0x60]

private func liveCell(hidden num: UInt8) -> UInt8 {
    return num ^ 73
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let notiTitleContent:[UInt8] = [0x5f,0x41,0x46,0x4c,0x47,0x5f,0x6,0x47,0x46,0x78,0x49,0x4f,0x4d,0x7b,0x40,0x47,0x5f,0xe,0xe,0x47,0x46,0x78,0x49,0x4f,0x4d,0x7b,0x40,0x47,0x5f,0x0,0x1,0x13]

private func valueLast(than num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let dreamTimeText:String = "jsEventvar list item holder make"
fileprivate let k_screenFormat:String = "view view(onP"
fileprivate let appFilePath:String = "how): shrink cell background image"

/*: --- :*/
fileprivate let m_videoSizePath:String = "kitkitkit"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let noti_observerText:[UInt8] = [0xd,0x13,0x14,0x1e,0x15,0xd,0x54,0x15,0x14,0x2a,0x1b,0x1d,0x1f,0x32,0x13,0x1e,0x1f,0x5c,0x5c,0x15,0x14,0x2a,0x1b,0x1d,0x1f,0x32,0x13,0x1e,0x1f,0x52,0x53,0x41]

/*: "jsEvent(onPageHide):  :*/
fileprivate let noti_standardUserIdent:String = "icon screen tap case labeljsEve"
fileprivate let notiLabelKey:String = "succeed"
fileprivate let k_effectData:String = "PageHireturn var send"

/*: "title" :*/
fileprivate let kLabKey:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let dream_endText:String = "canGoBaresponse from date"
fileprivate let notiToData:String = "cbackground"

/*: "PHPSESSID= :*/
fileprivate let k_countValue:String = "php"
fileprivate let main_shareCellPath:[Character] = ["S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let m_rateId:String = "item user bottom background actual;uid="

/*: "Cookie" :*/
fileprivate let app_languageContent:[Character] = ["C","o","o","k","i"]
fileprivate let m_backData:[Character] = ["e"]

/*: "nav_back_black_nor" :*/
fileprivate let appExceptionPath:[Character] = ["n","a","v","_","b","a","c","k","_","b","l"]
fileprivate let k_leadingFormat:String = "ack_nordata on gesture"

/*: "ico_intimate_list_sm" :*/
fileprivate let noti_toKey:String = "view equal color layerico_inti"
fileprivate let k_messageTagUrl:[Character] = ["m","a","t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let k_userPath:[UInt8] = [0xb1,0xba,0xb6,0xa0,0xb8,0xb0,0xbb,0xa1,0xfb,0xb2,0xb0,0xa1,0x90,0xb9,0xb0,0xb8,0xb0,0xbb,0xa1,0xa6,0x97,0xac,0x81,0xb4,0xb2,0x9b,0xb4,0xb8,0xb0,0xfd,0xf2,0xb7,0xba,0xb1,0xac,0xf2,0xfc,0x8e,0xe5,0x88,0xfb,0xa6,0xa1,0xac,0xb9,0xb0,0xfb,0xb7,0xb4,0xb6,0xbe,0xb2,0xa7,0xba,0xa0,0xbb,0xb1,0xe8,0xf2,0xa7,0xb2,0xb7,0xb4,0xfd,0xe5,0xf9,0xe5,0xf9,0xe5,0xf9,0xe5,0xfc,0xf2]

private func iconTransform(bottom num: UInt8) -> UInt8 {
    return num ^ 213
}

/*: "Alert" :*/
fileprivate let user_sexStr:String = "count mic window typeAlert"

/*: "OK" :*/
fileprivate let notiTitleKey:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let show_colorLineData:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let show_normallyMessage:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let showUpIdent:String = "true heightjs cal"
fileprivate let showFrameFirstManagerName:String = "else info datathod n"

/*: , message =  :*/
fileprivate let kCancelName:String = ", mevoice height hidden to string"
fileprivate let show_lengthTitle:[Character] = ["e"," ","="," "]

/*: "http://" :*/
fileprivate let show_finishUrl:String = "http:view lab"
fileprivate let app_layerHandlePath:String = "//"

/*: "https://" :*/
fileprivate let k_sunSpaceTitle:String = "cell"
fileprivate let notiDetailStr:[Character] = ["t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let main_distanceExitVarData:[UInt8] = [0xa,0x3b,0x29,0x20,0x7d,0x20,0x5d,0x30,0x5b,0x29,0x27,0x3d,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x6e,0x72,0x75,0x74,0x65,0x72,0x20,0x7b,0x20,0x29,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x28,0x70,0x61,0x6d,0x2e,0x29,0x27,0x20,0x3b,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x3d,0x20,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x72,0x61,0x76]

/*: "uid= :*/
fileprivate let noti_bagTitle:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let showModelKey:String = ";domain=asset name name return app"

/*: ;path=/" :*/
fileprivate let mCommentKey:String = "model make name to;path=/"

/*: "document.cookie=' :*/
fileprivate let mColorMsg:String = "docate"
fileprivate let appBrowStr:[Character] = ["o","o","k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let show_toValue:String = "\';\n"

/*: ;domin= :*/
fileprivate let app_colorValue:[Character] = [";","d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let showAllInfoIdent:[UInt8] = [0xc5,0xca,0x8c,0x84,0xcf,0xc3,0xc3,0xc7,0xc5,0xc9,0xe2,0xcd,0xc1,0xc9,0xdf,0x82,0xc5,0xc2,0xc8,0xc9,0xd4,0xe3,0xca,0x84,0x8b,0xfc,0xe4,0xfc,0xff,0xe9,0xff,0xff,0xe5,0xe8,0x8b,0x85,0x8c,0x91,0x91,0x8c,0x81,0x9d,0x85,0x8c,0xd7,0x8c,0xc8,0xc3,0xcf,0xd9,0xc1,0xc9,0xc2,0xd8,0x82,0xcf,0xc3,0xc3,0xc7,0xc5,0xc9,0x91,0x8b]

private func atVideo(head num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: '; };\n" :*/
fileprivate let notiSectionStr:String = "\'; };\n"

/*: '" :*/
fileprivate let dream_borderTitle:String = "\'"

/*: "h5Domain =  :*/
fileprivate let notiToConvertPath:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let notiTurnPath:String = "getAlmake model var"
fileprivate let userAddMessage:String = "if top typekies = "

/*: "PHPSESSID" :*/
fileprivate let mAdMsg:String = "image"
fileprivate let kClickTitle:String = "HrecordE"

/*: "uid" :*/
fileprivate let user_whiteUrl:String = "named"

/*: "appCookies =  :*/
fileprivate let k_bottomValue:String = "let in useappC"

/*: "UID" :*/
fileprivate let appGenderValue:String = "UIstyle"

/*: "navigator.userAgent" :*/
fileprivate let kSexId:[Character] = ["n","a","v","i","g","a","t","o","r"]
fileprivate let appNorMessage:[Character] = [".","u","s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let dream_blockMsg:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"]

/*: "  :*/
fileprivate let dreamEqualUrl:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let appIconMsg:String = "make share xun"
fileprivate let dreamTextTitle:String = "model viewce_id/"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let m_atStr:String = "xofnw"
fileprivate let dream_bySaveMsg:String = "count framehone x"
fileprivate let dreamValueMediaMessage:String = "make fatalunwu_"

/*:  xunwu_package/ :*/
fileprivate let main_ofEqualKey:String = "button task sure self array xun"
fileprivate let show_onPathAtData:String = "class table colorkage/"

/*:  xunwu_bundleId/ :*/
fileprivate let kElementTitle:String = " xunwu_bname view return"
fileprivate let show_infoMsg:String = "undleId/class data"

/*:   webkit/1" :*/
fileprivate let kCellIdent:[Character] = [" "," ","w","e","b"]
fileprivate let showAllValue:String = "value self make formatkit/1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NorViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class NorViewController: SoulRecognizerDelegate {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: BarbConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: ToOutputStreamable?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: ToOutputStreamable?, webConfig: BarbConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_maxPath.map{$0^149}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        dateCell()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        obscureFrameView()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (m_viewKey.replacingOccurrences(of: "to", with: "L") + String(dreamBackgroundUrl.prefix(6)))
        //: addNotification()
        notificationMode()
        //: func__checkConfig()
        firstIconConfig()
        //: func__setUpWebView()
        extendedHead()
        //: func__registerObserver()
        inscribeFor()
        //: func__beginStartRequest()
        rowPackage()
        //: func_checkAppleIAP()
        canalise()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: k_messageData, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            infoConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.subject(), .font: UIFont.fontFile(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(m_closeTitle.prefix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension NorViewController {
    /// 添加通知
    //: private func addNotification() {
    private func notificationMode() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(down),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: k_onName,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(dateCell),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func down() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: mainValueContent.map{liveCell(hidden: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func dateCell() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiTitleContent.map{valueLast(than: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(dreamTimeText.prefix(7)) + String(k_screenFormat.suffix(4)) + "ageS" + String(appFilePath.prefix(6))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func obscureFrameView() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: noti_observerText.map{$0^122}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(noti_standardUserIdent.suffix(5)) + "nt(o" + notiLabelKey.replacingOccurrences(of: "succeed", with: "n") + String(k_effectData.prefix(6)) + "de): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension NorViewController {
    //: func func__registerObserver() {
    func inscribeFor() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(kLabKey)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(dream_endText.prefix(7)) + notiToData.replacingOccurrences(of: "background", with: "k")))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(dataView), name: kViewData, object: nil)
    }

    //: func func__setUpWebView() {
    func extendedHead() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        insideInformation()
        //: func__addBridgeMethod()
        allMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.subTarget()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            AnagrammatizeView.genderIdentity(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(kPositionName)
            }
        }
    }

    //: func func__beginStartRequest() {
    func rowPackage() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.duringVoice()
            }
            //: } else {
        } else {
            //: func__startRequest()
            duringVoice()
        }
    }

    //: func func__startRequest() {
    func duringVoice() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = ModeStageReactiveCompatible.share.loginUid
        let uid = ModeStageReactiveCompatible.share.loginUid
        //: let ssid = ModeStageReactiveCompatible.share.loginSessionId
        let ssid = ModeStageReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (k_countValue.uppercased() + String(main_shareCellPath)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(m_rateId.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(app_languageContent) + String(m_backData)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        coloration()
        //: func__initRightButton()
        imageWrap()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func canalise() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            TitleRequestDelegate.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.contribute(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            TitleRequestDelegate.shared.canvassDeleteTransactionsView()
        }
    }

    //: func func__initLeftButton() {
    func decide() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.namedImageNameBundle(name: (String(appExceptionPath) + String(k_leadingFormat.prefix(7)))), style: .plain, target: self, action: #selector(toPopComponent(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func imageWrap() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(ToOutputStreamable.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.namedImageNameBundle(name: (String(noti_toKey.suffix(8)) + String(k_messageTagUrl))), style: .plain, target: self, action: #selector(ignitionKey))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func toPopComponent(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        liftOutJudge()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.startScreen() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: NorViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! NorViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! NorViewController).dateCell()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func firstIconConfig() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func gammaHydroxybutyrate(sucess: Bool) {
        //: ProgressHUD.dismiss()
        AnagrammatizeView.implant()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func coloration() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: k_userPath.map{iconTransform(bottom: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func statusToRemove(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: SucceedInfoReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            SucceedInfoReactiveCompatible.share.untilEqual(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func unseal(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func dataView() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kViewData, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func ignitionKey() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = OutlineReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText))
        //: view.show()
        view.fillDown()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension NorViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            AnagrammatizeView.genderIdentity(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            gammaHydroxybutyrate(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        decide()
        //: func__clearJSBgColor()
        coloration()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            gammaHydroxybutyrate(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            gammaHydroxybutyrate(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(user_sexStr.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(show_colorLineData)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(show_normallyMessage)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension NorViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(showUpIdent.suffix(6)) + "l me" + String(showFrameFirstManagerName.suffix(6)) + "ame = ") + "\(message.name)" + (String(kCancelName.prefix(4)) + "ssag" + String(show_lengthTitle)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.telegramValue(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func allMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in CreateConstraintCaseIterable.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func liftOutJudge() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in CreateConstraintCaseIterable.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension NorViewController {
    //: func func__addUserScript() {
    func exceptCookie() {
        //: var htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(show_finishUrl.prefix(5)) + app_layerHandlePath.capitalized)) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(show_finishUrl.prefix(5)) + app_layerHandlePath.capitalized), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((k_sunSpaceTitle.replacingOccurrences(of: "cell", with: "h") + String(notiDetailStr))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (k_sunSpaceTitle.replacingOccurrences(of: "cell", with: "h") + String(notiDetailStr)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = ModeStageReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = ModeStageReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = ModeStageReactiveCompatible.share.loginSessionId
        let tempSessionID: String = ModeStageReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: main_distanceExitVarData.reversed(), encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_bagTitle)) + "\(tempUid)" + (String(showModelKey.prefix(8))) + "\(htmlDomain)" + (String(mCommentKey.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((mColorMsg.replacingOccurrences(of: "at", with: "um") + "nt.c" + String(appBrowStr)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (k_countValue.uppercased() + String(main_shareCellPath)) + "\(tempSessionID)" + (String(app_colorValue)) + "\(htmlDomain)" + (String(mCommentKey.suffix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((mColorMsg.replacingOccurrences(of: "at", with: "um") + "nt.c" + String(appBrowStr)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: showAllInfoIdent.map{atVideo(head: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_bagTitle)) + "\(tempUid)" + (String(showModelKey.prefix(8))) + "\(htmlDomain)" + (String(mCommentKey.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((mColorMsg.replacingOccurrences(of: "at", with: "um") + "nt.c" + String(appBrowStr)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_bagTitle)) + "\(tempSessionID)" + (String(showModelKey.prefix(8))) + "\(htmlDomain)" + (String(mCommentKey.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((mColorMsg.replacingOccurrences(of: "at", with: "um") + "nt.c" + String(appBrowStr)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func infoConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(notiToConvertPath)) + "\(htmlDomain)")
            //: let userID: String = ModeStageReactiveCompatible.share.loginUserMode.userID
            let userID: String = ModeStageReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(notiTurnPath.prefix(5)) + "lCoo" + String(userAddMessage.suffix(7))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (mAdMsg.replacingOccurrences(of: "image", with: "P") + kClickTitle.replacingOccurrences(of: "record", with: "PS") + "SSID") {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (user_whiteUrl.replacingOccurrences(of: "name", with: "ui")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.rowPackage()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.makeweightPath(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func makeweightPath(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = ModeStageReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(notiToConvertPath)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: NorAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(k_bottomValue.suffix(4)) + "ookies = ") + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (appGenderValue.replacingOccurrences(of: "style", with: "D")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (user_whiteUrl.replacingOccurrences(of: "name", with: "ui"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (mAdMsg.replacingOccurrences(of: "image", with: "P") + kClickTitle.replacingOccurrences(of: "record", with: "PS") + "SSID"), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (mAdMsg.replacingOccurrences(of: "image", with: "P") + kClickTitle.replacingOccurrences(of: "record", with: "PS") + "SSID")
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.rowPackage()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func insideInformation() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(kSexId) + String(appNorMessage))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(kSexId) + String(appNorMessage)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(dream_blockMsg))) {
                //: let str = getUserAgentString()
                let str = represserTitle()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.cake { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(appIconMsg.suffix(4)) + "wu_devi" + String(dreamTextTitle.suffix(6))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func represserTitle() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (m_atStr.replacingOccurrences(of: "of", with: "u") + "u_ip" + String(dream_bySaveMsg.suffix(6)) + String(dreamValueMediaMessage.suffix(5)) + "version/") + "\(k_screenValue)" + (String(main_ofEqualKey.suffix(4)) + "wu_pac" + String(show_onPathAtData.suffix(5))) + "\(k_messageFlushValue)" + (String(kElementTitle.prefix(8)) + String(show_infoMsg.prefix(8))) + "\(kSystemName)" + (String(kCellIdent) + String(showAllValue.suffix(5)))
    }
}
