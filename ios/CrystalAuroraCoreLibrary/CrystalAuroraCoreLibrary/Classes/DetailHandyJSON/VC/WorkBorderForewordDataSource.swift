
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainBottomTitle:[UInt8] = [0x90,0x97,0x90,0x8d,0xd1,0x9a,0x96,0x9d,0x9c,0x8b,0xc3,0xd0,0xd9,0x91,0x98,0x8a,0xd9,0x97,0x96,0x8d,0xd9,0x9b,0x9c,0x9c,0x97,0xd9,0x90,0x94,0x89,0x95,0x9c,0x94,0x9c,0x97,0x8d,0x9c,0x9d]

/*: "Text greeting" :*/
fileprivate let dream_slideData:String = "Text to back"
fileprivate let mInviteFormat:String = "nshare"

/*: "Add" :*/
fileprivate let show_lastIndexValue:String = "Addshare left"

/*: "type" :*/
fileprivate let user_deviceTablePathStr:String = "tviewe"

/*: "addStatus" :*/
fileprivate let m_tabIdent:String = "path true title to cameraaddStat"
fileprivate let notiRestoreKey:[Character] = ["u","s"]

/*: "list" :*/
fileprivate let appAddFormat:String = "liveist"

/*: "Are you sure to delete this message？" :*/
fileprivate let user_subjectMessage:[UInt8] = [0xc,0x3f,0x28,0x6d,0x34,0x22,0x38,0x6d,0x3e,0x38,0x3f,0x28,0x6d,0x39,0x22,0x6d,0x29,0x28,0x21,0x28,0x39,0x28,0x6d,0x39,0x25,0x24,0x3e,0x6d,0x20,0x28,0x3e,0x3e,0x2c,0x2a,0x28,0xa2,0xf1,0xd2]

/*: "Cancel" :*/
fileprivate let show_rugStr:String = "Cancelmessage value case"

/*: "Delete" :*/
fileprivate let mContentStr:String = "let false height frame labDelete"

/*: "id" :*/
fileprivate let appEqualKey:[Character] = ["i","d"]

/*: "The upper limit has been reached" :*/
fileprivate let k_modelDeleteMakeMsg:[UInt8] = [0x41,0x55,0x52,0xd,0x62,0x5d,0x5d,0x52,0x5f,0xd,0x59,0x56,0x5a,0x56,0x61,0xd,0x55,0x4e,0x60,0xd,0x4f,0x52,0x52,0x5b,0xd,0x5f,0x52,0x4e,0x50,0x55,0x52,0x51]

fileprivate func fatalCell(succeed num: UInt8) -> UInt8 {
    let value = Int(num) - 237
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "text" :*/
fileprivate let mainMakeTitle:[Character] = ["t","e","x","t"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let show_nextUrl:[UInt8] = [0xa,0x2c,0x3a,0x3a,0x3c,0x2a,0x2a,0x3f,0x2c,0x35,0x35,0x20,0x79,0x38,0x3d,0x3d,0x3c,0x3d,0x75,0x79,0x29,0x35,0x3c,0x38,0x2a,0x3c,0x79,0x2e,0x38,0x30,0x2d,0x79,0x3f,0x36,0x2b,0x79,0x38,0x29,0x29,0x2b,0x36,0x2f,0x38,0x35]

private func sizeFailure(manager num: UInt8) -> UInt8 {
    return num ^ 89
}

/*: "Any-Hex/Java" :*/
fileprivate let appReportChangeData:String = "Any-Hpath true true less user"

/*: "#FF506D" :*/
fileprivate let kServerRequestColorPath:[Character] = ["#","F","F","5","0","6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let show_factorName:String = "btn_in icon"
fileprivate let app_shadowName:[Character] = ["m","e","s","s","a","g","e","_","d","e","l","e","t","e","_","n","o","r"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let notiRandomName:[UInt8] = [0xda,0xf5,0xf0,0xfa,0xf2,0xb9,0xed,0xf1,0xfc,0xb9,0xfb,0xec,0xed,0xed,0xf6,0xf7,0xb9,0xfb,0xfc,0xf5,0xf6,0xee,0xb9,0xed,0xf6,0xb9,0xf8,0xfd,0xfd,0x93,0xfa,0xf6,0xf7,0xed,0xfc,0xf7,0xed]

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let noti_showName:[UInt8] = [0x34,0xb,0x6,0xd,0x43,0x2,0x7,0x7,0xa,0xd,0x4,0x43,0xe,0x16,0xf,0x17,0xa,0x13,0xf,0x6,0x43,0x17,0x6,0x1b,0x17,0x43,0xe,0x6,0x10,0x10,0x2,0x4,0x6,0x4f,0x43,0x17,0xb,0x6,0x43,0x10,0x1a,0x10,0x17,0x43,0x6,0xe,0x43,0x14,0xa,0xf,0xf,0x43,0x10,0x6,0xd,0x7,0x43,0x17,0xb,0x6,0xe,0x43,0x1,0x1a,0x43,0x17,0x16,0x11,0xd,0x10,0x58,0x10,0xf,0xa,0x7,0x6,0x43,0x17,0xc,0x43,0x17,0xb,0x6,0x43,0xe,0x6,0x10,0x10,0x2,0x4,0x6,0x43,0x17,0xc,0x43,0x7,0x6,0xf,0x6,0x17,0x6]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WorkBorderForewordDataSource.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class WorkBorderForewordDataSource: StateViewController {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: SourceThen?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainBottomTitle.map{$0^249}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(dream_slideData.prefix(5)) + "greeti" + mInviteFormat.replacingOccurrences(of: "share", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.subTarget()
        //: designView()
        lab()
        //: getAudioListData()
        priceList()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [FrameNorTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [FrameNorTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: kHalfText - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(show_lastIndexValue.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.fontFile(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.versionSize(colors: UIColor.errorSuccess(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(socialShow), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension WorkBorderForewordDataSource {
    //: func getAudioListData() {
    func priceList() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(user_deviceTablePathStr.replacingOccurrences(of: "view", with: "yp"))] = 2
        //: ProgressHUD.show()
        AnagrammatizeView.titleShowCurrent()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        InfoDetailRequestTool.popComeUpon(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            AnagrammatizeView.implant()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.trifle()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(m_tabIdent.suffix(7)) + String(notiRestoreKey))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(appAddFormat.replacingOccurrences(of: "live", with: "l"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [FrameNorTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<FrameNorTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func butylate(index: Int) {
        //: let config = ShowAlertConfig()
        let config = SpaceAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.fontFile(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        AlertInfoThen.mug(message: String(bytes: user_subjectMessage.map{$0^77}, encoding: .utf8)!.localized, leftBtnTitle: (String(show_rugStr.prefix(6))).localized, rightBtnTitle: (String(mContentStr.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            AlertInfoThen.showAlert()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            AlertInfoThen.showAlert()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: FrameNorTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            AnagrammatizeView.titleShowCurrent()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            InfoDetailRequestTool.consultation(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                AnagrammatizeView.implant()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.priceList()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension WorkBorderForewordDataSource {
    //: @objc func addButtonClick() {
    @objc func socialShow() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.actionKey(showMsg: String(bytes: k_modelDeleteMakeMsg.map{fatalCell(succeed: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = SourceThen(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.totalerplay()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.chalkLineCell(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(user_deviceTablePathStr.replacingOccurrences(of: "view", with: "yp"))] = 2
            //: dict["text"] = tempStr
            dict[(String(mainMakeTitle))] = tempStr
            //: ProgressHUD.show()
            AnagrammatizeView.titleShowCurrent()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            InfoDetailRequestTool.viewControl(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                AnagrammatizeView.implant()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.logArea(showMsg: String(bytes: show_nextUrl.map{sizeFailure(manager: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.priceList()
                //: self.recordView?.dismiss()
                self.recordView?.textAt()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func chalkLineCell(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(appReportChangeData.prefix(5)) + "ex/Java") as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension WorkBorderForewordDataSource: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: kBlockRateValue - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.fontFile(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetTextCell.className()
        let identifier = CompartmentThen.className()
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: CompartmentThen = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! CompartmentThen

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.title(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(mContentStr.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.butylate(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(kServerRequestColorPath)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.butylate(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.namedImageNameBundle(name: (String(show_factorName.prefix(4)) + String(app_shadowName))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = SucceedRowWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension WorkBorderForewordDataSource {
    //: private func setupEmptyDataView() {
    private func trifle() {
        //: var style = EmptyStyle()
        var style = AtAdEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: notiRandomName.map{$0^153}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .subTarget()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func lab() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: TalkingGreetTextCell.className())
        MainTable.register(CompartmentThen.self, forCellReuseIdentifier: CompartmentThen.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kBlockRateValue, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.totalervertebralDisc()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.font(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: noti_showName.map{$0^99}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
